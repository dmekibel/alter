# BUILD SPEC — HOME 2c (David's pick, 2026-08-02) — "clean edges, garden top-right, chevrons top and bottom"
*Source of truth, in order: David's words in chat ("exactly like my screenshots"; "no white outline around the selected tile") → his two rendered frames (home + open dose card) → the live 2c markup pulled from Claude Design ("Home Screen.dc.html", frame `data-screen-label="2c Home shipped..."`, data model read in full). All px/hex below are QUOTED from that artifact. Tile/step HUES keep reading the app's own TBX_HEX registry (the mock rebuilt the shipped app from screenshot; its hues are approximations of ours). Opus agent, SOLO, no commit, no preship. Regions: the home face (@SEC:COCKPIT renderTrackerFull home face + @SEC:TOOLBOX2 shared dose-card builder) + designAudit (@SEC:DEV). Timeline/regression zone untouched. The GROUND toolbox (8-grid, heroes, folders) stays exactly as shipped; 2c changes the HOME FACE above it.*

## 1. Top HUD row (replaces the current clock-left + gems-right row; the home face shows NO clock)
Row: `display:flex; align-items:center; gap:14px;` at the top of the home face (respect existing safe-area top handling).
- LEFT sparkle button: 28x28 circle, `border:2px solid rgba(255,242,249,.16); background:#2a1220;` icon `ti-sparkles` 13px `#ff8fc0`. Action: open the existing character card (`characterCard()`), SAME as tapping the guardian mirror. FLAG in report: mapping is my inference, David verdicts.
- CENTER (absolutely centered): chevron-up `ti-chevron-up` 20px `#c77ea3` with float animation (`@keyframes hintFloat { 0%,100% { transform:translateY(0); opacity:.55; } 50% { transform:translateY(5px); opacity:1; } }` 2.2s ease-in-out infinite) above label "JOURNEY" 9px/800/letter-spacing 2px `#8a6d85` (through tr(), RU "ПУТЬ"). Action: smooth-scroll #tfWorld to the sky (the journey trail), the same target the upward drag reaches.
- RIGHT garden chip (the "soft chip" variant, the artifact default): `border:2px solid rgba(79,208,138,.4); background:rgba(79,208,138,.1); padding:5px 11px; border-radius:999px;` icon `ti-leaf` 15px `#4fd08a`. Action: `openGame()` (the garden/world door).
- FAR RIGHT gems: `ti-diamond-filled` 17px + count, Baloo 2 800 20px `#ffc41f`, gap 6 (reuse the existing gem count source).

## 2. Week strip v2 (the existing 5-dash strip upgrades in place)
- Pills: 5 × `flex:1; height:13px; border-radius:999px;` gap 9px + trailing `ti-chevron-right` 15px `#8a6d85` (keep the strip's existing tap handler). Fill: a day WITH logged practice = that day's dominant logged DOMAIN hue from the app registry (DOM/TBX_HEX); a day without = `#2e1a28`.
- NEW icon row under the pills: `display:grid; grid-template-columns:repeat(5,1fr); gap:9px; margin-top:11px; margin-right:24px;` (right margin excludes the chevron column so icons center under pills). Per day: the dominant logged activity's ti icon, 17px, centered; colored in its domain hue when the day has practice, else `#5a3f55`. No logged data at all → the icon row still renders with all-dim placeholder icons (reuse each weekday's would-be habit icon or `ti-circle` dim; builder picks the cleanest honest empty state and reports it).
- Strip block spacing: `margin-top:12px` under the HUD; icons `margin-top:11px` under pills.

## 3. Edges cleared
- REMOVE the left/right edge door tabs from the home face (the planner/garden side tabs). Garden access = the leaf chip (§1). Planner access = Plan-my-day + the puck (unchanged). Keep the door ELEMENTS in the file if other faces use them; on the one-page home they must not render. 
- REMOVE the home face clock readout (status bar shows time). The tracker/live faces keep their own clocks; only the calm home face loses it.

## 4. The board center (UNCHANGED — locked tuner numbers rule)
Date kicker, disc (52vw etc.), "What now?", the sub-line, Plan-my-day: all keep their shipped locked geometry. The mock's own sizes are canvas fits, not orders. Do not touch tickers or handlers.

## 5. The HERO ROW + inline dose card on the home face (the big piece)
- Under Plan-my-day: 4 tiles in `grid-template-columns:repeat(4,1fr); gap:8px; padding:0 34px;` ids: `firstLight, breatheLadder, mind, shutdown` (artifact gIdx [0,1,3,6] = Morning Stack, Breathe, Meditate, Night Stack).
- Tile primitive (matches the shipped stkCard family): 50x50 face `border-radius:18px`, lip `box-shadow:0 4px 0 <deep>` (deep = the shipped stkLip/50-percent-toward-black of the tile hue), two peek shards behind at `translate(-3px,-3px)` and `translate(-6px,-6px)` radius 20 (shard hues = the tile's existing peek domains from the registry), glyph 22px `#fff2f9`, label under: Baloo 2 800 13px in the tile's own hue, `line-height:1.1; max-width:80px; text-align:center;` gap 8.
- **NO selection chrome on the tile. None.** (David 2026-08-02: "no white outline around like in previous screenshot".) The open card below is the only selection signal.
- Tap a tile → the DOSE CARD opens INLINE directly under the row (single-open; tapping another tile swaps content; the close chevron collapses it). Reuse/extract the ground's `tbxBuildDose` machinery so dose state, bands, edits, Start and Adjust all share one code path — the face card is the same card, new placement + the 2c skin below. Zero new innerHTML wipes.
- Card skin (quoted): container `background:#221018; border-radius:26px; border:2px solid #2e1a28; box-shadow:0 8px 24px rgba(0,0,0,.45); padding:17px 17px 15px;` internal column gap 13.
  - Header: face coin 54x54 radius 18, tile hue + `box-shadow:0 4px 0 <deep>`, glyph 26px `#fff2f9`; kicker 10px 800 ls1.8 in the tile hue; name Baloo 2 800 24px `#fff2f9` line-height 1; CLOSE button right: 38x38 radius 14 `background:#170a12`, `ti-chevron-up` 16px `#b596ad`.
  - What/why: what 14px 700 `#f0dbe8`; why 13px 600 `#9a7a92` (the shipped gated strings; do not rewrite).
  - Steps (from tbxTrackForDose at the current dose): coin 34x34 radius 12 step hue, glyph 16px `#fff2f9`; name Baloo 2 800 16px `#fff2f9`; time right Baloo 2 800 13.5px `#b596ad`; row gap 12, list gap 10.
  - Chips row: horizontal, gap 8, `overflow-x:auto`. Default chips [2,5] labeled "2 min"/"5 min"; tapping More SWAPS the row to the full ladder [2,5,10,15,20,30,45] labeled "2m…45m" and hides More (the 2c behavior; replaces the current 4-col minute grid panel ON THE FACE CARD ONLY — the ground card keeps its shipped More panel until David says otherwise; FLAG this split in the report). Chip skin: `padding:10px 17px; border-radius:15px;` Baloo 2 800 15px. Unselected: `border:2px solid color-mix(in srgb, <hue> 40%, #1c0b15); background:#1c0b15; color:#d8a9bb;` Selected: `background:repeating-linear-gradient(115deg, <hue> 0 13px, color-mix(in srgb, <hue> 74%, #fff) 13px 26px); border:2.5px solid #160510; color:#160510; box-shadow:0 3px 0 <deep>;` (the 115deg/13-26/74 token — NOT the wall's 45deg tbxCandy; dose-card chips only). More button: `padding:10px 15px; border-radius:15px; background:#170a12; color:#e8c7d8;` 800 14px with `ti-adjustments-horizontal` 15px.
  - Start: full width, `padding:15px 0; border-radius:19px; background:#ff4fa0; box-shadow:0 5px 0 #a41d60;` Baloo 2 800 19px ink `#2a0d1c` with `ti-player-play-filled` 16px; press = translateY(4px)/shadow 0 1px. (Start still runs tbxLaunch at the chosen dose.)
  - Adjust row: `padding:12px 14px; border-radius:16px; background:#170a12;` icon+text `#b99cf0` 800 15px, PLUS badge `padding:4px 11px; border-radius:999px; background:#8a5cf0; color:#160510;` 800 10.5px ls.8, chevron `#7d5fa8` (this is the shipped row; only re-skin if it differs).
- When the card opens, ease the world scroll so the card is visible (the artifact animates scroll-to-bottom over ~380ms cubic-out; match with the existing --ease-settle idiom).

## 6. Bottom TOOLS hint (the fold invitation)
At the bottom of the home face, centered column gap 2, `padding:4px 0 20px` + the safe-area compensation already in the landing: label "TOOLS" 9px 800 ls2 `#8a6d85` (tr(), RU "ИНСТРУМЕНТЫ") above... CORRECTION per artifact: label ABOVE chevron-down (`ti-chevron-down` 20px `#c77ea3`, hintFloat 2.2s). Action: smooth-scroll to the ground (the toolbox). The ground itself is unchanged.

## 7. designAudit rewrite (the gate must tell the 2c truth)
- REMOVE the four door gates (door size / border / planner fill / garden fill / door-below-strip — all of them).
- ADD gates: (a) hud-sparkle 28px circle present; (b) garden chip `rgba(79,208,138,.4)` border + `#4fd08a` leaf; (c) JOURNEY hint chevron `#c77ea3` top + TOOLS hint bottom; (d) strip pills 13px radius-999 ×5; (e) hero row = exactly 4 tiles at 50px, glyph `#fff2f9`, NO outline/ring on any tile; (f) home face has NO clock element visible; (g) face dose card (open firstLight): container `#221018`/radius 26 + what/why non-empty + selected chip 115deg stripe backgroundImage contains "115deg". Keep every untouched board gate (circle, strip position, bloom, plan button, ground gates) as is. ALL gates must PASS in preview; report the new total.

## 7b. DAVID AMENDMENTS (2026-08-02 chat, sent to the builder mid-flight; override the above where they conflict)
1. The hero row sits LOW near the TOOLS hint (flex spacer ABOVE the row, not below): Plan-my-day → spacer → hero row → dose card → TOOLS hint.
2. TOOLS hint at the true screen bottom (~6-10px bottom padding, no extra safe-area for the hint; David accepts the iOS home-indicator overlap, the line fades).
3. THE HOME MAGNET: a scroll that settles within ~56px of the home target eases onto it exactly (~200ms, rAF ease-out; only after scroll settle + no touch + _worldPositioned; aborts on any new input; never fights momentum; no re-trigger loops). Feel = DEVICE-UNTESTED.

## 8. Verify + report
node --check app.js. Preview (mobile 375x812, demo profile, tuner cleared): boot → home shows HUD/strip/board/hero row/TOOLS hint with NO doors and NO clock; tap Morning Stack → card opens inline with steps at 5 min; chip More → full ladder swaps in; close chevron collapses; DEV.designAudit() ALL PASS (new total). Screenshot home idle + open card. REPORT: edit sites by anchor, the audit total, the flagged calls (sparkle mapping, ground-card More split, empty-week icon choice), node result. RU dict same-commit for JOURNEY/TOOLS labels. DEVICE-UNTESTED note stays for feel/scroll.
