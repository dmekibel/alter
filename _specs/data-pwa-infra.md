# SPEC — Data, PWA, infra, reminders  (slug: `data-pwa-infra`)

Build-ready specs for the **Data integrations**, **Tech/PWA/infra**, and **Coaching/reminders/notifications** cluster from `GRAND-AUDIT-2026-06-26.md`.

## Locked rules carried into every item below
- Deep berry/wine palette + the per-domain `DOM` colors. **No neon / glow / shine / white surfaces.** The pink now-line stays the single brightest thing. (`HANDOFF-visual-redesign-2026-06-27.md`, `CLAUDE.md`.)
- One activity at a time; reward-never-shame copy in every prompt/notification.
- Full-screen, low-clutter.
- **Edit only `index.html`, `app.js`, `manifest.json`.** `server.js` is GENERATED by `_dev/preship.sh` — never hand-edit. A service worker would be a NEW top-level file (`sw.js`) — see item TP-2 for the build-pipeline implication (it must be served by Cloudflare too, so `build-hf-server.js` has to learn about it).
- Reuse helpers: `add()`, `el()`, `DOM`/`DOM[dom].c`, `mixHex`, `esc`, `fmt`, `dur`, `hm`, `blocks()`, `logs()`, `save()`, `load()`, `toast()`, `proactive()`, `phase()`, `key()/todayK()`.
- **Timeline render is FRAGILE** (rebuilt 3×). Nothing in this cluster needs to touch `calendarView`/`buildPull`/`renderToday` — if any item starts to, flag HIGH RISK and stop.

## What already exists (verified, do not rebuild)
- **Backup export/import/merge is DONE and good.** `exportData(mode)` app.js:1292 (download + clipboard, versioned `{app,schema,exportedAt,data}` envelope), `parseBackup(txt)` app.js:1298 (accepts the envelope OR a raw old backup, validates), `mergeImport(d)` app.js:1300 (union days by id, dedupe habits by name + remap done-history, game takes the best, carries unknown future fields), `restoreUI(B)` app.js:1312 (paste / file-pick / Merge / Replace). Wired into the brain/settings sheet at app.js:3125-3126 ("📋 Copy", "⬇ Download"). **Storage-eviction mitigation is therefore already satisfied at the data layer** — the only gap is a *reminder to back up* and (optionally) auto-export-on-a-cadence. See item CR-1.
- `save()` app.js:1280 already toasts "⚠️ Couldn't save — storage may be full. Back up your data via 🧠." on a `localStorage.setItem` throw — quota handling exists.
- `proactive()` app.js:1381 is the in-app nudge engine (phase-aware kicker/line/sub/primary/chips), rendered by `renderHero()` app.js:1910. This is the natural home for in-app reminders (CR-2) — no new engine needed.
- Manifest exists (`manifest.json`) and is linked (index.html:12) with theme/apple-mobile meta tags (index.html:7-12). **No service worker. No Notification API anywhere. No `.gitignore`.**
- `SCHEMA = 1` at app.js:75; migration happens in `load()` app.js:1279 (defensive field-defaulting, not a versioned switch). `fresh()` app.js:1278 is the default-state factory.

---

# DATA INTEGRATIONS  (cluster "Data integrations", audit line ~159)

> **Verdict up front:** All three data-integration asks are **dropped / vague** and David has *deferred native* (no Capacitor/HealthKit). HealthKit, Plaid, and OS calendar push all require native shells or paid third-party APIs that are out of scope for a single-file localStorage PWA. The build-ready version of each is a **manual, first-principles, in-app stand-in** that matches the existing app philosophy (the app already ships "manual first-principles versions of AI planning" — DECOMP_TEMPLATES app.js:404). Spec the manual versions; explicitly mark the real integrations as OUT OF SCOPE so a build-agent doesn't burn a day on OAuth.

## DI-1 — Health data (Oura / bloodwork / scale / steps)
1. **Ask (audit):** "encourage any data like aura ring and blood work and scale and steps and everything" (🟦 BIG, dropped).
2. **Buildable?** **vague → nearest-concrete:** a **manual health-metrics log** — small numeric trackers (weight, steps, sleep-hours, resting HR) David types or taps, stored per-day, surfaced as sparkline trend in stats. NOT a sensor/Oura/HealthKit integration (native, out of scope; Apple Health export is a one-shot XML import a build-agent should NOT attempt under a token budget).
3. **APPROACH:** Add a `S.metrics` map `{ dayK: {weight, steps, sleepH, rhr} }`. A "📊 log a metric" entry in the brain/settings sheet (near restoreUI app.js:3125) opens a tiny `pickSheet`-style sheet (reuse `pickSheet`, see scheduleSubtask app.js:418 for the pattern) with 4 numeric steppers. Render a 7-day mini-trend in `renderStats()` (app.js, called from renderAll app.js:2694). Reward, never shame: trend copy is "↗ up 3 days" never "you're behind."
4. **CODE POINTERS:** new `metricsSheet()` near settings UI ~app.js:3120; storage default in `load()` app.js:1279 (`S.metrics = S.metrics || {}`); render hook inside `renderStats()`.
5. **UI (locked palette):** a card on the wine surface, domain-colored numeric chips (use `DOM.energy.c` for body metrics). Sparkline drawn in the pink now-line family but DIMMER than the now-line. No white fill — bars are `mixHex(DOM.energy.c, '#1a1726', .4)`.
6. **DATA:** `S.metrics = {}` ; per-day `{weight:Number, steps:Number, sleepH:Number, rhr:Number}`. load() default-init only — no SCHEMA bump needed (additive, old data safe; matches the `S.acts.children` precedent at app.js:1279).
7. **REGION:** settings/brain sheet (~app.js:3100-3130) + `renderStats()`. No timeline contact.
8. **EFFORT:** **M**.
9. **RISKS:** Low. Keep it OFF the home hero so it doesn't add clutter (locked rule). Do NOT attempt Apple Health XML parse (multi-MB, slow, native-ish). Mark real-sensor integration OUT OF SCOPE in the handoff.

## DI-2 — Bank / money data + spend limits
1. **Ask:** "data like ur bank account amount and stuff like that to help u manage money" (🟦 BIG, dropped, asked ×2).
2. **Buildable?** **vague → nearest-concrete:** a **manual monthly money tracker** — set a monthly spend cap, log spends, show remaining. NOT Plaid/bank-link (OAuth + paid + native-ish; OUT OF SCOPE).
3. **APPROACH:** `S.money = {cap:Number, month:"YYYY-MM", spent:[{d,amt,note}]}`. A "💰 money" sheet (same `pickSheet`/sheet pattern) to set the cap + log a spend. Surface "X left this month" as a `proactive()` chip when a cap is set (mirror the workout chip at app.js:1388). Reset `spent` when the month rolls (check in `load()`).
4. **CODE POINTERS:** new `moneySheet()` ~app.js:3120; chip injection in `proactive()` app.js:1388 (copy the `S.profile.exWant` chip pattern exactly); month-roll in `load()` app.js:1279.
5. **UI:** wine card, a single big "remaining" number in `DOM.work` or a money-domain color, a slim progress bar (NOT a glowing gauge). Over-cap copy is gentle: "over by $X — want to replan the month?" never red-alarm.
6. **DATA:** `S.money = {cap:0, month:..., spent:[]}`. Additive default in load(). No SCHEMA bump.
7. **REGION:** settings sheet + `proactive()` app.js:1381-1389. No timeline contact.
8. **EFFORT:** **M**.
9. **RISKS:** Low. Bank-link explicitly OUT OF SCOPE (note in handoff). Don't add a 4th nav tab for it — live in settings + a proactive chip.

## DI-3 — Doctor / appointment reminders
1. **Ask:** "to remind u to go to doctors the app will do that as well" (▫️ med, dropped).
2. **Buildable?** **yes (as the reminders feature, CR-2):** an appointment is just a high-priority dated block with a reminder. This is a thin wrapper over the reminders engine, not a separate integration.
3. **APPROACH:** Let any block carry `o.remindBefore` (minutes). An appointment is a normal `blocks(k)` entry (the editor already creates blocks — editorSheet ~app.js:2592). Surface upcoming appointments in `proactive()` and (if granted) fire a web notification N minutes before via CR-2's reminder loop. "Appointments" already exists as a caregiver seed activity (app.js:1002) so the category is there.
4. **CODE POINTERS:** add a "remind me" toggle to `editorSheet` ~app.js:2592-2662; the actual firing lives in CR-2 (`reminderTick`).
5. **UI:** a tiny bell toggle row inside the existing editor sheet, domain-colored, no new surface.
6. **DATA:** `o.remindBefore` on a block object (default undefined). No migration.
7. **REGION:** editorSheet + CR-2 reminder loop. No timeline-render change (adding a field to a block object is safe; do NOT change how blocks are drawn).
8. **EFFORT:** **S** (rides on CR-2).
9. **RISKS:** Low, but the firing depends on the page being open OR a service worker (CR-2 details the limits). Be honest in copy/handoff that web reminders only fire while the PWA is foreground/recently-backgrounded on iOS.

---

# TECH / PWA / INFRA  (cluster "Tech/PWA/infra", audit lines ~202, ~754)

## TP-1 — `.gitignore` for build junk  (the committed half of "DESIGN-BRIEF.md spec + .gitignore")
1. **Ask:** "Commit it so the spec is version-controlled... Add a `.gitignore` to stop committing build/working junk" (small, PARTIAL — DESIGN-BRIEF.md is committed, .gitignore was never made).
2. **Buildable?** **yes** — pure infra, no app code.
3. **APPROACH:** Create `/Users/Dmekibel/claudeCode/alter/.gitignore`. **CRITICAL:** this repo currently *tracks* a lot of junk (verified: 433 tracked `.png`, 3 `.mp4`, 3 `.gif`, `.DS_Store`, `server.js`). A fresh `.gitignore` will NOT untrack already-committed files — it only stops NEW ones. So the task has two parts:
   - **(a) Write `.gitignore`** with the rules below.
   - **(b) Untrack the already-committed junk** with `git rm --cached` (keeps files on disk).
   **Runtime-safety check (verified):** the app references ONLY `assets/*.png` at runtime (loadFairy app.js:1553 `assets/spr-*.png`, loadWorld app.js:1557 `cup-*/obj-*` which live in `assets/`, index.html:1331 `app.js?v=`). The frame dirs `360frames/ flyframes/ gridframes/ idleframes/` and root `*.png`/`*.mp4`/`*.gif` are sprite-SOURCE junk (sliced into `assets/`), NOT loaded at runtime. They are safe to untrack. **DO NOT untrack `assets/`** — that's live runtime art. **DO NOT untrack `server.js`** without confirming the Cloudflare/Pages deploy doesn't pull it from git (it's generated by preship and committed today; if Pages serves it from the repo, untracking breaks deploy — see RISKS; safer to leave `server.js` tracked OR confirm the deploy first).
4. **Proposed `.gitignore` contents:**
   ```
   # OS
   .DS_Store
   # sprite-source / build junk (sliced output lives in assets/, which IS tracked)
   360frames/
   flyframes/
   gridframes/
   idleframes/
   /*.png
   /*.mp4
   /*.gif
   /*.jpg
   /*.jpeg
   /*.zip
   _night-backup/
   # editor / scratch
   *.log
   ```
   Note the leading `/` on `*.png` etc. — root-only, so `assets/*.png` stays tracked. Keep `_specs/`, `_mockups/`, `meditation-scripts/`, `_dev/`, `*.md` TRACKED (they're real source/docs).
5. **UI:** none.
6. **DATA:** none.
7. **REGION:** repo root only. Zero app-code contact.
8. **EFFORT:** **S**.
9. **RISKS:**
   - **`server.js`:** verify before untracking. If the Cloudflare deploy reads `server.js` from the committed repo, it MUST stay tracked. `preship.sh` regenerates it locally each ship, but the deployed artifact may be the committed one. **Safest call: leave `server.js` tracked, add a comment, ask David.** Don't break the deploy to satisfy a tidiness ask.
   - Untracking 430+ files is a big, noisy commit. Do it as its OWN commit ("untrack sprite-source junk") separate from any feature, so a revert is clean.
   - Confirm `assets/` and `app.js`/`index.html`/`manifest.json` are NEVER matched by the patterns before committing (`git status` after `git rm --cached` should still show those as tracked/clean).

## TP-2 — Service worker (offline shell + web-push host) + manifest icons
1. **Ask (composite):** PWA infra to support web-side notifications/reminders (David deferred native push/Capacitor → web only). The manifest exists but has `"icons": []` (manifest.json:11) and there is NO service worker — both are blockers for installability polish and for any background/push notification.
2. **Buildable?** **partial → concrete:** (a) a minimal cache-first service worker is fully buildable and gives offline-launch; (b) **true web push on iOS requires the PWA be installed to home screen AND iOS 16.4+ AND user-granted permission, and even then push from a server needs a push backend** — David has no server (Cloudflare static + generated server.js for asset serving only). So **spec the SW for offline + LOCAL notifications via `registration.showNotification()` triggered while the app/SW is alive; mark server-pushed remote notifications OUT OF SCOPE** (needs VAPID keys + a push endpoint David doesn't run).
3. **APPROACH:**
   - **New file `sw.js`** (top-level). Cache-first for the app shell: `index.html`, `app.js?v=NNN`, `manifest.json`, the `assets/*.png` runtime art, the Tabler-icons + Google-fonts CSS if used. Bump a `CACHE` version string on each ship (tie it to the `app.js?v=` bump — preship could bump both; see build note).
   - **Register it** from `index.html` (a tiny inline script before `</body>` or at the end of the existing `<script src="app.js?v=572">` region, index.html:1331): `if('serviceWorker' in navigator){navigator.serviceWorker.register('sw.js')}`. Guard so it no-ops in the headless preview if needed.
   - **Build pipeline:** `build-hf-server.js` currently bundles `index.html`+`app.js`+`manifest.json` into `server.js`. It must ALSO serve `sw.js` at `/sw.js` with `Content-Type: application/javascript` and **scope `/`** (a SW can only control pages at/below its own path — it must be served from root, not a subdir). Update `build-hf-server.js` + re-verify `preship.sh` output. **This is the one place a server-side change is unavoidable** — but it's a change to the GENERATOR (`build-hf-server.js`), never to the generated `server.js`.
   - **Manifest icons:** generate (or point to existing `assets/`) at least 192px + 512px maskable PNGs and fill `manifest.json` `icons`. If no icon art exists, this sub-task is blocked on art — flag it, don't ship a white placeholder (locked: no white surfaces).
4. **CODE POINTERS:** new `sw.js`; registration snippet index.html near :1331; `build-hf-server.js` (whole file is 1264 bytes — read it before editing); `manifest.json` icons.
5. **UI:** none in-app, except an optional one-line "works offline ✓" in settings.
6. **DATA:** none.
7. **REGION:** new file + index.html tail + build-hf-server.js + manifest.json. **Does NOT touch app.js logic or the timeline.**
8. **EFFORT:** **M** (SW + registration: S; getting the build pipeline + iOS install caveats right: the rest).
9. **RISKS:**
   - **Cache staleness is the #1 PWA footgun and David already "hits Pages deploy-lag constantly" (CLAUDE.md).** A bad SW can pin an OLD build forever and the `fresh.html` cache-bust won't save him. MITIGATE: cache version MUST bump every ship (wire into preship), use `skipWaiting()` + `clients.claim()`, and network-first (not cache-first) for `index.html`/`app.js` so a new ship is picked up — cache-first only for static `assets/*.png`. Test the update path explicitly. **This is HIGH RISK for David's workflow specifically** — call it out and confirm on device.
   - iOS notification permission only promptable from a user gesture, only after install. Build the prompt behind a button (CR-2), never auto-fire on load.
   - The preview can't prove SW behavior or notifications — DEVICE-UNTESTED by definition. Say so plainly.

## TP-3 — Commit DESIGN-BRIEF.md (already done — verify only)
1. **Ask:** "Commit it so the spec is version-controlled and can't be lost."
2. **Buildable?** **already built** — `git ls-files` shows `DESIGN-BRIEF.md` tracked (audit-confirmed app.js-region line 759). No action beyond confirming. Bundle the verify into the TP-1 commit.
3-9. N/A (verify-only). EFFORT **S**.

---

# COACHING / REMINDERS / NOTIFICATIONS  (cluster "Coaching/reminders", audit lines ~176, ~742)
> Scope per task brief: **web-side notifications/reminders ONLY** (native push/Capacitor deferred). The two heavy coaching asks in this cluster (fieldguide-KB brain, composable self-help stack, over-drift fork, smarter brain) are LARGE and belong to other clusters' specs — covered briefly as CR-3 pointers so nothing is silently dropped.

## CR-1 — Backup reminder + optional auto-export cadence  (storage-eviction mitigation)
1. **Ask (derived from brief):** "the export/import/merge backup that mitigates storage eviction." Data layer is DONE (exportData/parseBackup/mergeImport, app.js:1292-1318). The GAP is David never being *reminded* to back up before iOS evicts localStorage.
2. **Buildable?** **yes.**
3. **APPROACH:** Track `S.lastBackup` (set in `exportData()` on a successful download/copy). In `proactive()` (app.js:1381) or on load, if `daysSince(S.lastBackup) >= 7`, add a gentle chip "💾 back up your progress" → opens the existing restore/export UI (restoreUI app.js:1312 / the Copy+Download buttons app.js:3125). Optionally: a settings toggle "remind me weekly to back up." Reuse `daysSince` (used by `messy()` app.js:1325).
4. **CODE POINTERS:** set `S.lastBackup = todayK()` inside `exportData()` app.js:1292; chip in `proactive()` app.js:1388 (copy the workout-chip pattern); load() default `S.lastBackup = S.lastBackup || null`.
5. **UI:** one wine chip in the hero proactive row, domain-neutral color, gentle copy. No new surface.
6. **DATA:** `S.lastBackup` (string dayK or null). Additive default in load(). No SCHEMA bump.
7. **REGION:** `exportData()` + `proactive()` + `load()`. No timeline contact.
8. **EFFORT:** **S**.
9. **RISKS:** Very low. Don't nag more than weekly. Reward-framed copy only.

## CR-2 — In-app + web reminders (the notifications feature)
1. **Ask:** doctor/appointment reminders (DI-3) + general "remind you to go to the doctor / do X" — generalized to **time-based reminders for blocks and habits**, delivered in-app always and via web Notification when granted.
2. **Buildable?** **partial → concrete:** in-app reminders (toast/hero + a sound) are fully buildable and reliable. Web `Notification` works only while the page is foreground OR (with the TP-2 SW) recently backgrounded on iOS-installed PWA; true scheduled background push is OUT OF SCOPE (no push server). Build the reliable layer first; layer web-notify on top, gated + honest.
3. **APPROACH:**
   - **Permission:** a settings toggle "🔔 reminders" → on first enable, call `Notification.requestPermission()` (must be from the tap). Store `S.notify = {on:Boolean, granted:Boolean}`.
   - **Reminder data:** blocks already have `time` + `mins`; add optional `o.remindBefore` (DI-3). Habits: optional `h.remindAt` ("HH:MM").
   - **Tick loop:** a single `setInterval(reminderTick, 60000)` started once (near the existing voice-poll interval app.js:25 or in init). `reminderTick()` scans today's blocks/habits, finds any whose reminder time is within the last minute and not yet fired (track `firedKey` in a per-day `S.fired` set), then: always `toast()` + optional gentle tone (reuse the meditation/breath WebAudio tone pattern app.js:2123); and if `S.notify.granted`, `navigator.serviceWorker.ready.then(r=>r.showNotification(...))` (or `new Notification(...)` fallback).
   - **No double engines:** there is exactly ONE proactive surface (`proactive()`); the reminder tick is a NEW lightweight timer that only fires notifications — it must NOT re-render the timeline or call renderAll on a schedule (perf + the full-DOM-rebuild landmine, CLAUDE.md). It only `toast()`s + notifies + sets `S.fired`.
4. **CODE POINTERS:** permission toggle in brain/settings sheet ~app.js:3100; `reminderTick()` new fn near `proactive()` app.js:1381; interval start in the IIFE init (after load(), near app.js:25 voice poll or wherever the app boots — grep `renderAll()` first call); `o.remindBefore` toggle in editorSheet app.js:2592; tone reuse from meditation finish app.js:2123.
5. **UI:** a bell toggle in settings (wine surface), and per-block a small bell in the editor. The notification body copy is reward/neutral: "🕊️ time for: Deep Work" — never "you're late." In-app fallback is a `toast()` (already styled, app.js:1290) so it stays on-palette automatically.
6. **DATA:** `S.notify = {on:false, granted:false}`; `S.fired = {dayK: {reminderId:1}}` (cleared on day-roll in load(), like `S.timers` filter app.js:1279); `o.remindBefore` on blocks; `h.remindAt` on habits. Additive defaults in load(). No SCHEMA bump (all old data safe; matches existing additive-default convention).
7. **REGION:** settings sheet + new `reminderTick` + init interval + editorSheet field. **No timeline-render change** — reminder tick must never touch calendarView/buildPull.
8. **EFFORT:** **M** (in-app layer S; web Notification + SW handoff + iOS caveats push it to M).
9. **RISKS:**
   - **iOS reality:** background notifications fire unreliably for web PWAs; a timer-based reminder only fires while the app is open. **DEVICE-UNTESTED in preview by definition** — say so. Don't claim "you'll get notified when the app is closed" — you can't guarantee it without a push server. Frame as "reminds you while ALTER is open; installed-to-homescreen improves it."
   - Permission must come from a user gesture or the prompt silently fails — gate behind the settings toggle, never auto-request on load.
   - The minute-tick must be cheap (scan only today's arrays, set `S.fired`, no renderAll). Adding a render call here would re-introduce the full-DOM-rebuild flicker landmine.
   - One timer only — don't spawn a second on every render (guard with a module-level `reminderTimerStarted` flag).

## CR-3 — Heavier coaching asks (pointers only — out of this cluster's build scope)
Not specced in full here (they belong to the brain/self-help/tracker clusters), but logged so they're not dropped:
- **Fieldguide / Brian Johnson KB into the brain** (BIG, ×2): brainContext() app.js:2799 sends a thin generic prompt; the real lift is injecting KB. → brain cluster.
- **Smarter AI brain** (BIG): David himself deferred ("get back to it later", audit ~749). → brain cluster.
- **Composable self-help stack with per-section durations** (BIG): planner-bubble stack exists for activities (presetsSheet/stackDetail app.js:498/2556) but not for chaining breath→meditate→mantra modules. → self-help cluster.
- **Over-drift fork** (med, ×2): partial nudge exists (ld-nudge app.js:989-1000 offers "back to plan"); the full "keep drifting + replan duration + reflow the day around the drift" fork is unbuilt. → tracker cluster. **HIGH RISK if attempted** (touches live-tracker + reflow + timeline).

---

## Conflict-planning summary (for parallel build-agents)
| Item | REGION touched | Collides with |
|---|---|---|
| DI-1 metrics | settings sheet ~3100, renderStats | CR-1/CR-2/DI-2 (same settings sheet) |
| DI-2 money | settings sheet, proactive() 1388 | CR-1 (same proactive chip area) |
| DI-3 appt | editorSheet 2592, rides CR-2 | CR-2 |
| TP-1 gitignore | repo root only | none (infra) |
| TP-2 service worker | sw.js (new), index.html tail, build-hf-server.js, manifest | none in app.js logic |
| CR-1 backup nudge | exportData 1292, proactive 1388, load 1279 | DI-2 (proactive chips) |
| CR-2 reminders | settings sheet, new reminderTick, init, editorSheet | DI-1/DI-2 settings; DI-3 |

**Serialize the settings-sheet edits** (DI-1, DI-2, CR-1, CR-2 all add rows near app.js:3100-3130) and **serialize the `proactive()` chip edits** (DI-2 + CR-1 both append chips at app.js:1388) — these are the only real merge-conflict hotspots. TP-1 and TP-2 are independent and can run in parallel with everything.

**None of this cluster needs to touch the fragile timeline render. If any item drifts toward calendarView/buildPull/renderToday, STOP and flag HIGH RISK.**
