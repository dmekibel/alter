# THE 1:1 REBUILD — 2026-07-03 (David: "I basically want the app to be one-to-one to what you visualized. Accept the feedback I gave.")

**THE CANON = `_specs/_epic-mockups/*.html`** — the 14 approved epic mockups (render any of them in a browser to compare; they are self-contained HTML). David approved ALL 14 with the per-visual corrections below. The written spec behind every element: `_specs/DESIGN-EPIC-2026-07-03.md` (522 law-checked suggestions). The laws: `GRAND-REDESIGN-2026-07-02.md` §9–§12.

**MODEL ROUTING (locked):** execution runs on **Opus** (routine build vs locked pixel spec + screenshot compare). Fable returns only if a timeline-physics slice fights back. One RUN per session; David device-checks between runs (never compress runs into one session).

**THE COMPARE DISCIPLINE (the thing that failed twice — now mandatory):** for every screen: ① open the canon mockup html in the preview at 390px → screenshot = reference; ② build; ③ screenshot the REAL app at 390px; ④ compare side-by-side and iterate until visually equivalent BEFORE David sees it. Preview proves boots/taps/layout only — gesture feel stays DEVICE-UNTESTED and is labeled as such.

---

## DAVID'S VERDICTS (2026-07-03, verbatim intent — fold each fix into its run)

| # | Mockup / phone | Verdict + required fixes |
|---|---|---|
| 1 | cockpit_idle_epic · with-plan | **AMAZING** — build as is |
| 2 | cockpit_idle_epic · empty day | **AMAZING** — build as is |
| 3 | cockpit_live_epic · on-plan | **Good** — fix: the green progress ring must be the SAME thickness all the way around (mock reads thicker top / thinner bottom). Even-width annulus, no perceptual wobble. |
| 4 | cockpit_live_epic · off-plan | **Great** — build as is |
| 5 | timeline_epic · thermograph day | **Great** — fix: the ghost block (Обед) has an invisible-bubble/overlap feel — its icon sits too low and TOUCHES the dashed border. Give ghost-block content proper padding/line-height so nothing kisses the border. |
| 6 | timeline_epic · edge inspector | **Great** — OPEN QUESTION David raised: the mock is single-lane; how do we show planned-X-but-did-Y? **Answer locked into this plan: the PLAN/REAL two-lane physics of the real timeline REMAINS — the mock simplified for readability. The thermograph sky, seam and inspector are layered ONTO the existing two-lane system, never replacing it (regression contract #2/#3).** The inspector edits the PLAN lane; reality keeps printing beside it. |
| 7 | plan_day_epic · beat 2/4 | **Great** — build as is |
| 8 | plan_day_epic · finish cascade | **Great** — fix: the lived morning block (Зарядка, brown + gold inset) reads MUDDY. Refine the lived-block tint family (jewel law: clean hues deepened, never muddy). |
| 9 | calendar_epic · week | **Pretty great BUT intimidating**: each day is multiple separate blobs with no containing outline → days don't read as columns. ITERATE ON DEVICE: option A = David's original one-blob-per-day; option B = this version with a containing day outline/track behind the blobs. Ship both behind a dev toggle; his eye decides (process law). |
| 10 | calendar_epic · month | **Good** — fix: cells must show the DATE NUMBER, and the grid needs weekday letters (П В С Ч П С В header). Keep everything else. |
| 11 | journey_epic · trail | **Like** — fix: stones read slightly disconnected — a bit too small, too far apart, too far from the center line. Tighten: bigger stones, tighter vertical rhythm, smaller lateral offsets. |
| 12 | journey_epic · the Return | **Like the Return** — fix: the prism card's edge reads as a WHITE outline, not a rainbow. Rework the prism finish so it reads Pokémon-holo, not white border (iterate 1-2 variants in preview before device). |
| 13 | goals_epic (both) | **Pretty great** — build as is |
| 14 | habits_epic | **Off**: ① ink-dark icons on bright striped stones read like "black emojis" — fix icon legibility (lighter ink? white icons on stripes like the journey hero? try both in preview); ② stones too far apart — too much scrolling; densify the rhythm. |
| 15 | game_epic · planting | **Like** — but the game-attention economy (how garden connects to the game pane) is UNDESIGNED. **TABLED per David — build the ceremony visuals only, no new economy.** |
| 16 | game_epic · binder | **Really like** — build as is |
| 17 | intro_epic | **Like the live-block door** — build (incl. load-preview + armed-erase cards) |
| 18 | onboarding_epic | **Like it more (than current)** — build both phones (payoff beat + trainer-card mint) |
| 19 | toolbox_epic | **Like** — build both phones |
| 20 | players_epic | **"A lot a lot more"** — build both phones (meditation player + PM mirror) |
| 21 | chrome_epic | **Like** — build all three (You-tab + compass nav + bento ignition) |

---

## THE RUNS (one per Opus session; commit per slice; preship every ship; David device-checks between runs)

### RUN 1 — THE COCKPIT + TIMELINE CORE (regression zone — re-run the 4-point contract before every ship)
1. Cockpit idle 1:1 (mock 1): mini-timeline-block sub-line w/ countdown + pink hairline; striped door names its block; ti-calendar-check on the planned chip; the foot line becomes a REAL 44pt ghost escape row (wire "без времени — просто трек" to start an uncommitted track).
2. Cockpit empty day 1:1 (mock 2): time-of-day invitation line; Plan-day door sublabel «3 дела — 60 секунд»; ВЧЕРА СЕЙЧАС echo sheet reusing #tfNextSheet (yesterday ±1h / same weekday; ti-history chip; chip = plan+track via playFirst plumbing).
3. Cockpit on-plan 1:1 (mock 3 + fix): five-element body for tracking states (chrome leaves), conic ring EVEN thickness, converter dot + end pip, activity pill = the switch, minute-earn pop on the timer, green «Готово — засчитать» + ghost Пауза/Перенести row.
4. Cockpit off-plan 1:1 (mock 4): dashed ghost ring pitch, matte activity disc (kill filters), «план удвоит очки» line, blue «Составить план» fusion door, docked «сколько ещё?» sheet, chip-tap ignition receipt (ring flashes blue → settles green).
5. Timeline day 1:1 (mock 5 + fix): thermograph sky (lived-hours rose heat bands), converter seam shimmer on the newest lived block edge, now-line end-dots, week-strip battery bars, midnight seam + crown, ghost-block padding fix (nothing touches the dashed border), wayback icon on ghosts. **Two-lane plan/real physics untouched (verdict #6).**
6. Edge inspector (mock 6): right-edge panel rising FROM the tapped block (spring, day stays live), НАЧАЛО stepper, ДЛИНА chips resize the block live behind it, bare domain dots re-type, Ещё ghost → full editor, hold-to-fill удалить, pink halo on the edited block. Edits the PLAN lane only.

### RUN 2 — PLAN-DAY + CALENDAR
1. Plan-day beats 1:1 (mock 7): beat pips header, identity hero line, v3 ignition chips, ember-breathing «ДАВНО СОБИРАЛСЯ» courage row, THE LIVE MINI-TIMELINE STRIP (picks land matte into projected slots beside the now-tick), blue Дальше + ghost skip.
2. Finish cascade 1:1 (mock 8 + fix): staggered spring landing, now-line double pulse, first-block blue-hairline handshake + live next-up line, guardian seal line; REFINE the lived-block tint (no muddy brown).
3. Week view (mock 9 → A/B): build BOTH: (A) one-blob-per-day columns (David's original), (B) multi-blob + containing day-track outline. Dev toggle; device verdict; loser dies same week. Keep: now-line across today, single fire-run underline, crowned column shimmer, mirror line, live zoom label.
4. Month view 1:1 (mock 10 + fix): jewel-step pink ramp cells WITH date numbers + weekday header row; gold hairline crowns; matte charge future; wayback glint; today breathing; story line; cause-anchored zoom rings; legend row.

### RUN 3 — JOURNEY + HABITS + GOALS
1. Journey trail 1:1 (mock 11 + fix): matte-future stones igniting on completion, node-pip day gauge, 90%-dark season banner + milestone pips + chapter-card preview, minutes on done stones, blue СТАРТ door + «не сейчас», guardian context line; TIGHTEN geometry (bigger stones, tighter rhythm, smaller offsets).
2. The Return 1:1 (mock 12 + fix): gap = dashed pause stone + «серии сохранены», the quiet-sea line, prism mint firing FROM the stone (rings + flying +N + gold dust); REWORK the prism edge (holo, not white — 2 preview variants → device).
3. Habits path 1:1 (mock 14 + fixes): inverted stones (done = full stripes + green check, undone = matte tint + solid colored border), ember→flame→blue-flame pips, ×21 blue ceremony, per-habit mirror lines, weekly pips; FIX icon legibility (try white vs light-ink on stripes) and DENSIFY spacing.
4. Goals 1:1 (mock 13): full-bleed Квесты room, pip strip + next-session fact on the hero card, tiered bounties, stale-card rotation whisper, mint-promise footer, released shelf; detail = the CARD-BACK FLIP (gold WOOP inscriptions, one-sentence if-then plan), «Отпустить с честью» rite.

### RUN 4 — FLOWS + CHROME
1. Intro 1:1 (mock 17): live-block green GO door «Вернуться · [block]», «день N вместе» + ember tag, first-press stripe-wipe ignition, 44px globe chip, load-save life-preview card, armed-erase card w/ visible 4s drain.
2. Onboarding 1:1 (mock 18): the payoff beat (flyPoints arc +18 → popping counter, task = first foil quest card, green Сделал door, dashes ignite as walked) + the trainer-card mint beat (matte card №001 w/ EST + flavor, pact on the card back, evolution pips, card flies to «коллекция · 1»).
3. Toolbox 1:1 (mock 19): library (SOS crimson door, v3 layer tabs, breathing cards w/ one when-line + why-fold, Stutz ladder w/ gold Благодать hairline, 44pt pins) + build-a-session (Полный стек hero + battery strip + time chips + green Играть, time-first packs each w/ mini battery strip, lived pack shines, ghost Собери сам).
4. Players 1:1 (mock 20): meditation player (element-tinted orb, catch ripple + «подмечено» pips, beat-pip session map, battery scrub w/ cue ticks + H12 20s silence tail, chrome dissolves) + PM mirror (the day itself as mini-blocks incl. drift/missed/now-line, «4 из 6 прожито» line, five jewel mood mini-rows w/ ignition pick, narrated door).
5. Chrome 1:1 (mock 21): You-tab (trainer card w/ earned-gold facts + sheen, four rooms, away-mode held-state row, vault pips, ghost Продвинутое) + THE COMPASS NAV (3-dot pill that blooms on touch, auto-folds 2.5s — replaces the 3-tab bar; Mom-safe labels on bloom) + bento (search collapses to bare icon, peek-cut rows, THE PICK IGNITION: 220ms own-color ignition while the room fades, overlay collapses toward its cause).

## PROGRESS LOG
### RUN 1 — in flight (paced as two ships: cockpit, then timeline)
- **SHIP 1 (v824, 2026-07-03) — Cockpit IDLE 1:1 (slices 1 & 2). DONE, screenshot-verified vs mocks 1 & 2 at 375px.**
  - Idle+plan: sub-line is now the mini-timeline block (`.tf-subblock`, pink inset hairline + domain icon + localized countdown `через …`); striped "Plan + track next" door carries the block-name tag (`x.tag` on the door renderer); the planned duration chip wears `ti-calendar-check`; the docked sheet's foot is a real 44pt ghost escape button `.tns-escape` ("без времени — просто трек", wired to `tfTrackNoTime` = uncommitted track) + caption "с планом очков больше".
  - Idle+empty: warm time-of-day invite (`.tf-invite`, `tfIdleInvite()`); Plan-my-day door sells its cost via `x.sub` ("3 дела — 60 секунд"); **ВЧЕРА СЕЙЧАС echo sheet** (`tfYesterdayEcho()` + `tfEchoStart()`) reusing #tfNextSheet — yesterday's activity near this hour, `ti-history` chip, one-tap plan+track.
  - Plumbing added: `renderTFControls` door mode now supports `x.tag` / `x.sub` / `x.half` (ghost 2-up row); `durLoc()` localizes countdown units (killed a latin leak — `__latinAudit` clean on the cockpit). Dev harness got `DEV.cockpit()` (opens the plain cockpit, no guided stage).
  - RU strings added for every new key (B4 honored).
- **SHIP 2 (v826, 2026-07-03) — Cockpit LIVE 1:1 (slices 3 & 4). DONE, screenshot-verified vs mocks 3 & 4.**
  - Five-element winning body: 12 chrome elements hidden in `st-onplan`+`st-off` (clock/spark/soul-force/say/verdict/elabel/switch-chips/close/gear) — only ring / pill / time / sub-line / doors remain.
  - On-plan (mock 3): EVEN green annulus via a symmetric ink ring on the disc (`box-shadow:0 0 0 3px #160510` — killed the asymmetric bottom drop-shadow that read thick-top/thin-bottom); converter white dot rides the arc-end (positioned in `setRing`'s paint), dark end-pip at 12 o'clock; `tfMinPop` on the timer; sub-line "по плану · осталось N мин · конец HH:MM" (24h — no am/pm latin); green "Готово — засчитать" + ghost 2-up [Пауза | Перенести].
  - Off-plan (mock 4): dashed empty ring (`st-off .tf-ring` border), MATTE disc (dark-tint bg + colored border + colored icon, filters killed); sub-line "N мин затрекано · план удвоит очки"; striped "Создать план" + ghost [Пауза | Стоп]; docked "{activity} — сколько ещё?" sheet → `tfMoreMins()` plans the live track forward (off→on-plan in one tap) + "сменить занятие" foot.
  - Plumbing: `DEV.cockpit()` for testing; `st-off.tf-nextsheet` gets extra bottom clearance. `__latinAudit` clean on both live states.
- **FIDELITY PASS (v827–829, 2026-07-03) — after David sent the TRUE widget exports (`_epic-mockups/_widget-refs/`), confirmed the saved `*.html` canon is faithful (no drift), then closed real subtleties I'd rationalized past:** timer is Jost 800 heavy (was thin DM-Mono — biggest miss); live title-pill darkened to the near-black domain tint (#0e2033/#2b1408); off-plan matte disc kept its chunky `0 6px 0` shadow; countdown spells "через N мин". On-plan ring = **even AND chunky** (David's call): hard shadow kept, disc nudged up 3px so the green band reads equal top/bottom.
- Known minor: off-plan disc color follows the app's domain palette (reading→blue), where the widget used amber for «Чтение» — flagged to David; palette question, not a cockpit-render bug.
- **✅ WHOLE COCKPIT (slices 1–4) is now 1:1 with the true widgets.** Idle+plan / idle+empty / on-plan / off-plan all screenshot-verified.
- **SHIP 3 — PENDING: TIMELINE (slices 5 & 6). NOT STARTED — the deepest regression-zone work; give it a fresh session, not a long-session tail.**
  - Slice 5 (timeline day, mock 5 + fix): thermograph sky (lived-hours rose heat bands behind the timeline), converter-seam shimmer on the newest lived block's edge, now-line end-dots, week-strip battery bars, midnight seam + crown, **ghost-block padding fix** (icon must not touch the dashed border — verdict #5), wayback icon on ghost blocks. **Two-lane plan/real physics UNTOUCHED (verdict #6).**
  - Slice 6 (edge inspector, mock 6): right-edge panel rising FROM the tapped block (spring, day stays live), НАЧАЛО stepper, ДЛИНА chips resize the block live behind it, bare domain dots re-type, Ещё ghost → full editor, hold-to-fill удалить, pink halo. **Edits the PLAN lane only.**
  - GUARD: re-check the 4-point regression contract every timeline ship; drag/zoom feel = DEVICE-UNTESTED label. Timeline render lives around `buildPull()` [app.js ~3042] + `renderPulls()`.

## FULL-REDESIGN SPRINT (2026-07-03, David: "finish the full redesign")
- **Spec swarm ran** (12 parallel agents, ~1M tokens): every remaining screen has a paste-ready BUILD package in `_specs/_redesign-build/<screen>-BUILD.md`. Read-only on app code — swarm never touched app.js.
- **Cockpit device bugs fixed first** (v832-833): on-plan domain detection (timer now carries block domain), play disc wired, striped doors brightened, claim-state now offers «Отследить сейчас» (instant-track a plan whose window is open).
- **INTEGRATED + SHIPPED from the specs:**
  - ✅ **habits** (v834, bg fix v836) — Duolingo stone path, white icons on stripes + dense rhythm (verdict #14) + cool-purple canvas. Verified.
  - ✅ **journey** (v835) — tighter trail geometry (verdict #11) + holo-prism Return frame (verdict #12). Trail verified; Return frame device-untested (away-state only).
  - ✅ **goals** (v837) — Квесты room (foil hero card, tiered bounties, pip strip, pink session door, mint-promise, released shelf) + card-back WOOP flip (gold inscriptions, if-then plan, release rite). Room + flip verified.
  - ✅ **plan-day** (v838) — VERDICT #8 muddy-brown fix (per-domain cool jewel ink on all 5 matched surfaces) + beat-pips header + identity hero + ember «ДАВНО СОБИРАЛСЯ» row + striped selected chip + blue beat-door. Chrome verified; tint = color-math (device-compare). **DEFERRED sub-parts: the live mini-timeline strip + finish-cascade** (most invasive picker/distributePlan changes) — in plan-day-BUILD.md §3b(iv)+§3d.
- **REMAINING to integrate (specs ready, in priority order):** calendar · intro · onboarding · toolbox · players · chrome · game · timeline · (plan-day strip+cascade follow-up) (timeline last — its spec has a LOW-risk paint layer + a HIGH-risk edge-inspector behind `window.__edgeInsp`; ship the paint layer, keep the inspector opt-in). Each = read spec → integrate → screenshot-verify vs `_widget-refs/<screen>.jpg` → ship. Risk from the swarm: most low, plan-day/intro med, timeline high.

### PARKED (do not build)
- Game-pane attention economy / garden↔game connection — David: tabled, needs its own design session. Planting-ceremony VISUALS ship in Run 4 or later without economy changes.
- Skate/world/sinks beyond the binder.

## GUARDS
- Every ship: `bash _dev/preship.sh` → commit → push → verify Pages serves the new v (auto-retry is in the workflow).
- Timeline slices: the 4-point regression contract + planless-mode contract re-checked per ship; drag/zoom feel = DEVICE-UNTESTED label.
- Every new string → I18N.ru same commit (B4). Run `window.__latinAudit()` on each rebuilt surface.
- Data: additive S fields only; SCHEMA bump + migration if any shape changes.
- Old surfaces retire only when their 1:1 replacement is device-approved.
