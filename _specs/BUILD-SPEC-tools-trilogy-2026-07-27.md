# BUILD SPEC — Tools trilogy (Claude Design import, 2026-07-27)

Three approved design canvases from the claude.ai/design project "Tools menu redesign directions", imported verbatim to `_design-sync/tools-menu-2026-07-27/`. Implement all three in the real app (`app.js` + `index.html` only; `server.js` is generated). **Port the ACTUAL CSS values from the design files — never re-derive numbers.** Last round's fidelity misses came from re-derivation; the .dc.html markup is ground truth.

Design refs (all on disk, read them — sc-for/sc-if are template loops; `{{x}}` are data slots; static px/hex/radius values are literal truth):

| Work package | Design file | Winning frames (line ranges) |
|---|---|---|
| WP1 Toolbox preview + deck | `Stack Language Tournament.dc.html` | turn 22 lines 25–142 (22a/22b anim), turn 21 lines 143–708 (21e = integration, 21a = minute grid, 21f = Plus row → editor) |
| WP2 Session Editor | `Session Editor.dc.html` (COMPLETE file incl. data script at 1337–1658 — the content model: BASE seed rows, DURS/PRESETS, CATS 6×6-8 tool tray, HUE/TINT maps) | 4a lines 24–278 (= final shell "same as 2b" + build-mode empty state + pinned tray); knob defaults in data-props: footerOrder `save-gear-start`, scrollHint `fade`+`chevron`, tintedSettings ON, catTray ON, perStepExtras ON |
| WP3 Activity Picker + Arranger | `Activity Picker.dc.html` | 18a lines 27–493 (canonical: Pick → folder sheet → tune panel → Arranger) |

## WP1 — Toolbox tap-open preview (upgrades `tbxBuildDose`/`tbxOpenDose`, @SEC:TOOLBOX2 ~5466–5733)
1. Dose card → 21e preview: add per-step TIMES (right column, 10.5px 800 #9a6a86 tabular; times = tbxScaleTrack at current dose), "more" chip (ti-adjustments-horizontal) after 2/5 chips → opens in-card minute grid (#1d0e18 r13 pad10, 4-col, label "YOUR MINUTES — THE STEPS RESIZE ABOVE" per 21a) with 8 presets (data script had 10/15/20/25/30/45/60/90-style ladder — use 21a's grid values from the design; persist via tbxSetDose extended to any minute — guarded additive read, keep 2/5 fast-path).
2. Turn-22 opening animation "the tile is a deck": on open, the tile's peek coins fly into the step rows (FLIP clones, --ease-settle), tile left face-only + ringed; panel `stackExpand .34s cubic-bezier(.34,1.56,.64,1)`, transform-origin the tapped tile (21e: `margin:0 -53px`, transform-origin per tile column). Build BOTH 22a (all at once, default) and 22b (dealt one-by-one, ~70ms stagger + slight tilt) behind `var TBX_FAN = "fan"` ("deal" = 22b) — David picks on device; expose in the dev Design tuner if trivial, else the var alone.
3. Plus row (21f): TBX_PLUS=true → "Adjust steps & timing" chevron row opens WP2's Session Editor for that stack id (replaces the v1 composer entry); keep PLUS badge + whisper fallback when TBX_PLUS=false.
4. Close = chevron-up or re-tap tile (existing single-open semantics stay; category-panel interplay per current tbxOpenCat resync).

## WP2 — Session Editor (NEW full-screen overlay surface; replaces the v1 composer for stacks)
1. One shell (4a): radial purple bg `radial-gradient(120% 72% at 50% 0%, #2c1442 0%, #1c0d26 58%, #150a1a 100%)`, header back-circle 40px + title Baloo 27 + running total gold 21 (`#ffc41f`, "N min").
2. Blocks mode: rows = full-width candy cards r22 (striped `repeating-linear-gradient(115deg, rgba(255,255,255,.3) 0 13px, transparent 13px 28px)`, icon 24 + title Baloo 21 ink + time 16 right, grip bar 32×4 bottom-center); tap → expand in place (edPop .2s): desc, duration chips rail (horizontal scroll min-width 54 r13 Baloo 14, right fade + chevron hint — scrollHint=fade, arrow=chevron), per-step flag chips (perStepExtras ON), controls row 44px: ↑ ↓ + Swap (flex) + Delete (pink). Selected ring per r.ring.
3. Settings mode (gear toggles): 5 collapsible tinted cards (tintedSettings ON — bg from each card's hue in the data script): Voice (3 voice cards + talk-amount segmented), Sound (3-col grid + volume bars), Guidance (list + checks), While it runs (toggle rows w/ 32×18 switches), Advanced (dashed card: keep-as-new + name chips + adv toggles). Settings persist per-stack additive on S.tools (e.g. S.tools.tbxPrefs[id] = {voice, talk, sound, vol, guide, flags…}) — wire what the runner already supports (voice/med/bg via @SEC:AUDIO/TTS + runStack opts); store the rest inert (honest: no fake toggles — omit a control if the runner can't honor it and note it in the handoff).
4. Tool tray (catTray ON): bottom edge panel rgba(0,0,0,.34), r24 top, grab-handle chevron collapse; category chips rail (data script CATS: Breath/Body/Mind/Feel/Rest/Log) + 4-col 52px coin grid; tap adds a step (mapped onto STACK_TOOLS ids like TBX registry `track` entries; unknown tools → nearest STACK_TOOLS id, keep design label as the row title).
5. Footer order save-gear-start: [Save 56px floppy][gear 56px][Start pink #ff4fa0 flex, glow `0 0 34px rgba(255,79,160,.45)`].
6. Build mode = same editor, empty list + dashed pink "Nothing in it yet" (ti-stack-2) + tray PINNED open. Entry: wherever the v1 composer/build-a-stack entry lives now. Save → tbxCustoms()/tbxSetEdit (existing stores). Editing an existing stack seeds rows from tbxTrack(id).
7. Mount as its own overlay container (not #sheet, not the notebook — do NOT add a third variant of those; this REPLACES the v1 composer surface). Register interactive selectors in PANE_GUARD (@SEC:CAROUSEL contract) + the overlay list at ~5217 if it can sit above home.

## WP3 — Activity Picker + Arranger (upgrades `bentoPicker` app.js:9743→~10062 flow)
1. Entry unchanged: timeline tap-empty-slot (makeBlock, :9743) → the NEW picker replaces bentoPicker's surface. Also check other bentoPicker callers (grep) — they get the same picker in "single-pick" seeding unless trivially incompatible (then leave them on the old bento and say so in the handoff).
2. Pick state (18a): kicker = the real gap ("N min open · after <prev block title>" — compute from the tapped gap; fallback "What next?" alone), folders wall 2-col (r20 #1e0b18, 3×2 mini preview), SAVED & READY-MADE dashed cards (chains/stacks/days = saved arrangements + tbx customs), bottom tune-panel: picked queue 40px coins + peeks + x, focus title+mins, length rail chips (63px min, overflow-x + fwd chevron), Priority + Steps buttons (+trash), pri chips row, chain step readout / "Add a step". Footer: selection label + primary btn — 1 pick = "Add", 2+ = "Arrange" (gold/pink per 18a doneBg slots).
3. Folder sheet: bottom sheet #1c0a17 r28-top, grab bar, 44px coin header + X (+back when drilled), naming input row (dashed) for YOUR OWN → New; content grids: bundles 2-col (26% radius squircles + peeks + count badge), tools 4-col circles, domain groups 5-col 28% squircles + group headers + More/New tiles. Populate from the app's existing activity vocabulary (DOM domains + bentoPicker's current item source + TBX stacks for the stacks folder + saved days/chains) — reuse the app's data, design's layout.
4. Arranger (18a isArr): full-screen state (bg `radial-gradient(115% 65% at 50% 0%, #44193a 0%, #2c1224 55%, #210e1a 100%)`), header back + "Arrange" Baloo 25 + total gold; rows = hour gutter (38px col: start hour Baloo 24 #efd7e6 + dash) + block card (r19, 3px ring, striped candy fill, ink #160510 text, chevron unfold `grid-template-rows 0fr→1fr .34s`), chains show 2 peek cards behind (translate/opacity transition); unfolded: chain = step readout (32px coins) + ONE scaling length rail + "Adjust steps & timing →" (opens WP2 editor) + trash; activity = length rail + Priority + Steps + trash + "Add a step" dashed. Drag to reorder by the card header (onPointerDown, touch-action:none) — sequential times recompute from the gap start (no overlaps; blocks lay end-to-end). "+ Add a block" dashed → back to Pick.
5. Footer: Save square (floppy — saves the set as a named chain: additive store S.tools.pkChains = [{id,name,steps:[{title,mins,domain/color,prio,catK}]}] guarded reads, NO schema bump) + big pink "Add" → writes ALL blocks into blocks(k) at the arranged times (existing shape: {id,time,mins,title,prio,color,catK,domain}), reflow(k), save(), renderToday(). Cancel/back with nothing picked = zero litter (match current onCancel stub-removal contract).
6. REGRESSION CONTRACT (verify all 4 before done): tap-empty-slot creates block(s) at that time ✓; started/past untouchable ✓ (picker only ADDS at/after the gap); drag/resize + tap-bubble→editor untouched ✓; day-nav/scroll untouched ✓ (picker is an overlay; zero timeline listeners added or changed).

## Laws (constitution — non-negotiable)
- ZERO new innerHTML wipes (ratchet fails the ship). Use the add()/child-drain idioms.
- Every user-visible string through tr() + RU dict entries (Object.assign(I18N.ru,{…}) in the same commit, B4 law).
- All new state ADDITIVE on S.tools/guarded reads — no SCHEMA bump unless you reshape an existing persisted field (then SCHEMA+MIG pair).
- Palette: only design hexes/domain vars above; Jost 800 + Baloo 2 800 — ADD `800` to the Jost weights in the index.html Google-Fonts URL (currently 500;600;700).
- New interactive elements → PANE_GUARD registry.
- No new scroll containers on the home surface (the preview inserts in-flow; editor/picker are their own overlays).
- Copy: the design's lines ship as written (placeholder-approved) BUT run `python3 _dev/copy-audit.py --file <new-lines.txt>` on every new user-facing line; fix deterministic failures. (Adversarial judge pass runs after the build, separately.)
- Comments: match app.js house style (dense single-line, @SEC anchors); extend the @MAP header for new sections; keep TOOLBOX2 comment updated.

## Verify (in-repo, before reporting done)
1. `node --check app.js` after each WP; `bash _dev/preship.sh` at the END only (it bumps the version + regenerates server.js) — do NOT commit preship's bump per-WP.
2. Boot in the preview (launch.json `alter`, port 8123, mobile 390×844): no console errors; toolbox tap-open → preview matches 21e (screenshot), minute grid opens, fan animation runs; editor opens from Plus row + build entry (screenshots blocks/settings/build vs 4a); timeline tap-empty-slot → picker (screenshot vs 18a) → pick 2 → Arrange → Add lands blocks correctly; `DEV.designAudit()` PASSES 12/12 (tuner vars + alter_tuner localStorage cleared first).
3. Gesture feel (arranger drag, tray fold, deck animation feel) = DEVICE-UNTESTED — say so, never "verified".
4. Commit per WP (3 commits, message style from git log), NO push.

## Out of scope (do not touch)
Timeline internals (tickCharge/buildPull/pull pager), the block editor `editorSheet`, journey/game/cockpit, server.js by hand, hero rows (WP1 keeps them), the old bento fallback path used by non-timeline callers if any prove incompatible.

---

## ADDENDUM — David's design-handoff corrections (2026-07-27, supersedes conflicting lines above)
Verbatim source: `_design-sync/tools-menu-2026-07-27/DESIGN-HANDOFF-NOTES-2026-07-27.md` — read it in full; it is the interaction-contract law. DS guide (`_ds/…/readme.md`) is the visual law. Deltas vs this spec:

1. **WP1: turn-22 fan-out is DISCARDED.** No tile-emptying, no shard flight, no TBX_FAN variants, no 22a/22b animation. Tiles keep their peek shards permanently ("deck-with-shards"); the 21e preview opens as a plain in-place panel with its own `stackExpand`-style pop (.34s spring, transform-origin at the tapped tile). Everything else in WP1 stands (per-step times, more → minute grid, Plus row → WP2 editor).
2. **WP3 arranger:** whole bubble = drag handle (press + pull >6px reorders; clean tap toggles expand; tapping B while A open closes A, opens B — one tap). NO grip line. Uniform-height bubbles (duration ≠ height). Chevron: collapsed = up, expanded = down. Footer = Save (save-as-chain) + **Start** (primary; label "Start" — lands the arranged blocks on today starting at the gap). Hour gutter: 38px wide, big hour numeral, dash when same hour as previous, 15px gap to bubbles.
3. **WP3 picker footer contract:** wall footer and folder-sheet footer byte-identical (metrics + content) — opening a folder must not shift the bottom. Panel padding `11px 22px 0`, action bar `10px 22px 28px`. Queue icons 40px, chains = deck bubbles (2 peeks); label under icon = the pick's NAME (truncate ~58px), never the time. ✕ removal badge on each icon (deleteStyle "x on icon"). Tune panel header = name + "for how long?" hint + big time right, NO icon coin. Priority button shows its value when set ("Must"/"Should"), the word "Priority" only when unset (same in arranger). Chains first-class: rail scales ALL steps proportionally, hint "for how long? scales every step", Steps = read-only scaled readout, chain panel-coin identity gold `#ffc41f` + `ti-stack-2`. Action bar: kicker + label ("Tap what you feel like" / "One thing" + name / "ON YOUR PLATE / 3 things · 3h05" — never a name→name→name list), primary = "Add to today" (1 pick) / "Arrange" (2+), no clear-X. wallPanel default "minimizes": footer collapses to action bar only; centered chevron tab at the TOP of the footer toggles; chevron up when minimized, down when expanded.
4. **WP3: NO Whole-days folder.** Days are out of the picker entirely (they'll live in Plan-my-day / week planner / evening review later). Saved section = chains + stacks only.
5. **WP2:** canonical shell = **2b** (4a = 2b from empty). Step length = scrolling chip strip + fade/chevron inside the block — NEVER a −/+ stepper. Footer sits directly on the plum background — no dark divider band behind it. Backport from 18a where cheap: priority-shows-value, chevron directions, whole-row drag reorder (don't block on the drag one — note if skipped).
6. **DS selection law (choice-row v3) for ALL chips/rails/pickers in this build:** at rest dark tint + own-hue outline + bare colored icon; selected = ignite into the option's OWN hue candy stripes (45°, 9px) + ink text + check. **Never gold for selection** (gold = earned/totals only). Sentence case on every button; ALL-CAPS only for tiny kickers; tap-don't-type (the picker's naming input is the sanctioned exception).
