# PLANNER CANON — the mockup David likes (pixel-sampled, LOCKED 2026-07-20)

Reference image: `_refs/planner-canon-mockup.jpeg` (588×1114). Gym / Lunch / Claude code NOW / Read.
These are David's liked nuances, sampled from the actual pixels. Match these VALUES, not a description.

## The ground
- Flat deep plum: **#1d0e21** (H287 S57 **V12**). Near-uniform, no lines, no per-hour tint.

## Bubble states (the whole grammar)
**DONE / lived = BRIGHT stripes.** (Gym, blue)
- Stripes are HIGH-VALUE, pastel, LOW-contrast: light `#7ab5e1`, dark `~#5a9fd8` (**V≈85–88**, S≈45–50). Bright and friendly, NOT dark jewel tones.
- Border = the BRIGHT domain colour (`#6fb2dc`), ~2px. **NEVER near-black.**
- Label = **white**, bold, big.

**QUIET / planned = dark fill + bright colored edge.** (Lunch brown, Read purple)
- Fill = a DARK domain tint, **V≈25–28** (Lunch `#422d2a`, Read `#301e48`).
- Border = a BRIGHTER domain colour edge (visible, not black).
- Label + icon = the domain colour (tan for Lunch, lavender for Read).

**NOW / charging.** (Claude code, pink)
- The block STRADDLES now: **top half = BRIGHT stripes** (`#ed83b3`/`#ffeef9`, V≈92–100) = the lived/charged portion; **bottom half = dark matte** (`#431c37`, V≈26) = not-yet-lived future. Stripes grow DOWN as it charges.
- Border = bright pink `#f183b2`; PINK HALO glow around the whole block; a pink **"NOW"** pill (`#ff2f92`-ish) at the now-line.

## Hard rules David called out
1. **NO thick black outline.** Borders are the bright domain colour. The `#160510` near-black border is WRONG.
2. **Bubbles are BRIGHT / higher value / friendly** — not dark and muted. Stripes go toward WHITE, not toward ink.
3. **Big, friendly** — large white bold labels (~19–20px), roomy cards (MINFLOOR ~64), generous radius (~16–18, height-scaled so short cards never pill).
4. Done = stripes · quiet = colored outline + dark fill · now = stripes-top + matte-bottom + pink halo + NOW pill.

## Header
- "Today" left · **bright pink D pill** (`#e8609e`) in the D/W/M toggle · gem count "1,240" in GOLD on the right (💎).

## Process note (why this file exists)
The canon mockup lived only in a chat and was lost for 8 iterations. Images now save to `_refs/` and their analysis here, immediately.
