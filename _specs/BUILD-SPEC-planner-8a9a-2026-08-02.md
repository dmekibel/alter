# BUILD SPEC — PLANNER WEEKS 8a + MONTHS 9a (David's picks, 2026-08-02)
*Source order: David's two frames (Weeks, Months) → the live artifact (`Day Planner.dc.html`, frames `8a Weeks — plan mode with stack picker` + `9a Months — plan mode with stack picker`, full markup + data model read). This implements the WHOLE-DAY STACK layer on the planner's existing W/M zooms: pick a day template, place it on days, Save commits it as real plan blocks. Opus agent, SOLO, no commit, no preship. WAIT-CONDITION: launch only after the home-2c build has landed and shipped (same file).*

## 0. Where this lives in the app (reuse, never a 4th menu system)
- The planner already has zoom modes day/week/month (`zoomMode`, `#zoomTabs .zt`, `weekGrid`). 8a/9a REPLACE the week and month zoom renderers' content. The D/W/M segmented control = the existing zoom tabs, re-skinned per §1. The day zoom (the timeline) is UNTOUCHED — regression zone, do not enter `buildPull`/timeline rendering.
- Day templates: the app has `presetsSheet` (masterpiece days). The DAYLIB below becomes the canonical data (`DAY_STACKS` registry + `S.tools.dayCustom` additive store for YOURS entries later; this build ships the 5 artifact entries, custom-creation is NOT in scope).
- APPLY path: placing a stack on a day STAGES it (in-memory); **Save** writes each staged day's skeleton as real plan blocks via the existing block-write path (`blocks(dayK)` + `reflow` + `save`), respecting the future-only law: never write into the past or across the now-line; today = only future hours get blocks (start the skeleton at the next free slot). Clearing a staged day removes only its staged state; Save never deletes existing user blocks — a day that already has blocks gets the stack APPENDED into free space (flag any collision handling choice in the report).

## 1. Shared chrome (both views; all values quoted from the artifact)
- Header row (`padding:54px 18px 6px`, gap 8): left D/W/M segment: inactive letters Baloo 800 15px `#96637e` gap 6; ACTIVE letter = 34x34 squircle `border-radius:11px; background:#ff4fa0; box-shadow:0 0 16px rgba(255,79,160,.55);` letter `#fff2f9`. Center title "Weeks"/"Months" Baloo 800 21px `#fff2f9`. Right **Save**: `padding:8px 15px; border-radius:13px; border:2.5px solid #160510; background:#ff4fa0; box-shadow:0 3px 0 #160510, 0 0 16px rgba(255,79,160,.4); color:#160510;` Baloo 800 14px; press translateY(2px). Save is VISIBLE only when staged changes exist.
- Context row (`padding:0 18px`): left label 14px 700 `#ff4fa0` ("Next week" / month name); center Baloo 800 14px `#d8a9bb` (date range / "next 4 weeks").
- Status + hint (centered, `padding:9px 18px 2px`): status Baloo 800 14px `#e8c7d8` = "N of 7 planned · M open" (week) / "N of 28 planned · M open" (month); hint 12px 700 `#7d5fa8` with `ti-focus-2` 13px = "tap a day, or slide a stack into one" ↔ "now tap its day below" when a picker stack is armed.
- THE STACK PICKER section: label "THE STACK PICKER · WHOLE DAYS" 10px 800 ls1.5 `#96637e`; 2-col grid gap 9; card: `padding:11px 12px; border-radius:18px; background:#241019; box-shadow:0 4px 0 #0d0309;` press translateY(2). Card contents: up to 3 coins 26x26 radius 9 (each `box-shadow:0 2.5px 0 <deep>`, glyph 12px `#fff2f9`; coins = the skeleton's unique non-rest hues in order); kicker 8.5px 800 ls1.3 in the stack's hue, ellipsized; name Baloo 800 14px `#fff2f9`.
- Interactions (both views): (a) tap a day/date then tap a picker card = place; (b) OR press a picker card and SLIDE onto a day (pointer drag; ghost coin 46x46 radius 15 in stack hue, `rotate(-4deg)`, `box-shadow:0 6px 14px rgba(0,0,0,.5), 0 0 20px rgba(255,79,160,.35)`); (c) while dragging, a trash well appears bottom-center (72px circle, `border:2.5px dashed #ff5f7a; background:rgba(255,95,122,.12); box-shadow:0 0 22px rgba(255,95,122,.35);` `ti-trash` 26px `#ff5f7a`) — dragging a PLACED day's stack to it clears that day. Selected day wears the pink ring (`box-shadow:0 0 0 2.5px #ff4fa0` family + glow, per artifact ring vars). PANE_GUARD: register every new draggable/scrollable selector so the pane-swipe never steals the gesture (@SEC:CAROUSEL contract).
- DEVICE-UNTESTED note mandatory for all drag feel.

## 2. WEEKS (8a)
- Columns zone `height:328px; padding:8px 14px 0; grid repeat(7,1fr); gap:7px;` — 7 tall columns radius 16, empty bg per artifact col var (dark well `#241022`-family from ring/bg vars), planned = the day's stack rendered as SOLID CANDY PILLS stacked top-down: each block `height:max(13, round(min*0.62))px`, radius per artifact block vars, hue = the block's mapped APP category hue; rest blocks wear the rest teal. Pills are shorter-width with radius; see artifact block vars (w/as/r per state). Column label row under: Baloo 800 12px, planned day label in its stack hue, else `#96637e`; Su..Sa.
- Week staging model: `days[7]` (null | stackId), `sel` index; tap column toggles select (pink ring); picker tap places; artifact seed state is mock data — the REAL initial state reads NEXT WEEK's existing staged/planned days (a day whose blocks were written by a stack remembers its stackId additively; fresh = all null).
- The view targets NEXT WEEK (the artifact header: "Next week · 2 – 8"). Keep the existing week paging if present; default lands on next week in plan mode.

## 3. MONTHS (9a)
- Weekday header row M..S 11px 800 `#96637e`; grid `repeat(7,1fr); grid-auto-rows:47px; gap:6px; padding:6px 14px 0;` 28 cells (next 4 weeks).
- Cell: radius 13; empty = dark well with date 11px 800 (dim ink `#6b5680` family per artifact); planned = the cell wears the stack as ONE CANDY COIN 26x26 radius 9 (stack hue face, `box-shadow:0 2.5px 0 <deep>`, glyph 13px) + up to 2 shard layers behind (`translate(-2.5px,-2.5px)` and `(-5px,-5px)`, radius 9, shard hues = the stack's other coin hues), date ink flips light on planned cells. Selected date = pink ring.
- Same staging + picker + drag + trash + Save grammar as Weeks.

## 4. THE DAY-STACK LIBRARY (DAY_STACKS registry; artifact DAYLIB4 verbatim except the mappings below)
Five stacks ship: **Masterpiece day** (`#8a5cf0`, ti-sparkles, sub "3 waves · the Big 3", skeleton: am bookend 45m ti-sunrise / deep work wave 1 90m ti-code / deep rest walk 20m REST / deep work wave 2 90m / deep rest lunch + nap 45m REST / deep work wave 3 90m / energy train 1h ti-run / love together evening ti-heart / pm bookend 30m ti-moon; stop-list: phone before the bookend · inbox till 10 · screens after 10) · **Deep work day** (`#36b3f0`→APP focus hue, ti-code, "2 waves, protected") · **Rest day** (teal, ti-moon, "planned rest counts") · **Errand day** (ti-shirt/ti-tool per artifact, "yours · 5 things") · **Studio Saturday** (ti-pencil+ti-heart coins, "yours · make all day").
- HUE MAPPING LAW: skeleton block hues map to the APP's own category/domain registry (deep work = the app's focus hue, energy = move, love = connect, bookends = their ritual hues, rest = restore teal). The artifact's raw hexes are the mock's approximations; the app registry wins (same rule as the 2c build). Flag any block with no clean category home.
- COPY: the artifact's names/kickers/subs are David's picked frame copy — ship verbatim EXCEPT em-dashes in the note/skeleton lines (the artifact's "wave 1 — 90m" style dashes): swap to the app's native middle dot per the shipped v1229 precedent (the no-em-dash law; flag each swap). Run every new string through `python3 _dev/copy-audit.py --file` before finalizing (mechanical Gate 1; the strings are artifact-verbatim so Gate 2 is satisfied by David's own pick — note this in the report). All strings via tr() + RU dict same-commit. The stop-lists and long `note` paragraphs ship ONLY if a surface in 8a/9a renders them (in the artifact they belong to the 4a library cards, NOT the picker) — do NOT add UI for them; store them in the registry for the future library surface, unrendered.
- Sunday/Saturday YOURS entries: ship as fixed registry entries for now (custom day-stack authoring is out of scope; note in report).

## 5. Audit + verify
- designAudit: these are planner-zoom surfaces, not the HOME board — add NO home gates. Add a minimal "w8/m9 chrome" gate ONLY if cheap (Save button styling + picker card bg `#241019`); otherwise verify via the checklist below and screenshots.
- Preview verification (mobile 375x812): zoom to W → columns + picker render; select Tuesday → ring; tap Masterpiece → column fills with pills; Save → real blocks exist on that day (verify via the app's own day data, e.g. `blocks(K)` length + titles), status line counts update; zoom M → grid renders, place + clear a date; drag path can be smoke-checked only (synthetic drag lies — DEVICE-UNTESTED). node --check. designAudit (existing gates) must still pass untouched. Screenshots: weeks idle, weeks selected+placed, months placed.
- REPORT: edit sites by anchor, the hue/category mapping table you chose, every em-dash swap, collision-handling choice, PANE_GUARD additions, node + audit results, flags.
