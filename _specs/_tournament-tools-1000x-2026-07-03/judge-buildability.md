## A (system-first) — 7/10
STRENGTHS: Most honest recording economics of the three: Batch A itemized to the clip (~35 x EN+RU), Batch B separately gated on David's register approval, and the only proposal that flags 'VERIFY the manifest actually contains POOLS-v1 §3/§14 first' before flipping stretchFloor/gratitudeBeat (correct diligence — I checked, gen-voice.py §8 does extract them). Keeps beatRunner untouched (real engine reuse). prescribe() degrades to uniform weights cold-start ('never worse'), orderPlan() is a pure function replacing hardcoded 20/30/25 — both preview-testable without audio. S8 states the register-skin truth plainly: skins re-voice SCREEN only, spoken bank stays neutral forever. RU-dict same-commit discipline named at S4.
WEAKNESSES: S1 is a hidden rewrite-the-world step wearing a 'behavior-identical' costume: merge five taxonomies AND route composeRitual/composeCharge/playTrack/meditationQuick through a brand-new composeSession AND collapse the meditation tri-path AND add the live-flag — all in the FIRST ship, where 'same audible session as before' is exactly the thing preview can't prove (audio scheduling feel is device-only). That step must be split or it's the classic build→ship→break→revert setup. Second flaw: the entire text sweep — David's loudest ask — is parked at S4 behind three engine steps; if the run stalls at S2, David wakes to zero visible change. The sequence is architect-ordered, not David-ordered.

## B (voice-first) — 8.5/10
STRENGTHS: The only sequence I'd hand to a build session tomorrow nearly as-is. V1 is the perfect first ship: pure SCREEN strings + data-cite templates, no engine risk, fully preview-verifiable, RU dict same-commit under the July-7 freeze, and it lands David's actual ask ('text A LOT') on day one. V2 is bounded to the tools region with the honest DEVICE-UNTESTED label pre-written. ONE recording batch (V4), honestly totaled at ~85–95 clips x EN+RU with a per-pool breakdown, gated on David's POOLS-v2 edit pass — the exact contract gen-voice.py already implements. Keeps beatRunner alive (registry run() for beats-form). Never touches the timeline/gesture regression zone; 'V2 removes call sites, adds none.' The 30-clip number bank is correctly identified as 'the cheapest 1000x purchase in the whole plan.' Rejection #8 (don't resolve David's reserved calls by stealth) is the right build-politics.
WEAKNESSES: One real sequencing hole: V2's composeStack replaces the Step-N-of-M cards with 'recorded glue clips' — but those glue clips are new recordings scheduled for V4, two steps later. As written, V2 either ships stacks with silent seams (unstated) or is secretly blocked on the recording batch. C solves this exact problem for free (reuse the already-recorded 'One slow breath — arrive here.' clip) and B should have. Minor: V2 still bundles registry-merge + two tool migrations + composeStack + readRecord in one ship — splittable, and should be.

## C (experience-first) — 6/10
STRENGTHS: Contains the two best pure-buildability ideas in the whole tournament. (1) Retire-first: 'dropping it from the pool array costs nothing and orphans nothing (the clip just stops being scheduled)' — the zero-cost kill mechanism for weak spoken lines that A and B both missed, which decouples the text cleanup from the recording batch entirely. (2) Zero-recording stack transitions via the existing ◆ clip. E1 (THE MARK, screen-only unified finish) is a strong, bounded, preview-verifiable first ship, and the plan is explicitly July-7-freeze-aware. mirror()'s guard contract ('a template renders ONLY when every read has real data; otherwise the timeless fallback') is the right degradation rule. Recording batch honestly sized (~45–55 x EN+RU) and correctly last.
WEAKNESSES: E4 is a genuine rewrite-the-world step: absorbing beatRunner into timelinePlayer via a new hold-segment type means re-porting SIX working Stutz tools onto a changed engine — and hold-then-reschedule inside a pre-scheduled iOS audio timeline is precisely the class of thing preview lies about. That violates the REUSE-engines law in spirit (it rewrites a working engine rather than reusing it) for near-zero user-visible gain; A and B both keep beatRunner and are right. E5's tray adds long-press-swap and drag-out chip gestures — brand-new device-untestable gesture surface. E10's banded block is the only piece in any proposal that enters the TIMELINE regression zone, and it builds a default for David's reserved decision #9 (the 'veto-able' framing doesn't cure the options-first law: show 2–3 in chat, he picks, THEN build). Add the candle and it's 10 steps — the scope-widest plan with the riskiest engine move.

## GRAFTS
- C's retire-first policy, verbatim the keystone of text economics: 'every weak SPOKEN line lives in a rotation pool — dropping it from the pool array costs nothing and orphans nothing (the clip just stops being scheduled).' This lets ALL spoken kills ship in step 1, with re-records optional and batched later.
- C's zero-recording stack transitions: reuse the already-recorded 'One slow breath — arrive here.' ◆ (POOLS-v1 §2) + 4s gap as the inter-tool seam — 'Zero new recordings for v1 transitions.' This unblocks the eyes-closed-law fix from the recording batch (plugs B's V2→V4 dependency hole).
- B's V1-first ordering: 'THE SCREEN SWEEP (no recordings, no engine risk)… This alone is a visible "the app got smarter about me" jump' — the entire free half of David's text ask ships first, RU dict same-commit, before any engine is touched.
- C's E1 THE MARK as an early screen-only ship: the unified finish card (stripe-gauge drain, delta line, chronicle typewriter line, forward bridge) killing the 4 divergent closers — bounded, preview-verifiable, and the reward moment David feels immediately.
- A's acceptance bar for the registry merge: 'every preset must produce the same audible session as before' — but applied to a SPLIT step (registry merge behavior-neutral first, composer routing second), never as one ship.
- A's cold-start safety clause for the prescription engine: 'Cold start: uniform weights — the system degrades to today's behavior, never worse.' Plus orderPlan() as a pure function replacing runFullStack's hardcoded 20/30/25% — testable headless per the composeRitual precedent already in app.js:7429.
- A's manifest-verification habit: 'VERIFY the manifest actually contains POOLS-v1 §3/§14 first' before migrating stretchFloor/gratitudeBeat. (I checked — gen-voice.py §8 extracts those lines, so the migration is genuinely zero-recording; keep the verify step as ritual anyway.)
- The number bank, sized per B ('~30 clips (EN+RU) unlock every spoken chronicle-cite in the app forever') with C's composite tails added ('points lighter' / 'points heavier — and you showed up anyway' / 'catches') — richer sentences from the same halves mechanism than A's minimal 13.
- C's mirror() guard contract as the data-cite law: 'a template renders ONLY when every read has real data; otherwise the timeless fallback line renders' — no half-filled templates ever reach the screen.
- B's cheap seam closures bundled into the engine step: S.tools.running guard (double-gauge hole) and TTS.warmAll() moved to the launch gesture (beatRunner silent-clip gap).
- A's S8 register-skin honesty as a permanent law: 'skins re-voice SCREEN ONLY — spoken clips cannot be re-skinned by a dict, so the spoken bank stays register-neutral forever.' Prevents anyone ever proposing to double the clip bank.
- B's single-batch recording discipline: ONE gen-voice session (kills + replacements + new pools + number bank, EN+RU), gated on David's POOLS-v2 edit-in-place approval — never dribbled recordings across steps.

## KILLS
- C MERGE 2 — absorbing beatRunner into timelinePlayer via hold-segments and re-porting the 6 Stutz tools. Violates the REUSE-engines law (rewrites a working engine instead of reusing it); hold-then-reschedule inside iOS gesture-gated audio is exactly the preview-lies territory; risks six working tools for zero user-visible gain. beatRunner survives (A and B agree).
- C NEW 3 — the banded session block built as a default. It is the ONLY item in any proposal that enters the timeline regression zone (the 3x-rebuilt core), AND it pre-builds David's reserved decision #9. 'Veto-able default' violates the options-first law (show 2–3 in chat, he picks, THEN build). Out of this gameplan entirely.
- C NEW 6 / E10 — the candle in this run. A's rejection stands on evidence: 'high charm, zero system leverage,' plus it depends on David's open mic-blow-out decision. It is its own self-contained R-step later; here it is scope.
- C E5 Level-1 tray gestures — long-press-to-swap and drag-out-to-remove chips. New drag surfaces are device-untestable by the constitution's own verification law, and drag is the app's historically worst feel-regression area. The Level-0 card (2 taps to voice) and the existing Level-2 desk survive; the drag middle tier does not ship in this gameplan.
- A S1 as a single ship — 'behavior-identical' merge of five taxonomies + new composeSession + meditation tri-path collapse in one step. A hidden big-bang whose acceptance criterion (same audible session) is device-only-verifiable. Must be split: registry merge behavior-neutral first, composer routing as its own ship.
- A's ordering that parks every text change at S4 behind three engine steps — inverts David's explicit ask ('work on the text A LOT'). The content survives; the sequence dies.
- B V2's transitions-need-V4-clips dependency as written ('Next — the breath.' glue pool doesn't exist until the recording batch) — either the eyes-closed stack fix silently ships with dead air or the step is blocked two steps forward. Replaced by C's existing-clip transition.
- Any spoken rewrite shipping outside the single David-approved batch — all three proposals nominally agree, but A's Batch A at S5 plus Batch B at S6 is two recording gates; collapse to one (B's model).

## VERDICT
Build it on B's spine with C's economics and A's math, in strictly David-ordered sequence. Step 1 is the text landslide and it is nearly free: the full SCREEN sweep (all three proposals converge on essentially the same 20 rewrites), THE MARK unified finish, the mirror()/cite() guarded templates, the ban-list lint, the emoji fix, and — via C's retire-first policy — every weak SPOKEN line dropped from its rotation array at zero recording cost, RU dict rows same-commit under the July-7 freeze. That single preview-verifiable ship delivers the majority of 'make the text way more clever' before any engine is opened. Step 2 splits A's S1 into two behavior-neutral ships (registry merge with the same-audible-session bar, then composer routing), keeping beatRunner alive as B and A both insist. Step 3 fixes the eyes-closed law with composeStack using C's already-recorded transition clip plus the confirmed-recorded stretchFloor/gratitudeBeat migration — zero recordings, honestly labeled DEVICE-UNTESTED. Step 4 is A's computed order (orderPlan + prescribe with cold-start-to-uniform safety) finally reading the gauge ledger. Step 5 is the ONE recording batch — B's totals and gating contract, C's number-half tails and chronicle frames — after David's POOLS-v2 edit pass. Step 6 is the meditation spine on the existing teacher banks. What never enters this gameplan: any touch of the timeline regression zone, any drag gesture, the candle, the banded block, or any resolution of David's reserved decisions — those return as options-first chats. The result is a sequence where the first two ships are pure strings, every engine step has a rollback-sized blast radius, exactly one step waits on recordings, and David sees the app get cleverer about him on day one rather than at step four.
