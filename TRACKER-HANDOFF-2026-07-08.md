# ALTER HANDOFF — 2026-07-08 (voice lock + 3-voice journey build)
*Paste this to prime a fresh session. Read `alter/CLAUDE.md` first (the constitution), then this.*

## UPDATE 2026-07-18 — FABLE PLANNING SESSION (design + thinker synthesis; NOTHING BUILT, all pending David verdicts)
**Model law hardened (in CLAUDE.md rule 0):** NO building and NO chat-widget rendering on Fable, ever, unless David explicitly says so. Fable = gameplan + taste enforcement; Opus renders widgets in chat and builds; Sonnet/Haiku read and mine. Also new memory `agent-fanout-guardrail`: delegation prompts must cap subagent fan-out (the Rohn mine fanned into ~9 children).
**Corrections logged (memory `oren-john-marketing-canon`):** David's marketing canon = Oren John / orenmeetsworld (balance KB, 108 transcripts), distinct from Ava. Withers has NO "inherited voice" concept: his real frames are factory settings / belief cycle / alignment / emotional scale / stimulus-GAP-response.
**New mine:** `_specs/JIM-ROHN-SOUL-2026-07-18.md` (all 8 books, 20 atoms, 60+ quotes, ALTER-application, Herbalife+Johnson lineage). Killer find: Five Major Pieces foreword says "to change the effect, we must ALTER the cause": the app's name is in Rohn's thesis sentence; Five Pieces map 1:1 to app surfaces (Philosophy=journey, Attitude=tools, Activity=planner, Results=tracker, Lifestyle=sanctuary).
**Name check:** quick web search found no head-on "Alter" collision in wellness/planner (nearest: AlterMe, DNA/ring wellness). Formal trademark/ASO check owed only before store wrap. Channel stays "David's Field Guide" funneling to product ALTER; no rename needed to start posting.

**PENDING DAVID VERDICTS (the whole session hangs on these; lock specs only after he answers):**
- *4 design calls:* (1) kill pane-swipe → Duolingo tab bar Home/Today/Journey/World, cockpit = overlay state (supersedes the locked three-pane-spine; needs explicit yes; frees one-finger world pan + calendar day-swipe). (2) Home = mission control (guardian contextual line + hero circle + BIG points + streak + day story bar + 2-row tool shelf); Opus renders 2-3 layout widgets in chat for pick. (3) My Things personal library + category-level tracking (Toggl insight); built via three domain stones (Energy/Work/Love), NOT in onboarding. (4) Planner = one-board builder (my-things chips shelf + day stack; kills the Energy→Work→Love beat interrogation; reuses the locked programBuilder day-plan spec).
- *8 first-day convergence points (from the thinker panel):* text to 1/3 + voice-first; onboarding compressed to ~4 beats (questions move into stones, asked when used); day one = one loop lived (delta shown as proof + win named/celebrated + ledger entry + tomorrow's one thing + PM promised = the return trigger); 30s self-image rehearsal in day-one meditation aimed at tomorrow (Maltz); user picks/edits mantra words; primes shrink to one spoken line; one honest resistance line (Jung); hook cut to ONE line + gem moment big/screenshotable. Open call: voice source (one recorded human voice vs TTS; Puddicombe evidence says one human voice, possibly David's).
- *9 canon candidates (Withers/Gabriel/designers/Herbalife):* Want Excavation stone closing on "Define the what. I'll handle the how"; belief momentum meters in Rewire (two bars, reps transfer, honest flare-up); one-rung-up mood routing on the gauge; the Gap panic button (Notice→Stop→Soothe→Redirect→Deposit); charge→release→evidence (app goes quiet about charged intents until proof exists: sigil logic, anti-engagement); grown-vs-bought object registers in the world (earned things unfakeable); named identity levels + weekly re-ignition rally (not a stats report); celebration timing sacred (reward lands <1s after the rep, app-wide); one human voice.
- *Stone grammar v2:* PRIME (one spoken idea, may be a 2-sentence micro-parable: Rohn's parable-then-law = Gabriel's drop) → guided ACTIVE exercise in the tools carousel (voice prompt + timed silence; ONE player everywhere; gspin spinning-rays visual dies) → NAME → PLACE (every stone deposits something real: a thing, a block, a layer) → SEAL. Layers model: practices (breath/meditation/mantra/...) × layers (tapping/soundscape/voice; mudras later) × intention, inside the rite frame (hold opens, seal closes); the stack builder IS the ritual composer.

**VERDICTS RECEIVED (David, later 2026-07-18):** (1) three-pane swipe: KILL, confirmed. (2) Bottom tabs: NOT locked; render three nav models as Opus chat widgets first: A tab bar / B hub-and-spoke (home IS the app, surfaces are places entered and exited from home) / C hub + mini-dock. (3) Home: wants an even friendlier deeper redesign; concepts via Opus widgets. (4) NEW: hold-to-commit gets a full dopamine arc (shake ramp + audio ticks rising, since iOS PWA has NO vibration API, then gold-metallic flash + particle burst + gems fly to counter, reward lands <1s); build as ONE reusable charge component for every hold in the app (commit/claim/seal/charge). (5) NEW: journey gains a "lesson mechanics kit": tiny 10-30s interactive mechanics (WarioWare variety), ONE per stone max, each must EMBODY the lesson (the Scale = ounces-vs-tons; the Tear = excuses ripped, "me" remains; the Sail; weeding; wish-solidifies-into-goal on writing the reason). (6) Guided journaling = the NAME beat formalized: one clever question per stone, chips-first + optional typing, spoken-prompt+silence variant merges journaling with guided audio; mine Johnson/Michael worksheets (in _course/) into question sequences via a cheap agent later.

**NAV CONCEPT ROUND 2 (Fable, later 2026-07-18; first Opus render REJECTED for generic-SaaS style, see memory `chat-widget-style-law`):** nine concepts on the table: (1) the Village: one continuous world, surfaces are PLACES the camera flies between; (2) the Snow Globe: home = living miniature (island diorama + day-ring orbit around it + stone constellations in the sky), tap a region to enter it; (3) the Compass Hold: zero chrome, press-hold the guardian mark anywhere → 4 doors fan out radially (extends the app's signature hold gesture into navigation); (4) the Grimoire: surfaces are pages, nav = bookmark ribbons + page-turn; (5) the Guardian Butler: dialogue-led nav for new users that GRADUATES into shortcut chrome (nav itself matures, teach→mirror law); (6) the Zoom Ladder: altitude = timescale, pinch from block→day→week→island; (7) the Day Spine: the timeline IS the app, journey stones and world live inside/behind it; (8) the Satchel: pull-down drawer of chunky game-piece cards, throw a card to enter; (9) restyled Hub+dock baseline. Fable rec: Snow Globe home + Compass Hold travel (+ Butler for day one). OPUS RENDER RULES: read `_specs/STYLE-ART-IDENTITY.md` + index.html `:root`/game-piece CSS, screenshot the live app first, obey chunky/ink/hard-shadow/berry language; render Snow Globe, Compass, Satchel, Grimoire + restyled baseline as interactive widgets.

**NAV ROUND 2 VERDICTS (David, 2026-07-18 night):** exotic spatial models REJECTED as primary nav ("A tab bar vibe was closest"). Locks: (1) NAV = tab bar as the base, ITERATED in ALTER's language, not replaced. (2) DAY SPINE (#7) = not nav but the PLANNER's aesthetic north star: "oversimplify what a planner looks like", minimalism + space + welcoming; full functionality-fit brainstorm owed. (3) HOME = keep current face (IG-story bars + circle + bottom menu) and ADD to it, not rebuild. (4) Hub (#9) boring/not functional. Keep the outside-box spirit but aim it at DETAILS. Next Opus renders queued: (a) tab-bar iterations: game-piece tabs (chunky pieces, active pops with gold ring), guardian-center bar (home = big glowing mark in the middle), LIVING tabs (each tab carries micro-state: Today = tiny day-progress ring, Journey = glowing stone when lesson ready, World = sprout when claimable; diegetic badges, never red dots), bar-as-ground (cel soil/grass strip the pieces stand on, tinted by time of day); (b) home face v2 = current home + day-ring ORBIT around the big circle (the Snow Globe's best organ grafted: blocks as arcs, now-marker) + guardian contextual line + BIG points + 2-row tool shelf; (c) planner Day-Spine realization mocks (pure minimal vs island-parallax-behind).

**ROUND 3 RENDERED (Opus 2026-07-18, widget `alter_round3_bars_home_planner`):** 4 bar iterations (game-piece / guardian-center / LIVING tabs / bar-as-ground) + home face v2 (day-ring orbit grafted onto the existing Track circle, BIG sparks, guardian line, glowing next-stone chip, 2-row tool shelf, living bottom bar) + 2 Day-Spine planner mocks (pure minimal / island-parallax-behind). Awaiting David's picks. Fable rec: Living tabs fused with game-piece+ground into one bar; home v2 day-ring is the keystone add; planner Day-Spine = pure vs parallax is a device-feel call.

**ROUND 3 VERDICTS + ROUND 4 SPEC (David 2026-07-18 night):** BAR: guardian-center mark = liked visually; fuse with game-piece selection (#1 lift+gold-ring); the other 4 tabs still unsolved, iterate their look. HOME v2: interesting but (a) sparks must return to the GOLD CHIP with ink outline style and be BIGGER, (b) background TOO BRIGHT: home sits on dimmed twilight-dawn, light is the accent (art law), (c) render TEN distinct home iterations (unconsidered directions): 1 Vigil (candle-light hierarchy, everything dim but the lit), 2 Guardian-present (sprite beside/on circle, speech-bubble line), 3 Sky-clock (bg = real time of day, sun/moon arc = the story bar), 4 Ledger (3 tiny evidence lines under circle), 5 Next-three rail (next block / next stone / one tool, nothing else), 6 Diorama strip (slim pixel island at bottom, tools into a satchel), 7 Orbital hero (day-ring thicker w/ block icons, circle smaller), 8 Greeting-card (guardian's line as big hero type, circle below), 9 Split-state (top = day excerpt rows, bottom = practice circle+tools), 10 Altar (circle on cel pedestal + flame, sparks as floating gold sigil, AM/PM bookend chips). Constants for all ten: gold-chip sparks BIG, dim ground, guardian-center bar. PLANNER: minimalism liked but missing time-of-day + features; render a TEN-LEVEL SPECTRUM from pure spine to current v1121: L1 pure spine (no time) · L2 day-part bands (MORNING/AFTERNOON/EVENING zones) · L3 chip start-times · L4 GHOST TIME ★ (hour gutter+lines materialize only while scrolling/dragging, vanish at rest) · L5 sparse ruler (3h ticks, proportional gaps) · L6 thin always-on hour gutter, narrow centered blocks · L7 lanes return (full-width, faint hourlines, week strip) · L8 current-decluttered (current structure, calm dark skin, less chrome) · L9 current + all decorations (charge fill, stripes, pips) · L10 current v1121 verbatim (anchor). Opus renders both ten-sets as widgets (style law applies).

**ROUND 4 RENDERED (Opus 2026-07-18, widgets `alter_home_ten_iterations` + `alter_planner_ten_levels`):** 10 homes (Vigil/Guardian-present/Sky-clock/Ledger/Next-three/Diorama/Orbital/Greeting-card/Split-state/Altar) all on dim ground w/ gold-chip BIG sparks + guardian-center bar; 10 planner levels L1 pure spine → L10 current v1121 (L4 Ghost-time flagged ★). Awaiting David's home shortlist + planner detent pick. Fable rec: home = Vigil light-hierarchy + Ledger evidence lines + Orbital day-ring, fused; planner detent likely L4 (Ghost time) or L5 (sparse ruler).

**ROUND 5 SPEC (Fable 2026-07-18 night; David verdicts: home = SPLIT-STATE (#9) iterate; sparks = the NINE-CONCEPTS style chip (dark translucent, 2px gold BORDER, gold glowing text, bloom) NOT solid gold fill; UNIFY flame+gems into one cluster (same light system); planner = converge ghost-time(maybe) + SUBTLE daypart tint (as current) + thin gutter (L6) + block icons (L7 "missing the emojis" = activity icons); iterate the GUITAR HERO off-plan layer):**
- *Spark counter variants (render 3):* (a) one gold-outline chip: spark icon + count, tiny flame corner-badge carrying streak n; (b) flame IS the icon (flame=spark, one glyph, one number); (c) twin readout in one chip (sparks | kept-days). Constant: BIG, gold outline + glow, dark interior.
- *Split-state home v2 (render ~6, must cover IDLE and TRACKING states since home = the expanded cockpit):* v1 baseline (day rows top: prev dim / NOW hot / next; action row under NOW: Start · Break · Change plan; circle+tools below) · v2 circle-on-the-seam (the now-line runs through the circle; circle IS Start; day above, practice below) · v3 TRACKING state (elapsed ring on circle, Break/Done, next queued) · v4 conveyor (upcoming blocks slide down toward the now-line = mini Guitar Hero on home) · v5 guardian-on-the-seam (line comments the transition: "Gym done. Code in 13 min.") · v6 compact (day zone collapses to now+next bar, expands on tap).
- *GUITAR HERO layer (the off-plan mechanic, ties to pending reconcile engine #15/16):* now-line = the hit line; time flows down; blocks approach it. (1) THE SWEEP: a tracked block fills with light as the now-line passes through it (battery principle animated); untracked passes hollow. (2) THE CATCH WINDOW: block entering ±10min of the line glows/pulses; tap = catch (start tracking, snap + spark burst, audio tick; iOS PWA has no vibration); missed = block gently detaches sideways into an off-plan lane, dim not red, draggable back (bring it back today/tomorrow). (3) OFF-PLAN CHIP: doing something unplanned → a chip rides the now-line: quick-log reality or take a BREAK block (break = first-class rest note, pushes plan forward via reconcile). (4) LAW: reward-never-shame: a miss never reddens or breaks anything, it just does not light; the music keeps playing; kept-count ticks quietly (no fire imagery per streak law).
- *Planner convergence (render 3-4, not another 10):* narrow-ish blocks WITH activity icons + thin hour gutter + subtle daypart tint + ghost-time full grid materializing on drag + the Guitar Hero now-line layer.

**ROUND 5 RENDERED (Opus 2026-07-18, widgets `alter_sparks_and_splitstate_homes` + `alter_planner_guitar_hero`):** spark cluster 3 variants (outline-glow chip, flame folded in: a badge / b flame-is-icon / c twin readout); 6 split-state homes (v1 baseline w/ Start·Break·Change · v2 circle-on-seam ★ · v3 tracking-state · v4 conveyor · v5 guardian-on-seam · v6 compact); Guitar Hero planner = INTERACTIVE toy (catch/pass/sweep/off-plan-lane/off-plan-chip, reward-never-shame). Minor bug in v3 render (leftover "23:speaker" label). Awaiting: spark variant pick, split-state home pick, Guitar Hero lock. Fable rec: sparks (b) or (a); home v2 circle-on-seam + v3 as its tracking face + v5 guardian line; Guitar Hero = LOCK (it IS the reconcile engine #15/16 design).

**ROUND 5 VERDICTS + ROUND 6 SPEC (David 2026-07-18 late):** LOCKED: (1) sparks = gem-only glyph in the gold-outline glow chip, NO flame anywhere in the counter. (2) GUITAR HERO = LOCKED ("very clever, feels super powerful"); off-plan lane moves to the LEFT; iterate the concept further. (3) Home = baseline v1 direction BUT: bottom bar takes too much space (home must feel FULL-SCREEN); tool ROWS (2) move to the bottom; the day zone above gets MORE COLOR = the game-piece planner language we've been designing (colored blocks w/ icons, now-line through); KILL the Start/Break/Change button row (read: it made no sense next to the now-line: the action IS tapping the glowing now-block = the catch; Break/Done appear only in tracking state).
ROUND 6 RENDERS: (A) Home v1.2 × 4 nav treatments, all full-screen (no persistent bar): A1 corner portals (Today bottom-left, World bottom-right, cel round buttons) · A2 fold-away bar (thin handle, swipe-up reveals, auto-hides) · A3 guardian-mark-only bottom-center (tap = talk/actions, HOLD = compass fan travel; David liked the guardian center button visually) · A4 corners + guardian center combined. DESIGN PITCH riding on A-set: "the journey comes to you": Journey stops being a nav destination; stones schedule themselves as BLOCKS in your day + a next-stone chip on home; the journey map opens through the stone/guardian → app reduces to 3 places (Home, Today, World). (B) Guitar Hero v2 × 4: B1 tap-the-block catch (no buttons, ripple + snap) · B2 reconcile feel: pass-uncaught → "bring back?" chip, compare plan-compresses (future slides up) vs times-hold (gap remains) · B3 Break = rest note inserted at the line, future visibly pushed · B4 the day's tape: played/lit blocks stacking below the line as the record. Constants: gem chip, off-plan lane LEFT, reward-never-shame, colored game-piece blocks with icons.

**ROUND 6 RENDERED (Opus 2026-07-18, widgets `alter_home_v12_nav_treatments` + `alter_guitar_hero_v2`):** 4 full-screen home nav treatments (A1 corner portals / A2 fold-away bar / A3 guardian-mark-only tap-talk+hold-travel / A4 corners+guardian ★) all w/ gem-only chip, colorful game-piece day slice, 2 tool rows bottom, NO persistent bar; + the "journey comes to you" pitch (stones = blocks in the day, app collapses to Home/Today/World). Guitar Hero v2: off-plan lane LEFT, B1 tap-the-block no-buttons ★ / B2 reconcile toggle (compress vs hold) / B3 break pushes plan / B4 the day's tape record. Awaiting: home nav pick, GH facet picks. Fable rec: A4; B1 as the verb + B2-hold + B3 + B4-tape all COMPOSE into the one reconcile engine.

**ROUND 6 VERDICTS + ROUND 7 SPEC (David 2026-07-18/19; STYLE DRIFT called out and hardened into memory `chat-widget-style-law`):** REJECTED: A1/A3/A4 (buttons cover content); A2 swipe-up-to-reveal = weird for an important menu; the off-plan SIDE LANE (odd, breaks the real geometry); B1-B4 as presented (read as disjoint functions). CORRECTIONS: gems chip too big (top-left disbalance; fold into a balanced status strip); Start button = redundant (the circle/play IS start) but BREAK and CHANGE stay as real buttons; mockups must reuse the REAL planner geometry (center-split plan-lane/lived-lane per `.plan-empty`, berry colors, 45° stripes = matched, solid dim = drift, real bg) and Opus must screenshot the live app first.
ROUND 7 DIRECTION: (1) HOME = THE JARVIS BRIEF (Iron Man assistant feel): info architecture = small status strip (time · date · phase · sparks small right), guardian's one-line read, NOW (live/catchable block), NEXT + time-until, today's shape (kept-count/story), stone-ready chip. (2) THE SCROLL-SEAM HOME (David's own idea, formalized): home = ONE scrollable surface anchored at the seam; scroll UP = more planner until you're simply IN Today (the Today destination dissolves into home); scroll DOWN = more tools/library; menu bar stays at bottom at rest and COLLAPSES INTO ONE SIDE BUTTON on scroll (the existing app mechanic, kept); render 3 variants (S1 seam+collapsing menu / S2 + JARVIS brief card / S3 denser status strip). (3) PLANNER/GUITAR-HERO in the REAL geometry: catch = planned block at now-line glows, tap → it crosses the spine and the LIVED side fills with striped matched-fill as time passes (the sweep = "matched grows into past" ANIMATED, the existing law); miss = block stays in plan lane past the line, solid dim (drift), with a small bring-back affordance ON the block (no side lane); off-plan = lived-lane entry without a plan twin (tap empty lived side at now = quick-log); Break/Change buttons dock near the now-line while tracking; ONE coherent interactive demo + 2 style-degree variants, not 4 disjoint panels.

**ROUND 7 RENDERED (Opus 2026-07-19, screenshotted live app first per hardened style law; widgets `alter_catch_in_real_planner` + `alter_jarvis_scroll_seam_home`):** REAL geometry confirmed from index.html: timeline = center-split (plan lane left `.plan-empty` left:34px→right:50%+3px / lived lane right), pink now-line `#ff5fa8` + left dot, chunky `.calblk` rounded-15 2.5px ink #160510 border, matched = -45° white stripes + pink-berry rim inset glow (`.calblk.act.onplan` = pink NOT gold per David 2026-06-27), miss = grayscale .42 desaturated (never red), berry/domain block fills, white text #fff2f9. (1) CATCH DEMO in real split timeline: tap glowing now-block → it spans both lanes + fills matched stripes as line sweeps ("matched grows into past" animated); miss → stays plan lane, greys below line, bring-back ON the block (no side lane); Break/Change dock at now-line; Start killed (block=start). (2) JARVIS scroll-seam home: one scrollable surface anchored at seam; brief = status strip (time/date/phase + small sparks) · guardian one-line read · NOW (catchable) · NEXT+time-until · today's-shape (story+kept) · stone chip; scroll UP dissolves into full Today, scroll DOWN into tools; bottom menu (Today·guardian·World) collapses to side pill on scroll. Awaiting David verdict. This round wears the REAL skin.

**ROUND 8 SPEC (Fable 2026-07-19; David: planner MESSY, home INTIMIDATING, iterate both):**
- *Planner diagnosis:* round-7 broke the app's own law (memory `alter-tracker-design-rules`: **future = single-lane; the split is a PAST-only artifact**). Mock put half-width plan blocks in the future + showed every signal at once (stripes+glow+dashed+miss+dock+labels). Planner v2 renders: FUTURE = full-width single lane, matte, calm; the ONE block at the now-line is the only glowing thing; caught → continues into the past as full-span matched stripes; missed → becomes the half-width ghost-plan (left) with empty lived beside it, grey, in the PAST where divergence belongs; hour gutter = ghost-time (scroll/drag only); ONE empty-slot hint max (next gap only); signals budget = max one special treatment per block; Break/Change attach to the live block only while tracking. Render P1 (true-law) + P2 (same, zoomed next-3-hours default).
- *Home diagnosis:* the brief read as a dashboard: 4 ALL-CAPS section labels, 6 bordered boxes, too many words. De-intimidation: KILL section labels (content self-explains); merge guardian line + NOW block into one spoken unit; strip borders from secondary info (whisper hierarchy via size/opacity, not boxes); at REST show only: tiny time strip · guardian sentence · NOW block · circle; next/shape/stone live one small scroll away (trust the seam). Render H1 "three things" (minimal rest state) · H2 "spoken brief" (guardian sentence absorbs next/stone info, one block, circle) · H3 "soft panels" (more info, zero labels/borders).

**ROUND 8 RENDERED (Opus 2026-07-19, widgets `alter_planner_v2_calm` + `alter_home_v2_calm`):** PLANNER fixed to the law: P1 = single-lane matte future, only now-block glows, split appears only in PAST where diverged (Read planned vs errands lived), one gap-hint, ghost-time gutter; P2 = same zoomed to next-3-hours default. HOME de-intimidated: H1 three-things (time·guardian line·now-block·circle, rest one scroll away); H2 spoken-brief ★ (guardian sentence carries next+stone, no labels/boxes); H3 soft-panels (more info, zero labels/borders, hierarchy via size/opacity). Awaiting David's planner + home pick. Fable rec: P1 (or P2 default zoom) + H2 spoken brief.

**ROUND 9 SPEC (Fable 2026-07-19, grounded in `_specs/BOOKS-DEV-CANON-2026-07-18.md`; David: P1/P2 still messy, H3 not great):**
- *Canon diagnosis:* PLANNER messy because every block ran full-saturation domain color at equal weight with glow-inflation (canon law 4: hierarchy by DE-emphasis + 9-level ramps; also grey-text-on-color violations, uniform spacing = no grouping). HOME not great because it has TWO heroes (now-block AND circle = parallel systems, canon law 5: one deep mechanism).
- *TOKEN SYSTEM first (mockups and future `:root` build share it):* 9-level ramp per domain hue + grey ramp with ROLES: future block = level-3 light tint + level-8 hue-matched INK text (no white/grey on color) · NOW = the sole saturated level-6 + the ONLY glow on screen (Octalysis Glowing Choice / 4-second rule) · past matched = level-4 + stripes · drift = grey-ramp level-3. 5-level shadow ladder (rest/hover/held/dragged/modal). Nonlinear spacing scale (4/8/12/20/32): within-group < between-group; blocks cluster by day-part.
- *HOME = ONE HERO SLOT (deep module):* layout = status whisper (tiny time + tiny gems) → guardian sentence (ONLY prose, ≤10 words, halve-twice law) → THE HERO SLOT → whisper footer. Slot contents by state: block-due → the block big · tracking → live block + elapsed · free → practice circle · night → PM card. The non-hero option demotes to a text whisper line, never a second button-hero. No "tap to start" caption (glow+form = the affordance). Render the hero-slot home in its 4 states.
- *PLANNER:* keep P1 geometry (single-lane future, split-only-past) but recolor via the ramp roles; day-part group spacing; ONE gap-hint; ghost-time gutter. Render ramped full-day + zoomed.
- *Canon open decisions still owed David (Part 4):* sound pass greenlight · menu-unification scheduling · stone self-assessment timing · :root scales session.

**ROUND 9 RENDERED (Opus 2026-07-19, widgets `alter_planner_ramped_calm` + `alter_home_hero_slot_states`):** PLANNER recolored via ramp ROLES on dark ground (de-emphasis = recede): future = deep muted hue fill + hue-tinted text + no glow; NOW = full-saturation + the ONLY glow on screen; past matched = mid tint + faint stripes, dimmed; drift = grey split (plan left / lived right), never red; day-parts grouped w/ spacing; ghost-time gutter; one gap-hint. HOME = ONE hero slot, content by state: block-due (block IS hero, circle→whisper) / tracking (elapsed ring, break→whisper) / free (circle hero, plan→whisper) / night (PM close card, rest→whisper); status whisper strip + guardian sentence ≤10 words; no "tap to start" (form+glow = affordance). Awaiting David. Fable rec: lock the ramp-role token system (feeds the `:root` scales the canon wants) + hero-slot home.

**ROUND 9 VERDICTS + ROUND 10 SPEC (David 2026-07-19):** Planner SHAPE system = GOOD ("the way you shaped everything works pretty good"); but the muted recede colors read MUDDY-UGLY (dark gold → brown "deep work", muted blue gym, read): recede must keep HUE PURITY: recipes to render: (a) true hue at low alpha over the berry ground, (b) pastel hue-tint dim, (c) outline-ghost (hue border, near-transparent fill). Bring STRIPES back where earned. HOME hero-slot concept survives BUT the hero must stay THE CIRCLE (square block-due + square night card broke established visuals): the circle's FACE changes by state (block-due: icon+name+time in the circle · tracking: ring+elapsed · free: play · night: moon). Kill the floating white guardian text (random/ugly/off logo): style it warm, attached, smaller. Kill empty space: tools = 2-3 ROWS; add NEXT strip; more to click.
- *HOME first-principles decision matrix (what home must serve):* idle-with-plan → start-next (hero), see-next, quick tool, replan · tracking → continue (zero-tap), pause/break, done, see-next · idle-no-plan → plan-day, quick tool, ad-hoc track · evening → close. Derived layout: status whisper · guardian line (styled, ≤10 words) · CIRCLE hero (state faces) · NEXT strip (1-2 upcoming + stone) · tools grid 2-3 rows · ONE contextual plan/replan chip. Nothing else.
- *PLANNER nav decision (constitution warning: never two day-nav models):* David proposes side-swipe = yesterday/tomorrow + smooth pinch-zoom day→week→month. CONFLICT: current law = continuous vertical scroll across days (regression contract #1, rebuilt 3×). Options: (A REC) keep vertical-continuous + ADD pinch-zoom ladder day→week→month, no horizontal swipe · (B) switch to horizontal day-pages + vertical-within-day (big regression rebuild). David must choose before any build spec.
- *ROUND 10 RENDERS (Opus):* 10 planner iterations (vary ONE dimension per cell: 3 recede recipes × stripe usage × gutter/day-part styles × now-treatments + a zoom-out week-view teaser) + 10 home iterations (circle faces × what fills the middle: NEXT strip / orbit ring / mini-timeline sliver / stone chip × tool-grid densities 2-3 rows × guardian-line stylings × contextual action chips). Small-phone grid, compact, so David can point at cells.

**ROUND 10 RENDERED (Opus 2026-07-19, widgets `alter_planner_ten_looks` + `alter_home_ten_looks`):** PLANNER 10: P1 hue-low-alpha ★ / P2 outline-ghost / P3 pastel-dim / P4 stripes-forward (now fills w/ stripes live) / P5 day-part zones / P6 no-gutter airy / P7 now-dominant / P8 sky-tinted ground / P9 pinch-to-week / P10 pinch-to-month heat map. HOME 10 (circle ALWAYS the hero, faces by state): H1 block-face+next+2rows / H2 orbit-ring / H3 timeline-sliver / H4 dense-3rows / H5 tracking-ring-face / H6 progress-whisper / H7 free-play-face / H8 action-chips(replan/whole-day) / H9 upcoming-rail / H10 night-moon-face. Recede recipes now hue-pure (no mud). Awaiting David's planner-look + home-look picks + the nav decision (A vertical-continuous+pinch-zoom vs B horizontal-pages). Fable rec: planner P1 (or P2) + P9/P10 as the pinch-zoom ladder (nav option A); home = compose H1 base + H5 tracking face + H7 free face + H10 night face (state machine) + H8 chips when idle.

**ROUND 10 PLANNER VERDICTS (David 2026-07-19):** P1 hue-low-alpha = WINNER (colors/visuals liked). P2/P3 = rejected. P4 stripes = liked BUT undecided stripes-in-NOW vs stripes-in-matched-PAST (render both to decide). P5 day-part zones = KEEP (good organization). TIME = written on the LEFT, friendly + minimalist (NOT faded/ghost-time; he doesn't want time "relaxed"/hidden). P8 sky-tint = good concept but TOO BRIGHT → render DARKER, near original berry twilight. P9 week = too tall/stretched/overwhelming → use CURRENT planner's week-view SIZE (compact, don't radically change). P10 month = rejected as "not what a calendar looks like" BUT likes per-day-different-color idea → render as a REAL calendar grid (weeks as rows), per-day color density, NOT evenly-spaced abstract grid.
**KEY REQUEST — visualize the NUANCED NOW system in the minimalist planner:** a block STRADDLING the now-line (past/tracked portion = matched stripes · future portion = matte P1-hue · now-line cutting through, glowing), the partial-match case (matched span + drift remainder), "matched grows into past" as the block fills live. This is the real app's convbar/straddle behavior (`.calblk.convbar .convmatch`, "future matte · past ghost · tracked matched") ported to the minimal P1 look.
ROUND 11 RENDERS: (1) the nuanced-now straddle + partial-match, in P1 colors + friendly left time + day-part zones, with stripes-in-now vs stripes-in-matched side-by-side. (2) P8-darker sky-tint + compact week (current size) + real-calendar-grid month with per-day color.

**ROUND 11 RENDERED (Opus 2026-07-19, widgets `alter_nuanced_now_minimal` + `alter_planner_darkskytint_week_month`):** (1) NUANCED-NOW straddle shown in P1 minimal look: block caught on now-line = lived-half matched stripes (top) + future-half matte (bottom), stripe line rises with real time = "matched grows into past" in ONE block; partial-match = lived portion striped, remainder greys to drift; friendly WRITTEN left time gutter (12/noon, 1/pm), day-part zones. Stripes A-vs-B rendered: A=stripes in NOW/active (Fable+Opus lean, "living energy"), B=stripes in matched-past permanent mark. (2) darker sky-tint ground (deep twilight-lilac→night, P8 idea minus brightness); compact week at current size (not stretched); month = REAL calendar grid (weekday cols, weeks as rows) with per-day color density + today ringed. Awaiting David final planner picks. THIS likely LOCKS the planner visual language.

**ROUND 11 VERDICTS + ROUND 12 SPEC (David 2026-07-19):**
- *Today:* GOOD except NOW label + time gutter UNREADABLE (bigger/contrast). NEW LOCKED IDEA: **the SCANNER now-line**: a subtle glow of light rising just ABOVE the now-line (pink→transparent gradient, slow shimmer) implying the now is PRINTING the day. Sky-tint STILL too bright → darker again. WEEK: too short, must show NOW, match current in-app week height. MONTH: cells too tall → match current in-app calendar cell proportions.
- *Home verdicts:* H1 = base (2 tool rows LIKED + "Code is up next" top line LIKED); tools become horizontally SWIPEABLE pages (stay 2 rows) with a PEEKING half-tile edge affordance + page dots + last tile = "All tools"→toolbox. CIRCLE: NO TEXT EVER: giant centered symbol only (play / activity icon / moon); name+time move to the line above/below. COLUMN RHYTHM LAW (from H7 vs H8): the circle's width = the chips'/buttons' width below = one centered column; nothing sticks out sideways. H5 tracking: blend WITH established circle (thin edge progress ring on the classic pink circle, elapsed below); Break/Done NOT neon (berry-palette soft chips, column width). H6 whisper: unclear, label evidence lines if used. H7 good. H8 good but fix proportions per column law. H2/H3/H9 rejected; H4 3-rows too many; H10 night pretty but add options (close chip + journal + rest tools row).
- *NAV / APP MAP (the mom-test question):* David wants to SEE the fold-away bar (bottom bar → side pill on scroll, tap to restore). Maps to render: A) 3 tabs (Home·Today·World) + pill, Journey via stones, Toolbox via all-tools tile, Goals inside Journey · B REC) 4 labeled tabs (Home·Today·Journey·World), fold-away behavior, Journey tab hosts GOALS + chronicle, mom-proof labels · C) 3 tabs + "More" sheet (Journey/Toolbox/Goals/Settings as labeled grid). Day→week→month: REC = tiny D·W·M segment top-right of Today (minimal, mom-proof) + pinch as shortcut; alternatives: tap-title-cycles, zoom-out button.
- *GOALS = un-designed surface, flagged for its OWN design round* ("the goal emulator"): pitch = goals as few big minimalist cards (the want · the reason · who-it-makes-you (Rohn) · linked blocks/stones), goal generates blocks+stones; lives in Journey tab (Map B).
- *ROUND 12 RENDERS (Opus):* (1) fold-away bar interactive demo + the 3 map options; (2) Today v3 (scanner glow, readable time/NOW, darker sky, D·W·M segment) + week at real height w/ now + month w/ app-proportion cells; (3) Home v3 composed (no-text circle, column rhythm, swipeable 2-row tools w/ peek, berry tracking face blend, upgraded night).

**ROUND 12 RENDERED (Opus 2026-07-19, widgets `alter_today_v3_scanner` + `alter_home_v3_composed` + `alter_nav_foldaway_and_maps`):** TODAY v3 = SCANNER now-line (pink glow rising ABOVE the line = present printing the day, shimmer anim) + readable time gutter + NOW chip + darker sky + D·W·M segment top-right; week at real height w/ now-line across 7 days; month = calendar-proportion square cells + per-day color + today ring. HOME v3 composed on 3 laws: circle = SYMBOL ONLY (name/time in the line above), ONE column (circle width = chips/buttons width), tools = 2 rows w/ peeking half-tile + page dots (swipeable, last tile=All tools/toolbox); tracking = classic pink circle + thin EDGE ring + elapsed inside, Break/Done soft berry chips (not neon); night = moon circle + close chip + wind-down tools row. NAV = fold-away bar interactive (bar at rest → guardian side-pill on scroll → tap restores) + 3 maps: A 3-tab minimal / B REC 4 labeled tabs (Journey hosts GOALS+chronicle, mom-proof) / C 3+More sheet. Awaiting David picks (scanner lock, home v3 lock, nav map). GOALS still owed its own design round. Likely the design-spine LOCK point.

**ROUND 12 VERDICTS + ROUND 13 SPEC (David 2026-07-19):**
- *Today = AWESOME (scanner LOCKED).* Header spacing bug: D·W·M segment crowds the gems; HEADER MUST BE CONSTANT across D/W/M (scope switch can't reflow the top bar). Fix: fixed 3-zone header (title left · D·W·M center · gems right), same on all three scopes.
- *Week:* drop the now-line + time rail; week = SEE THE WHOLE DAY of each of 7 days AT A GLANCE (full-day columns, not time-cropped). Showing time was a bad idea for week (David).
- *Month:* good but pinned to top w/ dead space; render options: (a) vertical scroll of MULTIPLE months stacked (continuous, David's lean-ish), (b) single month fills the view, (c) swipe month-to-month. David asking which is better: stacked-scroll vs one-at-a-time.
- *HOME — the big idea (LOCKED direction): the SWIPEABLE-CIRCLE home = the player mechanic as the whole home.* Circle SMALLER; above it the established IG-story bar (colored segments + emoji/icons showing where you are in the day); SWIPE left/right moves through big circles, one per activity (prev/now/next...), so you PREVIEW the whole day without committing to track. Tap = start that one. Same swipe+tap+side-peek feel as the current player. Tools move BELOW and are reached by scrolling DOWN (vertical), not side-swipe (side-swipe now = day preview). 
- *Tool palette FIX:* several tool tile colors ugly (the 3-dots one, a muted ugly green, muted blue-green bottom-right, the red). Rebuild tool tiles on a curated harmonious subset of the domain ramps (no muddy mid-tones); each tool = clean domain family; render a corrected tool-tile palette board.
- *Break/Done colors ugly:* re-pick from berry palette (soft, non-neon, harmonious with the pink circle; likely muted-rose "break" + soft-green-that-isnt-ugly / or gold "done").
- *NAV: Map B LOCKED (Home·Today·Journey·World).* Fold-away bar liked BUT the corner pill = too bright/invasive/ugly. Render subtler restore affordances: (a) tiny dim guardian dot, (b) a thin edge-tab/handle, (c) bar peeks a sliver, (d) just re-show on scroll-up (no persistent button at all). David lean: subtle.
- *ROUND 13 RENDERS (Opus):* (1) Today constant-header (D/W/M same top bar) + whole-day week + month scroll-stack vs one-at-a-time; (2) the swipeable-circle home (IG-story bar + smaller circle + side-swipe day preview + scroll-down tools) as an interactive demo; (3) corrected tool-tile palette board + new Break/Done chips; (4) 4 subtle fold-away restore affordances.

**ROUND 13 RENDERED (Opus 2026-07-19, widgets `alter_today_week_month_v4` + `alter_home_swipeable_circles` + `alter_tool_palette_and_nav_polish`):** TODAY = constant 3-zone header (title·D·W·M·gems, same all scopes); WEEK = whole-day columns (7 day-shapes at a glance, no time-crop/now-line); MONTH = stacked-scroll (multi-month continuous) vs one-at-a-time (fills view) — David to pick. HOME = swipeable-circle carousel (LOCKED direction, interactive): IG-story bar + icons on top, smaller circle, side-swipe scrubs day (prev/now/next big circles, preview-without-commit), tap=start, tools moved to scroll-DOWN. Tool tiles rebuilt clean domain colors (mud gone); Break/Done = soft rose + warm gold (non-neon). Fold-away restore affordances: A dim-dot / B edge-handle / C peeking-sliver / D nothing-scroll-up-returns ★. Awaiting: month choice, home-swipe lock, palette approve, restore choice. Fable rec: month stacked-scroll · home swipe LOCK · palette yes · restore D (or B).

**ROUND 13 VERDICTS + ROUND 14 SPEC (David 2026-07-19, WITH 2 REAL SCREENSHOTS — real facts now in memory `chat-widget-style-law`):**
- *Today header GOOD* but FONT wrong (used Jost; real app = rounded Baloo/Fredoka). ALL mocks must switch to rounded bubbly font.
- *Week whole-day = TOO TALL/overwhelming; "This week" font bad.* Make week columns SHORTER/calmer, rounded font.
- *Month STACKED = GOOD (locked direction)*; colors slightly too bright → tone down. Month ONE-AT-A-TIME = BAD/didn't fit → DROP.
- *HOME-as-player, corrections (ref screenshot 1 = the REAL cockpit):* NO big side-circles (not how player works); show ONE big detailed circle image-1 style (big pink, ink ring, drop shadow, big play-triangle), swipe replaces it (no visible side peeks). Circle currently TOO SMALL + lacks detail → match cockpit fidelity. Text/info goes BELOW circle, not above. KILL "start now" label (play IS start). The CONTEXT bar above must be COLORED (like onboarding/intro-stone story bar, always colored). Likely keep the real cockpit's 3 buttons below (track now / plan+track-next striped / plan day) per screenshot 1.
- *PLANNER block-select → the REAL SIDE MENU (ref screenshot 2):* right panel slides in on block tap: domain header, start −/+ steppers, length chips 30/60/90, color dots, Ещё/More, delete. Reuse this exact pattern (it exists) in the minimalist planner.
- *TOOL tile colors: bright ones TOO BRIGHT, muddy ones closer in brightness but wrong hue → MID-TONE berry-compatible* (match screenshot medium-sat domain colors). Re-render palette at correct tone.
- *FOLD-AWAY: all 4 (dot/handle/sliver/nothing) rejected or unsure. David REF = Apple Music: a subtle persistent button in the BOTTOM-LEFT corner (mini-menu/now-playing style).* Render an Apple-Music-style corner menu button + how tapping it expands to full nav.
- *ROUND 14 RENDERS (Opus):* (1) rounded-font Today header + shorter whole-day week + toned-down stacked month (drop one-at-a-time); (2) HOME v5 = big cockpit-fidelity circle (image-1 look) + colored context bar + info/buttons BELOW + swipe-replaces-circle (no side peeks) + scroll-down tools; (3) planner block-tap SIDE-MENU (image-2 pattern) in minimal planner; (4) mid-tone tool palette + Apple-Music-corner-button nav.

**BLOCK EDITOR verdict (David 2026-07-19):** the screenshot-2 side panel (30/60/90 length chips + color dots) = NOT real functions (you don't pick color; duration isn't capped — could be 30s or 2h). David's idea: keep the side-panel FORMAT (cleaner) but pour in the REAL `editorSheet` functions. Grounded from app.js `editorSheet` (@ ~8931): name=SWITCH-activity (ti-switch-horizontal), START −/+ 5-min nudge (start+end always shown), free-length duration scrubber (drag, any length), PRIO 3-level (low/med/high, `o.prio` PRIOS), pin, sub-steps (ti-puzzle, `o.subs`), mark-done (only once started, not future), delete, auto-save. NO color-picker, NO 30/60/90. RENDERED `alter_planner_real_side_editor` (Baloo font, mid-tone, block gold-ring selected + side panel w/ real functions). Awaiting David lock.

**ROUND 14b (David 2026-07-19): side-panel editor RETRACTED (too crammed).** Editor stays BOTTOM-SHEET but redesigned MINIMAL (current one too busy: colors/shapes, not minimal enough). Rendered `alter_editor_minimal_bottom`: E1 airy-list / E2 time-hero-scrubber / E3 progressive(more-hidden), all Baloo font, one accent, real functions (switch/start±/free-length/prio/pin/steps/done/delete). TOOLS-ON-HOME = open question (player purity vs access): rendered `alter_home_v5_tools_balance` w/ cockpit-fidelity BIG circle (pink + thick ink ring + drop shadow + play-triangle, per screenshot 1) + colored context bar on top + info below + 4 tool options: T1 two balanced mid-tone rows always / T2 hidden behind Tools button (purest) / T3 one row + swipe-up / T4 pure player (tools in own tab). Colors now mid-tone (not neon). Awaiting: editor pick (E1/E2/E3), tools pick (T1-T4). STILL QUEUED (deferred to conserve budget this long session): shorter whole-day week, toned stacked month, mid-tone palette board, Apple-Music-corner nav button, GOALS design round.

**ROUND 15 SPEC — THE HOME STATE MACHINE (Fable 2026-07-19; David defined the core decision matrix; editor = E1 locked visually):**
*Design laws (canon: one deep mechanism + position constancy):* the CIRCLE is always the primary verb; the action row below holds ≤3 contextual verbs; same verb always same position (break/pause left · switch/replan middle · done/extend right); 100ms feedback; one glowing choice.
*THE STATES (each = a home face, same skeleton):*
- **S0 EMPTY** (no plan, not tracking): circle=play, tap=quick-start picker (My Things chips → track ad-hoc). Row = the REAL cockpit trio (screenshot 1): Track now (pink) · Plan+track next (striped blue) · Plan day (dashed). Tools MOST prevalent here (2 rows).
- **S1 PLANNED-IDLE**: circle=play; line below: next block + countdown. Row: Track now · Replan. 
- **S2 BLOCK-DUE (the catch)**: circle wears the activity icon + glows/pulses; tap = start. 
- **S3 TRACKING**: the established progress ring wraps the pink circle, elapsed shown. Row: Pause(=Break) · Switch · Done. Tools recede (1 row or behind button) but stay one tap for interleave.
- **S4 ENDING-SOON / NEXT-DUE**: row gains EXTEND (one tap, +15m or drag) beside Done; "next: X, start?" chip appears. 
- **S5 GAP-RETURN (forgot to track)**: guardian asks ONE friendly line ("It's been 2h since Lunch. What were you up to?") → quick backfill chips (My Things + "just life") → fills the lived lane retroactively. Reward-never-shame.
*THE INTERLEAVE LAW (tool during tracking):* ONE live track at a time. Tapping a tool mid-activity AUTO-PAUSES the activity (recorded as a break-for-practice), tracks the tool session, then offers one-tap "back to [activity]?". The ledger records the nesting honestly.
*Circle fidelity fix:* match screenshot-1 exactly: bright pink fill w/ subtle radial highlight + lighter inner rim, OUTER dark-berry ring (= the track the progress ring fills), soft drop shadow. Current mock too dark/flat. Tool tile colors: SAMPLE THE REAL APP (grep the actual home/tool tile CSS in index.html), stop approximating.
*E1 length-tap:* map-drag stays the PRIMARY length control (editor = subtle adjustment, per David's start-time logic); tapping Length expands INLINE into the real durationSheet chips (5/15/30/45/60/90/120) + a fine drag scrub. No separate screen.
*NAV render:* Apple-Music-style corner button bottom-left (dim guardian mark, rounded-square) → tap expands the 4-tab bar (Home·Today·Journey·World); compare vs a tiny always-on unlabeled mini-bar. David finalizes.
*OPUS RENDERS (round 15):* (1) the 6-state home board (S0-S5), true circle fidelity, real cockpit trio in S0, real tool colors; (2) interleave 3-beat storyboard (tracking → breathe → resume offer); (3) E1 length inline-expand; (4) corner-button nav collapsed/expanded + mini-bar alternate.

**ROUND 15 RENDERED (Opus 2026-07-19, REAL CSS sampled from index.html/app.js; widgets `alter_home_six_states` + `alter_interleave_and_nav`):** REAL VALUES locked: circle = `radial-gradient(circle at 36% 30%,#ff9ec6 0%,#ff5fa8 58%,#c23578 100%)` + 6px `#3a2540` ring (idle = dashed, tracking = the ring the conic/progress fills); TOOL TILES = `mixHex(col,#160510,0.80)` = near-black bg + bright colored icon (why "muddy" read closer); tool icon colors #48d0e0/#9a5cf0/#ff4fa0/#ff8a1e/#ffd24a etc. SIX-STATE HOME board S0-S5 rendered w/ true circle + dark tiles + real cockpit trio (S0). Position law held (Break left · Switch/Extend mid · Done right). INTERLEAVE 3-beat storyboard (track → tool auto-pauses activity, tracks tool honestly → "back to X" resume at saved elapsed). NAV: Apple-Music corner button collapsed → tap raises full 4-tab bar; + tiny mini-bar alternate. Awaiting David: state-board approval + nav pick (corner-button vs mini-bar). This is the CORE-LOOP cockpit spec — once locked, the app's central mechanic is designed.

**ROUND 15 VERDICTS + ROUND 16 SPEC (David 2026-07-19):**
- *CIRCLE STILL WRONG (recurring): too 3D/glossy.* My `radial-gradient(...#c23578)` dark edge makes it look 3D. REAL circle = FLAT-ish bright pink `#ff5fa8` fill + only a SUBTLE top-left highlight; the dimensionality comes ONLY from the thick dark ink RING + soft drop shadow, NOT a heavy pink→purple gradient. NEXT SESSION MUST screenshot the real cockpit circle (get past start screen: Start → onboarding or load save) before re-rendering it; stop approximating.
- *S0 EMPTY:* KILL "Track something now" button (the play circle IS that). Keep Plan+track-next + Plan-day. RE-ADD the 2 tool rows below.
- *S1 PLANNED-IDLE:* KILL "Track now" button (redundant w/ play). Just Replan (+ tools).
- *S2 catch = GOOD.* 
- *S3 TRACKING:* circle looks worse (same 3D bug); Break/Switch/Done button COLORS bad (rose/gold clash) — re-pick berry-harmonious; the minimal tool row "doesn't make sense" — rethink.
- *S5 GAP-RETURN:* concept good but NO "?" symbol inside circle (looks bad); redesign the visual (guardian asks, chips backfill) without a ? in the circle.
- *NAV:* corner button collapsed = BAD (too big, hate yellow, hate symbol). Full bar = fine BUT drop the yellow top-line + probably ICONS-ONLY (no text). Mini-bar pill = emojis too small. **NEW DIRECTION:** corner element = MINIMAL "now-playing"-style indicator showing the CURRENT tab only (like Apple Music mini), tap to expand full bar. Small, represents where you are.
- *SYMBOLS:* use the app's actual Tabler icons, not approximations.

**★ THE BREAKTHROUGH — INTERLEAVE = THE MODULAR LAYER SYSTEM (unifies ritual-composer + activity-tracking into ONE system):** David: mid-activity actions are THREE distinct types, not one:
  1. **BACKGROUND LAYER (always-on add-on):** attach guided breathing / soundscape / mudra / tapping to ANY activity as a running layer (exactly the ritual-composer "layers" from early this session: practices × LAYERS). Doesn't pause the activity; runs alongside. e.g. "Deep work + background box-breathing."
  2. **MICRO-INTERRUPT (non-blocking, earns points):** a tiny scheduled nudge like "get up and stretch every 14 min" — so short it doesn't impede or split the tracked activity, but still earns points. Reminder layer on top of the activity.
  3. **REAL BREAK (blocking, splits + logs):** an actual 5-min break — pauses the activity, logs the break, splits the tracked activity in half (the S3 Pause=Break path). 
  → This means the ACTIVITY is a composable object like a ritual: base activity + layers (background practice / micro-reminder) + break events. The `composeMeditationSegs`/stack-layer engine and the tracker are the SAME grammar. HUGE unification; deserves its own design round. Memory link: [[alter-charging-primitive]] layers, `_specs/SPEC-RITUAL-ENGINE`.

**SESSION STATUS (2026-07-19):** ~15 render rounds deep, very long session, deep in token budget. DESIGNED + near-locked: planner P1+scanner+nuanced-now, block editor (E1 bottom-sheet minimal), month stacked-scroll, week whole-day (shorter), 6-state home loop (needs circle fix + button-color fix), interleave (now expanded to modular layers), nav (needs minimal now-playing corner). OWED FRESH SESSIONS: (a) circle-fidelity fix via real screenshot; (b) the modular-layer/interleave design round; (c) GOALS ("goal emulator") design round; (d) deferred polish: mid-tone palette board, week/month color tone. RECOMMEND: bank here, resume on Opus starting with a real-cockpit screenshot.

**ROUND 16 — FINAL DESIGN SPEC (Fable 2026-07-19; David: finish ALL core redesigns this session; approximation ok, identical comes in the build):**
- **THE LAYER SYSTEM (locked design): THE CHANNEL MODEL.** Channels: HANDS · EYES · VOICE · BODY · EARS (+breath, always free). Every activity/tool declares which channels it OCCUPIES (deep work = hands+eyes; walking = body; mantra = voice; gratitude/meditation = mind only). Every layer declares which it NEEDS (tapping/mudra = hands; breathing = breath only; soundscape = ears). **A layer is offered only if its needed channels are free.** So: deep work → breathing yes, tapping NO ("your hands are busy"); mantra/gratitude → tapping YES. The layer tray auto-greys incompatible layers with guardian microcopy. THREE mid-activity types: (1) background layer (compatible, runs alongside, small bonus on completion), (2) micro-interrupt (stand+stretch every N min: brief, loggable, earns sparks, does NOT split the activity; plannable beforehand), (3) real break (pauses, splits, logs). PLAN-AHEAD: E1 editor gains one "Layers ›" row → inline chips ([+ Breathing] [+ Soundscape] [+ Stretch every 15m], compatible-only).
- **STATE FIXES (final):** flat circle (bright #ff5fa8 flat fill + subtle top-left highlight ONLY + thick #3a2540 ink ring + soft shadow; NO dark-edge gradient). S0: play circle + Plan+track-next + Plan-day + 2 tool rows (no Track-now button). S1: play + next-countdown + Replan only. S3: dark-berry chips (plum #3a1a30 bg, colored icon+label) for Break·Switch·Done, positions fixed. S5: no ?-in-circle; guardian bubble asks, backfill chips below, circle stays a dim play.
- **NAV (final direction):** corner = MINIMAL now-playing pill (small, dim, shows current tab icon+name only, Apple-Music mini style, NO yellow, NO guardian-mark button); tap → icons-only 4-tab bar (no labels, no yellow top-line). Auto-folds on scroll.
- **GOALS (the goal emulator, designed):** lives in JOURNEY tab (Map B). Max 3 active goals ("this season"). THE GOAL CARD: the want in the user's words · the WHY (Rohn: reasons first; four motivators as chips + custom) · "who this makes you" line · attached practice = My-Things + weekly dose (e.g. Deep work ×3/wk) · progress = EVIDENCE COUNT (kept blocks), never a % bar. Creation = 3 beats: name the want → the reason → attach practice. Goals feed the planner (suggested blocks) + the guardian's contextual lines. JOURNEY TAB layout: stone path (top) · season goals (mid) · chronicle (bottom).
- **OPUS FINAL RENDERS (round 16):** (1) Layer-system board (channel model + compatibility examples + 3 types + editor Layers row); (2) fixed states strip S0/S1/S3/S5; (3) nav final (now-playing pill collapsed + icons-only bar expanded); (4) Goals board (Journey tab + goal card + 3-beat creation).
- **Already fully specced, no re-render needed (build specs exist in this handoff):** hold-to-commit dopamine arc (round-6 spec: shake ramp + audio ticks + gold flash + burst + fly-to-counter, one reusable component); scanner now-line; nuanced-now straddle; E1 editor + inline length chips; month stacked / week whole-day (shorter + toned).

**ROUND 16 RENDERED — CORE REDESIGN COMPLETE (Opus 2026-07-19, widgets `alter_layer_channel_model` + `alter_states_fixed_and_nav_final` + `alter_goals_emulator`):**
- LAYER CHANNEL MODEL (interactive): activity occupies channels {hands·eyes·voice·ears·body·breath-always-free}; layer offered only if its needed channels free → deep work greys tapping ("hands busy"), keeps breathing; mantra greys breathing, allows tapping; gratitude allows all. 3 types (background / micro-interrupt / real-break). = ritual-composer ⊕ tracker unified.
- STATES FIXED: FLAT circle (radial `#ffb2d6→#ff5fa8 46%` subtle highlight only + 6px `#3a2540` ring + soft shadow, NO dark edge). S0 = play + Plan+next + Plan-day + 2 tool rows (no Track-now). S1 = play + countdown + Replan. S3 = ring + flat circle + Break·Switch·Done as calm PLUM chips (`#3a1a30` bg, colored icon) + "+layer" compatible tools. S5 = dim play (NO ?) + guardian bubble + backfill chips.
- NAV FINAL: now-playing PILL collapsed (small, shows current tab icon+name, no yellow/no guardian-mark) → tap → icons-only 4-tab bar (no labels, no yellow line), folds on scroll.
- GOALS EMULATOR: Journey tab (Path·Goals·Chronicle); max 3 season goals; card = want(user words) + why(Rohn motivator chips) + who-it-makes-you + attached practice+weekly dose + EVIDENCE-COUNT (kept blocks, no %); creation = 3 beats (want → why → attach practice+dose → "plant the goal"). Feeds planner suggestions + guardian lines.

**ROUND 17 RENDERED (Opus 2026-07-19; widgets `alter_goal_engine_woop` + `alter_states_v2_press_stop` + `alter_layer_doors_and_nav_context`):** GOAL ENGINE = WOOP 5-beat (Domain→Wish→Outcome→Obstacle★→If-Then-plan+practice+dose) + paid-AI-interview tier + goal card v2 (obstacle + if-then + "beaten Nx" evidence) + Journey Path shows NEXT-stone-with-why. STATES v2: S0 berry buttons; S1 tappable day-bar + Replan/Plan-tomorrow; S3 = activity-face circle, elapsed under title, PRESS-CIRCLE→break/finish sheet, row=Switch·Add-layer; S5 = fill-gap (+full picker) AND reshape-future. LAYERS = 3 doorways (block-editor Layers row / in-session + tray / stack-builder nesting), incompatible = INVISIBLE not greyed. NAV pill shown in-context (rest=pill, tap=icons bar). Design phase re-confirmed complete w/ these refinements folded in.

**ROUND 17 SPEC (Fable 2026-07-19; David's round-16 verdicts):**
- *LAYER INTEGRATION (fixed):* NO standalone layer menu, incompatible layers are INVISIBLE (never greyed). Layers surface at exactly THREE existing points: (1) the STACK BUILDER (already how you compose a stack), (2) the BLOCK EDITOR "Layers ›" row when planning a block, (3) IN-SESSION: a small "+" in the player chrome opens the compatible-only tray mid-session. The player is the home of layers.
- *S0:* buttons still too neon → dark-berry chips (plum bg, colored icon+label, stripes kept but mid-tone berry-blue).
- *S1:* more than Replan: the colored CONTEXT BAR is tappable = "see my day" (name that affordance); row = Replan + (evening: Plan tomorrow); play = start-next-early.
- *S3 TRACKING (interaction redesign):* time-in-circle-middle = WRONG (not the established visual): circle wears the ACTIVITY (big icon face, real .tf-tile style), title below, elapsed small under title. **PRESS THE CIRCLE mid-session = the stop gesture → 2-option sheet: "Take a break / Finish" (+cancel).** Break+Done collapse into the circle press; row below = Switch · +Layer only. No red-tinted buttons; berry palette.
- *S5 GAP-RETURN v2 (more nuanced + connected):* two zones: FILL THE GAP (recent My-Things chips + "More…" full picker + "just life" + skip) AND "Reshape what's left?" → Replan-rest-of-day chip. Backfill never blocks replanning the future.
- *NAV:* render the pill IN CONTEXT (full home with pill bottom-left; expanded icons-bar in place).
- *★ GOAL ENGINE v2 (WOOP + the KB; goals = a key app pillar: track, plan into calendar, whole thing):* creation = 5 beats: (1) DOMAIN first (the Big 3 = Energy/Work/Love, the app's own canon categories), (2) WISH (user words + domain example chips), (3) OUTCOME (one vivid who-it-makes-you line: Maltz self-image + Rohn what-it-makes-of-you), (4) **OBSTACLE (the inner obstacle: "what usually stops you?" chips + custom: mental contrasting, WOOP's proven differentiator)**, (5) IF-THEN PLAN (implementation intention "If [obstacle], then [tiny action]" + attach practice + weekly dose → blocks feed the planner). FREE = this flow; PAID = the guardian AI interviews you conversationally (Want-Excavation + BYO-AI VIP tier). Goal card v2 adds the obstacle + if-then line + "obstacle beaten N× this week" evidence. Weekly review = goals reviewed at the week-seal/Harvest. Journey-tab Path section must JUSTIFY itself: show the NEXT stone + one line of why, not a decorative dot row. Fix off colors (yellowish white, odd green).
- *OPUS RENDERS (round 17):* (1) WOOP goal creation 5-beat flow + paid-AI teaser + goal card v2 + justified Journey tab; (2) layer entry points (block-editor row / in-player "+" / stack-builder, invisible-not-greyed); (3) S0/S1/S3/S5 v2 (activity-face circle, press-circle stop sheet, berry buttons, nuanced gap screen); (4) nav pill + bar in full-home context.

## ★★ BUILD ROADMAP (David 2026-07-19: "build the other stuff we agreed upon"; GOAL ENGINE DEFERRED — build later, fully specced round-17 above) ★★
**Principle:** INCREMENTAL, never a rebuild (constitution: no 4th rebuild; update in place, leave regions cleaner). Each = ONE Opus session, spec-first (10 lines), verify boots, DEVICE-UNTESTED honesty on all gesture/feel. Order = low-risk/high-leverage first, regression zones (@SEC:TIMELINE, nav) LAST.

- **BUILD 1 — HOLD-TO-COMMIT dopamine component (START HERE).** Contained, reusable, high-delight, ~zero regression risk; validates the build loop after the design marathon. Build one `holdToCharge(el, {onComplete, secs})` helper: shake-ramp (transform jitter easing up) + rising Web-Audio tick train (NO vibration API on iOS PWA) + at threshold gold-metallic flash + particle burst + gems-fly-to-counter (<1s). Wire into ONE existing hold first (the seal or claim), verify, then it's the shared primitive for catch/seal/claim/charge. Regression: none (additive helper).
- **BUILD 2 — Block editor → minimal bottom-sheet (E1).** Restyle existing `editorSheet` (@~8931) to the E1 airy-list look (Baloo, hairline rows, one accent, real functions unchanged: switch/start±/free-length/prio3/pin/steps/done/delete). Add the inline "Length" expand (durationSheet chips inline). Add a "Layers ›" row (gated, but layer LOGIC can stub until BUILD 6). Regression: contained (editor is a sheet); verify stack + planner still open it.
- **BUILD 3 — Tool tiles + tool colors polish.** Confirm real `mixHex(col,#160510,.80)` tiles read right; fix any off ones; berry-harmonize. Trivial CSS. Regression: none.
- **BUILD 4 — HOME cockpit states S0/S1 + flat circle.** Restyle the existing `renderTrackerFull` idle/`tf-home` face (@SEC:COCKPIT ~1546): flat circle (no 3D gradient), colored context bar, S0 (empty: play+Plan-next+Plan-day+2 tool rows) / S1 (planned-idle: tappable day-bar + Replan/Plan-tomorrow). Regression: idle cockpit only; do NOT touch live-tracking render yet.
- **BUILD 5 — TRACKING states S3/S4/S5.** Activity-face circle + press-circle→break/finish sheet; S4 ending-soon Extend + next chip; S5 gap-return (fill-gap picker + reshape-future). Touches live tracking logic. Regression: MEDIUM (tracking render); spec-first, one pass.
- **BUILD 6 — LAYER CHANNEL MODEL.** Data: activities/tools declare occupied channels; layers declare needed; `compatibleLayers(activity)` pure helper. 3 doorways (editor row from BUILD 2, in-session "+" tray, stack-builder nesting). 3 types (background/micro-interrupt/real-break). Regression: touches player + editor + stack; new system, careful.
- **BUILD 7 — INTERLEAVE.** Tool-mid-activity = auto-pause activity (log break-for-practice) → track tool → "back to X" resume at saved elapsed. Depends on BUILD 5 + 6. One-live-track invariant.
- **BUILD 8 — PLANNER visual: scanner + nuanced-now + constant header + P1 recede.** @SEC:TIMELINE REGRESSION ZONE. Scanner glow above now-line, straddle-block (lived-stripes/future-matte), P1 hue-low-alpha recede, 3-zone constant header, D·W·M segment, whole-day week, stacked month. Spec-first, boot-verify ONLY, gesture DEVICE-UNTESTED. Do NOT combine with nav.
- **BUILD 9 — NAV: Map B + pill fold-away.** REGRESSION ZONE (kills 3-pane swipe, supersedes three-pane-spine). Now-playing pill → icons-only bar; 4 tabs Home·Today·Journey·World; Journey hosts Goals+Chronicle. Own session, most disruptive, last.
- **DEFERRED (own sessions): GOAL ENGINE (WOOP, round-17 spec) · journey stones 2-8 content (Fable) · first-day-v3 flow wiring.**
**Recommended first session: BUILD 1** (hold-to-commit) — safest, most delightful, gives David a felt win + a reusable primitive. Then 2→3→4 as contained confidence-builders before the medium/regression work.

## ★★ DESIGN PHASE COMPLETE — BUILD-READY INVENTORY (all Opus builds, spec-first, one region/session) ★★
1. NAV: Map B (Home·Today·Journey·World), now-playing pill → icons-only fold-away bar. Kills 3-pane swipe (supersedes three-pane-spine lock).
2. HOME = idle cockpit state machine S0-S5, flat circle hero, colored context bar, tools context-gated, action row ≤3 fixed-position verbs.
3. TODAY/PLANNER: P1 hue-low-alpha recede, SCANNER now-line (glow-above = present printing), nuanced-now straddle (lived-stripes/future-matte, partial-match), constant 3-zone header, D·W·M segment. WEEK = whole-day columns (shorter). MONTH = stacked-scroll calendar cells + per-day color.
4. BLOCK EDITOR = minimal BOTTOM-SHEET (E1 airy, David liked; E2 time-hero alt), real editorSheet functions (switch/start±5min/free-length scrub/prio3/pin/steps/done/delete), inline length chips; NO color-picker/no 30-60-90.
5. LAYER SYSTEM (channel model) on activities+tools; unifies with ritual composer.
6. GOALS emulator in Journey tab.
7. INTERLEAVE (auto-pause activity for tool, resume offer).
8. HOLD-TO-COMMIT dopamine arc (round-6 spec): shake-ramp + rising audio ticks (NO vibration API on iOS PWA) + gold-metallic flash + particle burst + gems fly-to-counter, <1s reward; ONE reusable component for every hold.
9. TOOL TILES = `mixHex(col,#160510,.80)` dark bg + bright icon (already real).
STILL PURE-DESIGN-OWED (small): mid-tone palette board final; the deep journey-stone content session (stones 2-8, own Fable session); first-day-v3 flow wiring. 
NEXT SESSION: pick ONE build target from 1-9, Opus, spec-first (10 lines), verify boots, DEVICE-UNTESTED honesty on all gesture/feel.
BUSINESS BIRTH LIST (unchanged, parallel track): Stripe rails+license key, landing+science page, 4-persona device tests, content lane (David: accounts + first Sanctuary clip). FIRST-DAY-V3 spec build · nav tab-bar swap · home widgets → picked concept build · My Things + domain stones · one-board planner · Arrange-your-day programBuilder mode (spec at top of this file, still build-ready) · AND the business birth list unchanged: Stripe rails + license key, landing + science page, four-persona tests, content lane starts (David, zero tokens: accounts + first Sanctuary clip).

## UPDATE 2026-07-17 (cont.) — Arrange-your-day = the stack-planner block editor BUILT + SHIPPED (v1121, Opus, worktree-checked)
Built the spec below. The "Arrange your day" step now IS the stack planner (`programBuilder`), per David's clarification (kill the ⠿ dots + length chips; chunky blocks, drag-resize, hold-reorder, tap-open; min visible thickness + tap-for-custom-shorter).
- **How:** generalized `programBuilder` with SIX additive, `isMin`-gated knobs (`unit:'min'`, day-plan `blockH` scale, item-aware `look(k,item)`, `onAdd`/`onSwap` via `bentoPicker`, `showPrio`, `customLength`). `orderStep` shrank to ~15 lines: builds a track (`{k, d:mins, act, prio}`) and calls `programBuilder` in day-plan `pick` mode; commit reuses pick-mode → `onPick` → `distributePlan(k, out)` (order + mins + prio preserved). Removed `LEN_CHIPS` + the whole old row DOM/gesture/swipe-delete code (net −47 lines; wipe count DROPPED to 149).
- **Min-thickness rule:** `MIN_H 44px` floor; `≤15min` → 44px; above `44 + (mins−15)×1.6` cap 220. Drag-resize snaps 15min floored at 15; the tap-open **Length stepper** (±5) goes below 15 (block holds at 44px, stores the real minutes).
- **Panel (tap a block):** custom-length stepper + importance pips (prio moved here, OFF the bubble, per David) + Swap + Delete.
- **NO SCHEMA bump.** Ratchet passes (wipes 149, anchors 19/19, SCHEMA 5). Zero console errors.
- **Verified in preview:** day-plan editor renders as the stack planner; stepper → custom 5m holds at the 44px floor + stores 5m; 30m=68px; add-via-bento works; commit placed 3 blocks back-to-back with the 5m preserved. **The STACK editor + medEditor are unregressed** (seconds scale, Save/Play footer — all new branches skipped when `unit!=='min'`).
- **DEVICE-UNTESTED (preview lies about gestures):** drag-bottom resize FEEL + press-hold reorder FEEL on the day-plan blocks. Confirm on phone.
- **TRADEOFF (flag for David):** the v1118 **swipe-to-delete is GONE from the arranger** — the merge to programBuilder means delete = tap-block → Delete (exactly like the stack planner). This is truer to "exactly like the stack planner," but if you want swipe-delete back on top, it's a follow-up (adds a 4th gesture to programBuilder's attach handler — deliberately deferred). Dead CSS from the old editor (`.editrow`, `.edit-*`, `.bento-orderlist`) left in index.html (harmless; not touched to avoid entangling with the game-visuals chat).

## BUILD SPEC (BUILT v1121 — kept for reference) — Arrange-your-day = the stack-planner block editor (David 2026-07-17)
David clarified after v1117: the "Arrange your day" step (`orderStep`) should look + behave EXACTLY like the stack planner (`programBuilder`), NOT the row-with-chips design. Kill the ⠿ grip dots and the length chips along the bubble width ("not aesthetic"). Minimalist chunky blocks: height = duration, DRAG the bottom edge to resize, PRESS-HOLD to reorder, TAP to open. A minimum visible thickness so short blocks stay clickable; to go shorter than that, tap → custom length (bubble never renders thinner than the floor). **Both decisions LOCKED (David 2026-07-17) → spec is build-ready.**
- **Approach (recommended): route `orderStep` through `programBuilder` in a new "day-plan mode" — don't fork the gesture code.** programBuilder already IS the aesthetic/gesture model (chunky blocks, `blockH` 46px floor, drag-bottom resize, hold-reorder, tap-expand panel, add-tray, swap/delete) and is already cfg-driven (`pick`/`onPick`/`lookup`/`pool`/`addDur`/`playLabel`). Add SIX additive, cfg-GATED knobs (existing stack+meditation callers pass none → unchanged):
  1. `cfg.unit:'min'` — minutes not seconds → drives `mins()`, resize snap (15-min), total readout.
  2. `cfg.heightScale` — day durations 15–120min need a gentle scale + cap (not 16px/min → a 2hr block = 1400px).
  3. `cfg.lookup` receives the ITEM (day blocks carry own `{title,icon,color,domain}`, no pool identity) — fall back to `stackTool(k)`.
  4. `cfg.onAdd`/`cfg.onSwap` — "add a block"/"swap" open `bentoPicker`, not the tool pool tray.
  5. `cfg.showPrio` — importance pips INSIDE the tap-open panel, OFF the collapsed bubble.
  6. `cfg.customLength` — tap-open panel length control allows SUB-minimum durations.
  - Commit path = reuse `pick:true` AS-IS: one CTA "Add N to today" → `cfg.onPick(track)` → `distributePlan(k, track)`. `distributePlan`/`reflow`/now-line NEVER entered.
- **Block model (the min-thickness rule):** `MIN_H≈44px` floor (always tappable); `MIN_VIS≈15min` maps to MIN_H (anything ≤15min renders at MIN_H); above: `height = MIN_H + (mins−15)×~1.6px` capped ≈220px (30m≈68 / 60m≈116 / 120m≈212). Drag-resize snaps 15min, floored at MIN_VIS (can't drag thinner than the floor). To set <15min: tap → custom-length control → stores real mins, renders at MIN_H, distributes at the real (short) length.
- **Interactions** (all inherited from programBuilder): resize = drag bottom hairline (the ONLY affordance, no ⠿ dots); reorder = press-hold body; open = tap → panel (custom length + importance-if-kept + delete); keep the v1118 swipe-to-delete as the fast path.
- **Risk:** programBuilder is SHARED (stack + `medEditor`) — every knob cfg-gated with existing behavior as default; verify the stack builder + meditation editor render/drag IDENTICALLY after. `orderStep`'s only caller is `shapeFlow`'s final beat (small seam). Gesture FEEL device-only; preview CAN verify heights/min-floor/add-swap-delete/custom-length-stores-sub-15.
- **DECISIONS LOCKED (David 2026-07-17):** (1) Importance/prio → **KEEP, moved into the tap-open panel** (off the collapsed bubble; `distributePlan` still gets prio). (2) Height numbers **CONFIRMED as proposed** (MIN_H 44 / MIN_VIS 15min / 1.6px-min / cap 220) — still allow ONE on-device tuning pass after it renders on the phone.

## UPDATE 2026-07-17 (cont.) — #14 TIMELINE adjacency snap + arrange swipe-to-delete shipped (v1118, Opus, regression-zone care)
The next item from David's timeline notes (msg: "slide an activity right up next to an activity so they're touching before it forces you to reorder... right now it just leaves a gap"). Root cause found: the future-block MOVE (`calendarView` `mv2`, @SEC:TIMELINE ~8616) snapped `dragMin` to a 15-min grid → couldn't touch a neighbour ending at e.g. 2:37, and pushing further triggered overlap→reorder. `reflow` only ever pushes blocks LATER, never pulls to close a gap.
- **Adjacency magnet:** new PURE helper `adjacentSnap(s, dur, edges, floor, ceil)` right after `reflow`. In `mv2`, after the 15-grid `dragMin`, if the block's start is within 10 min of a neighbour's END (sit flush after) or its end within 10 min of a neighbour's START (sit flush before), it overrides to the EXACT flush start-minute (one soft haptic on catch, latched). Neighbour edges frozen at drag-start (`_edges`). Result re-clamped to `[_floor,_ceil]` → **regression contract #2 holds: a future block still cannot cross the now-line** (a flush position past the floor is rejected → null → coarse grid stands).
- **Swipe-to-delete** in `orderStep` (the "Arrange your day" / day-planner arranger): `swipeDelete(row,i)` — horizontal swipe past 40% width removes the row; vertical reorder stays on the ⠿ grip, length/importance taps unaffected (`.editrow.gp{touch-action:pan-y}`). Fixed the "Add N to today" count going stale after a delete (`syncGo`). Hint copy updated (Gate 1 clean).
- **NO SCHEMA bump.** `reflow`, cross-day re-home, now-line, week-strip untouched. Ratchet passes (wipes 150≤150, anchors 19/19, SCHEMA 5). Boots clean, zero console errors.
- **Verified in preview:** `DEV.adjSnap` unit-tested 8 cases exact (flush-after from both grid sides → 2:37; flush-before; >threshold → null; **floor-clamp → null**; closest-edge wins). Swipe-delete driven via synthetic pointer events: row spliced, count 3→2, length-tap + reorder-handle NOT hijacked. 
- **DEVICE-UNTESTED (preview lies about gestures):** the magnet CATCH feel during a real finger-drag, the swipe-delete throw feel, and that the magnet threshold (10 min) feels right on-device. Confirm on phone.
- **NOT DONE this pass (David also named these in the same note):** (1) swipe-delete in the **stack** arranger (`programBuilder`) — it already has an explicit Delete button, and adding a 4th gesture to its resize/reorder/tap handler is riskier; deferred, not blocking. (2) The **break button + reconcile engine** (#15/#16: break-as-tracked-activity that pushes the plan forward, off-plan tolerance, orphaned-block "bring it back today/tomorrow" suggestion) — the biggest piece, still its own focused session on the now-line invariant.

## UPDATE 2026-07-17 — PLANNER list-and-restyle shipped (v1117, contained, Fable→Opus finish)
The plan-day picker's "Day taking shape" strip was an abstract colored-cell rail (David's complaint: not a readable list of the things). Fixed + game-piece pass, all additive, engine untouched:
- **Readable chosen-list** (`pd-list`/`pd-pick` in `bentoPicker.pdSyncStrip`, `@SEC:` near `shapeFlow`): every pick so far now renders as a mini game-piece chip ABOVE the dry-run rail. This beat's picks are tappable to unpick; earlier beats ride dimmed (`.prev`, Back edits them). Earlier-beat picks now flow in via a new `opts.accPicks` from `shapeFlow`'s `runBeat`/`runElse` so the strip never lies across beats. The abstract cells stay as the placement dry-run below.
- **Game-piece pick UI:** chosen chips in the plan sheet get the pink game-piece select ring (`.bento-card.bento-plan .bchip.sel`), and the **Arrange step** rows (`orderStep`) are restyled as game pieces — 45° canon stripe fill per domain, ink text/controls, activity icon replaces the color dot (`.editrow.gp`). Matches the `programBuilder` language.
- **NO SCHEMA bump** (reads picks + blocks; `distributePlan` commit path untouched). Ratchet passes (wipes 150≤150, anchors 19/19, SCHEMA 5). Copy Gate 1 clean; one em-dash removed from the arrange hint.
- **Verified in preview** (taps are authoritative here): boots clean, zero console errors, full beat-walk drives, unpick works, earlier-beat picks ride as dimmed chips, arrange rows render as striped game pieces, plan commits correctly (Outdoor gym 16:31 + Claude code 17:01 back-to-back from now).
- **DEVICE-UNTESTED (preview lies about gestures):** the `pd-list` horizontal scroll vs the pane-swipe arbitration, the Arrange-step drag-reorder feel on the new striped rows, and the tap-to-unpick feel. Confirm on phone. Timeline/now-line regression zone NOT entered.

## UPDATE 2026-07-13 — §10f.7 HOME built (Opus, regression-zone) — the idle cockpit IS the app's landing
Built the LOCKED "Refined C2" home (§10f.7 in `_specs/HANDOFF-stacks-and-meditation.md`, David ✓). The idle cockpit is now the app's front door.
- **The face (`renderHomeFace`/`renderHomeBars`/`homeStory`, near `renderTFControls` @SEC:COCKPIT):** story bars on top (today's titled blocks, domain-colored, filled where lived — reuses the v1040 actBars visual) → "What now?" greeting → the big pink **Track CIRCLE** (CSS `.tf-home.st-idle`: pink fill, 7px `#2a1730` ring, DARK drop-shadow below, outline play, inside the dim dashed cockpit ring) → the plan sub-line chip → **"Plan my day"** dashed ghost → a tinted **tools side-scroll** (`STACK_TOOLS`, most-worn floated front via `S.tools.use`, scroll-snap squares, page dots). The circle = `playFirst` (Track); a tool tap = `runStack([{k,d}])`; Plan = `shapeFlow`.
- **How it lands:** `renderTrackerFull`'s `idle` branch now adds `tf-home` + calls `renderHomeFace` (supersedes the old 3-door §12 frame). Boot opens the cockpit via `openHomeInstant()` (no-morph, behind the z-200 start screen) for a set-up user with nothing tracking; new users (onboarding) and mid-activity/claim/night states are untouched. Sliding it down = the existing `closeTrackerFull` morph → reveals the panes/nav.
- **Additive only, NO SCHEMA bump** (reads `blocks`/`S.tools`). Ratchet passes (wipes 150≤150, anchors 19/19, SCHEMA 5). Boots clean, no console errors; home renders (8 bars, 7 tools, circle, plan) in preview.
- **DEVICE-UNTESTED (preview lies about gestures):** the slide-down morph feel, tools horizontal scroll vs the pane-swipe arbitration, and the boot-landing feel on a real returning user. Confirm on phone.
- **FLAG for David:** (1) idle now ALWAYS shows the home face everywhere the idle cockpit opens (unified, per the lock), not just at boot. (2) The old idle **docked next-sheet** (one-tap "<block> — how long?" duration-commit) is retired by the locked layout — the circle + plan sub-line replace it; say if you want that one-tap-plan wired onto the "Focus block · now" chip.

## UPDATE 2026-07-12 — PM CLOSE completed (v1028, Opus, cockpit-only) — the plan→live→close→plan-better loop now closes
The PM close already existed as a 6-beat cockpit stage (win → learn → rating → tomorrow(WOOP) → install → deal, `@SEC:COCKPIT` `pmBeatWin`/`pmBeatTomorrow`/`exitStage('pm')`). This session added the three pieces it was missing:
1. **ONE honest observation** of the day's charge — `pmObservation(mr)`, PURE from `bookendMirror`, deterministic by day-shape (all-kept / partial / drift / unplanned-but-moved / quiet), reward-never-shame, rendered in the WIN beat. Six lines gated Gate 1 + Gate 2; the one Gate-2-killed line (naked domain stat, "most hours went to your body") logged as a KILLED anchor in COPY-ANCHORS.md.
2. **Carry-forward** — the TOMORROW beat now lists today's undone blocks (`mr.missBlocks`) as tap-chips → existing `pmCarryToTomorrow(b)` safe path (blocks(tomK()).push + reflow, de-duped). Count rides into the event.
3. **THE CLEAN PLAN-AHEAD EVENT (load-bearing for next session)** — `S.plannedAhead[tomK()] = { plannedOn: todayK(), ts, oneThing, carried }`, written UNCONDITIONALLY on PM-close commit (`pmRecordPlanAhead`, called from the `exitStage('pm')` commit branch). Completing the close IS the plan-ahead action. Unambiguous: keyed by the TARGET date; `plannedOn < targetKey` = planned before the day arrived. **The next-session "Planned Ahead" hero streak reads THIS map** — do not re-derive from bk/blocks. Additive state, NO SCHEMA bump (S.tools/S.bk/S.dayClose precedent; defaulted in load() base defaults).
- Verified in preview: boots clean, no console errors, full beat-walk drives, observation renders, carry chip writes + confirms, `S.plannedAhead["2026-07-13"]={plannedOn:"2026-07-12",oneThing,carried:1}` recorded, dayClose recorded. **Flow feel is DEVICE-UNTESTED — gesture/tap feel confirm on phone.** Timeline render untouched (regression zone not entered).
## UPDATE 2026-07-12 (cont.) — THE TWO-STREAK SYSTEM built (v1029, same session, Opus, cockpit-only)
Built immediately after the PM close (David: "keep going"). FOUNDATION-PITCH "STREAK DECISION" delivered.
- **Engine (pure, read-only, `@SEC:COCKPIT` near the pm helpers):** `paDaysPlanned()` (days the user did a valid plan-ahead), `streakAhead()` → `{n, status: lit|ready|repairable|cold}`, `streakFollow()` → `{n}`, `heroProjectedClose()` (the count tonight's close will reach, since the event commits on "Rest now"). Shared `_streakWalk` allows ONE bridged gap = the make-up window (repairing IS the rep); `S.away` softens 'cold'→'repairable' (rest held). NO new stored state, NO SCHEMA change. `DEV.streaks()` inspects it.
- **HERO "Planned Ahead"** surfaces ONLY in the guardian's existing voice: the PM deal footer (projected count: "N evenings running now, planned before the day arrives...") and the AM greeting's GENERIC slot (never clobbers the identity/one-thing greetings above it; shows 'ready'/'repairable'/'cold' variants).
- **QUIET "Followed Through"** = one companion line in the deal footer at n≥2 only. Never its own surface (honors "never a 5-counter stats page").
- **Copy:** all Gate 1 + Gate 2. Judge KILLED every fire/chain-metaphor line ("chain reconnects", "went cold", "lights it again") for FORCED ANALOGY + streak-shame; rewrites are plain + effortlessly-repairable ("tonight you just pick it back up"). New streak-copy LAW logged in COPY-ANCHORS: no fire/chain imagery, a slip reads neutral, never a dead chain to rescue.
- **Verified in preview:** engine exact (ready n=3, repairable n=2, cold n=2 across seeded histories), deal footer shows projected count 4, no console errors. **AM greeting line is time-gated (`phase()==='morning'`) → its rendered display is DEVICE-UNTESTED; the engine it reads is verified.** Timeline render untouched.
- **NOT DAVID-RATED yet:** none of the PM-close or streak copy is an EPIC anchor until David verdicts it on his phone.

## NEXT (open)
- Get David's verdict on the PM close + streak voice on-device (rate lines → COPY-ANCHORS).
- Possible: a subtle VISUAL treatment for the hero streak if David wants one (kept to voice-only this session per the "no stats page" law — a visual badge would be additive, options-first).
- The de-game subtraction pass (FOUNDATION-PITCH §3) is still its own scoped session.

## STATE RIGHT NOW
- **Live: v976** on the phone (https://dmekibel.github.io/alter/fresh.html). Boots clean, verified.
- **Constraint:** NO Fable access this session; running **Opus on max**. Regression-zone ENGINE logic is DEFERRED — only safe content/data slices were shipped on Opus.
- Session was mostly research + voice + copy-deepening. The day-one FLOW was NOT restructured; the VOICE at key beats was.

## THE BIG DECISION LOCKED THIS SESSION — the voice
ALTER's iconic voice = **Sage, written by Chris Gabriel** (David's favorite channel, MemeAnalysis; he also wrote the tarot in `magic/`). ONE voice that flexes by **MOMENT, not user**:
- **TEACH moment** (a real mechanism to reveal, user is reading) → the concrete "why" drop.
- **TOUCH moment** (user is doing, or raw/tired) → one short warm line.
- **The bar: concrete or it dies.** Real noun, real mechanism, no vague lines, no sweeping "everyone who…" claims, no tidy "it was never X" reversals. Half the earlier AI-generated samples were mush; this bar kills them.
- Wired as SCRIPT-ENGINE's PRIMARY voice layer. Full doc: `_specs/VOICE-SAGE-BY-CHRIS-GABRIEL-2026-07-08.md` (read its "LOCKED MODEL" section).

## COPY AUDIT — the locked voice, now enforced AUTOMATICALLY (new this session)
The "concrete or it dies" bar is a GATE now, not a hope. Copy kept failing all session because the author graded his own work, and an author passes his own slop. Two INDEPENDENT gates every user-facing line must pass before David sees it (hard rule added to `alter/CLAUDE.md`):
1. **Gate 1, deterministic:** `python3 _dev/copy-audit.py "the line"` (or `--file lines.txt`). Regex kills dashes, emoji, the negation-contrast cadence, slop/hype words, coercive CTA grammar, and the "no idea underneath" family (grand-abstract superlatives, vague quantifiers, clinical jargon). Exits nonzero on a hard tell so it can gate.
2. **Gate 2, adversarial judge:** spawn a FRESH cheap-model agent told ONLY to KILL each line against the craft rubric (moment-not-category, one-idea-underneath, mechanism-as-feeling, no-cheerleading, could-this-be-any-app). Rewrite or cut what it kills. Prompt template in SCRIPT-ENGINE "THE AUDIT".
Proven live: the two gates caught every vague/cheerleading line the author had passed by eye, including "it was never X" reversals and "everyone feels it" sweeps — the exact tells the locked Chris-Gabriel bar names. So Gate 1+2 = that bar, automated. When new slop slips through, add its pattern to `copy-audit.py` or the kill-shot list. SCRIPT-ENGINE also gained Part 1.5 (THE CRAFT: fieldguide's 10 techniques + balance's value-bomb test) and Part 4.5 (competitor writing lessons).

## THE CONTENT MODEL — 3 voices, one journey
- **Brian Johnson** = the spine (meaning/virtue/areté; already the 8-chapter arc).
- **Cal Newport** = the hands (concrete systems). Mined: `_specs/CAL-NEWPORT-SYSTEMS-PLAIN-2026-07-08.md` + 5 layer docs.
- **Daniel Barada** = the fire (identity / "who you're becoming" / boring consistency). Mined: `_specs/DANIEL-BARADA-FIRE-LAYER-2026-07-08.md`.
- Owed content voices (mine focused, never wholesale): Therapy in a Nutshell (emotional safety), School of Life (meaning), Huberman (mechanisms). Style seasoning: Kurzgesagt/CGP/Nerdwriter (`_specs/ALTER-VOICE-BENDING-KIT-2026-07-08.md`).

## READ FIRST
- Memory: `alter-wisdom-roster`, `alter-cal-newport-mine`, `alter-script-engine`, `no-emojis-tabler-icons-only`, `no-em-dashes`.
- Roadmap: `_specs/BUILD-ROADMAP-EPIC-2026-07-08.md` (the wave order + model routing).

## SHIPPED (all voice/copy, engine untouched)
- **W1 v974** — PM close lesson (`DAY1_LESSONS.fd4`): the browser-tabs "why" + win-or-learn, em-dashes gone.
- **W2 v975** — AM fuse-loop: the `aimedAhead` greeting ("You named this last night, before the day could argue") + one-thing lesson.
- **W3 v976 (SLICE)** — the 8 `JP_CHAPTERS` `why` subtitles reshaped to the 3-voice ladder. Johnson titles kept. **The progression engine (`advanceGate`/`journeyNode`/milestone `done()`) was DELIBERATELY NOT TOUCHED — that is the regression zone.**
- **Earlier today (v968–v973, this session's first half: depth + tools + first-day):** Plan-Tomorrow adaptive intention surfaced next morning (v968); attention-adaptive ritual silences via `pauseFor` (v969); Breathe = 4 real protocols picker `breathPicker` sigh/box/4-7-8/resonance (v970); NEW true-PMR "Tense and Release" `muscleRelease` + Grateful-Flow rebuilt on mental-subtraction, tap-only (v971); Tapping/EFT gains the real SUDS rate→re-rate→adaptive-rounds mechanism (v972); Stone-1 `DAY1_LESSONS.fd0` rebuilt to PRIME→DO→NAME→CHECK→PLACE→SEAL + `fd0b` landing (v973). Tool-roster audit: MOST tools FAITHFUL, don't churn (memory `alter-tool-depth-pass`).

## NEXT WORK — pick up here
1. **TOOL UI CONSISTENCY (David flagged, OPEN).** Tools do NOT all use the beautiful unified carousel (`runStackCarousel`, ~app.js:9166). Single tools (breathwork, tapping, gratitude, meditation) fall back to their own older card UIs. CTA buttons are a mishmash: `▶` (many tools), `▸` (38×, onboarding/lessons), `→` (several), and an actual **emoji `go deeper ✨` at ~app.js:4117** (violates the hard no-emoji rule; already has a ti-sparkles icon). TWO fixes: (a) QUICK mechanical sweep — standardize all CTAs to `<i class="ti ti-arrow-right">`, kill the ▶ ▸ → ✨ glyphs; (b) REAL build — route single tools through `runStackCarousel` so they all move like the carousel.
2. **W3 DEEP** — reshape the long chapter `idea` teachings (`JP_LESSON`, ~app.js:1869) + em-dash sweep in the locked voice. The progression engine stays a regression zone: spec first, do not rewire on Opus without a tight spec.
3. **W4** charging redesign (flat-then-jump, boring-consistency = the rep) · **W5** copy pass across existing surfaces · **W6** emotional-safety layer (needs Therapy mine) · **W7** identity-evidence · **W8** depth-on-demand.
4. **Prep:** topic-sample mines Therapy/SoL/Huberman; Cal transcript top-up (104 pending, `fieldguide/…/Cal_Newport/PENDING_MISSING.txt`, needs IP cooldown).
5. **Bigger open question:** day-one STRUCTURAL redesign (vs just voice) — see `_specs/FEEDBACK-FIRST-DAY-2026-07-06.md`. David asked "what do I feel as day one redesigned" and the honest answer was "voice at 4 beats, not a new flow." If he wants the flow itself redesigned, spec that.
6. **FIRST DAY v2 — THE LOCKED DESIGN (David 2026-07-08 evening, Fable; SUPERSEDES the older item-6 text below and the transparent-game intro shipped in v980).** Memory: `alter-first-day-zero-context` (the seven laws). Build on OPUS:
   - **User model:** stressed stranger, ZERO context, from a TikTok ad. Day-one register = DIRECT Duolingo-teacher clarity (the Chris-Gabriel literary register mis-fires on day one; it earns its place later in the journey). Gate-2's day-one rubric: kill for needs-a-reread / fancy / vague / cheesy / FALSE SCIENCE / assumed context.
   - **Day one = one continuous argument:** the onboarding spark line is sentence one; every screen after is the next logical sentence. No tone restarts.
   - **Onboarding adds two early beats:** AGE (chips) + experience CHECKLIST ("Which of these have you tried?" meditation/journaling/therapy/breathwork/none, multi-select). These set the explanation dose app-wide. "None" reply: "None yet? Good, nothing to unlearn. I'll start at the beginning."
   - **Stone 1 rebuild (replace the v980 transparent-game intro slides, which David killed as cheesy):** bridge line → the ASK ("Do you have thirty seconds? That's enough to try a scientifically proven way to calm your body down.") → SCIENCE-SANDWICH stack: before-line + exercise + after-line. Stack = muscle release + breath ONLY (mantra REMOVED from stone 1, returns as a later stone once its science-before earns it). Gated copy (both gates passed, science verified): squeeze/release contrast line; exhale→vagus line; "same nerve doctors use to calm a racing heart" after-line. The recap keeps the tension pre→post + "you just ran the whole app: plan, do, track."
   - **Johnson rooting:** day one = Module O oversimplified (Flip the Switch = the 60-sec body switch; Initiate & Celebrate = the pact stone; eulogy-WHY later, lighter). `_course/COURSE-MAP.md` = spine. The journey = the 300-day course, cleverly oversimplified.
   - (Older, still-valid pieces from the first pass: yes-ladder as the flow's spine; guardian-as-daimon line 1 David approved for LATER, non-day-one use; drop the "use me less" thesis; universal entry, never app-hopper framing.)

   *Prior item-6 text (historical):* STONE-1 OPENER copy (drafted this session, needs a final pass through the LOCKED Chris-Gabriel voice + both audit gates before wiring). Explored as the guardian-AS-daimon (Brian's daimon = ALTER's guardian) fused with the areté-gap and closed with a Cialdini yes-ladder. David: line 1 "powerful," keep. Current 3-line draft (passed Gate 1 only):
   1. "I am the part of you that already knows who you are capable of being. You have heard me clearly on your best days. Then the noise comes back, and I get hard to hear." (LOCKED, David approved)
   2. "That distance closes one small rep at a time, never in a single leap. Every January proves it." (REWRITE: "never in a single leap" is the tidy reversal the locked bar bans.)
   3. "So here is all I am asking today. Sixty seconds. One minute, once, and you owe me nothing after. That is a yes you can afford to make." (the yes-ladder close.)
   **Greenlit principle:** the **yes-ladder** (Cialdini commitment-and-consistency, foot-in-the-door) is the FIRST-DAY FLOW's spine — each step earns the next affordable yes so the user invests one small yes at a time. Fold into the flow build, not just this line.
   **Also from this session (David verdicts):** DROP the "app wants you to use it less / graduation-as-thesis" line from the opener (he does not vibe with it). Anchor on UNIVERSAL struggle (the New Year's resolution, the gap everyone feels), NOT a jaded "you have tried other apps" framing. Tune HARD into Brian Johnson (the main textbook / his course in `_course/`); the guardian speaks AS the daimon, not a coach describing it.

## HOW TO WORK HERE (from the constitution)
- **Ship loop:** `bash _dev/preship.sh` → `git add app.js index.html server.js` (NAMED files, NEVER `-A` — research artifacts stay local) → commit (end message with the Co-Authored-By line) → push → hand David `/fresh.html`.
- **Copy:** load `_specs/SCRIPT-ENGINE.md` + run its procedure. Concrete or it dies. No em-dashes, no emojis (Tabler `ti-*` only).
- **Navigate app.js by grep `@SEC:` / `@CONTRACT`, never line numbers.** ~11,200 lines.
- **Regression zones** (`@SEC:TIMELINE`, `@SEC:JOURNEY-ENGINE`, `@SEC:JOURNEY-TRAIL`): spec first (10 lines), one-shot, verify boots. **Preview lies about gestures** — the only honest report is "boots clean; gesture/feel DEVICE-UNTESTED, confirm on phone."
- **Model routing:** contained builds → Opus; regression-zone/multi-region → Fable when available. State the call before building.

---
## 2026-07-09 — SPEC-FIRST-STONE-BUILD **Phase A shipped (v981, Opus)**
Spec: `_specs/SPEC-FIRST-STONE-BUILD-2026-07-08.md`. Onboarding is now the **blueprint read** the day-one stone adapts from.
- **Added 3 tapped survey questions** in `@SEC:ONBOARD` (`onboardV2` `QS`): **age** (band → `P.age`, was orphaned to unread `P.ageBand`), **experience** (meditation/journaling/therapy/breathwork/"None yet" → `P.experience`), **challenges** ("Where do you want the most help?" — 6 rows, each a short label + a specific-moment sub → `P.challenges`).
- **Occupation** captured WITHOUT a redundant screen: `finishV2` maps the existing life-shape answer → `P.occ` (an OCCUPATIONS key; powers `activeCats` + char sheet + AI context).
- **Blueprint reader** `blueprint()` added next to `activeCats` — the single normalized read the stone branches on (`everMeditated`, `practiceNovice`, `wantsFocus/Sleep/...`). Phase C/D consume this to baby-step meditation for novices etc.
- **Biomes dropped** (David 2026-07-09: "don't like the biome idea for onboarding"): removed the region-gate transition beats + the per-section screen tint (`data-sec=""`). Questions flow in the established clean-card style. The battery progress bar + section labels stay (subtle, not the biome drama).
- Copy: both gates passed. Gate 2 KILLED the first challenges options as generic-category beats; reframed to label+specific-sub (the `vibe`-question pattern) and re-passed.
- **Verified in preview** (all taps, no gestures → preview is authoritative here): boots clean, zero console errors through the full flow, all 3 questions render, `finishV2` stores `age/occ/experience/challenges` correctly.
- **NEXT = Phase B (carousel opening), FABLE — regression-zone gesture/carousel work:** Tab-0 typewriter text mode, the "30s to try this?" offer, the 30/60/120 + skip chooser, press-hold-to-commit morphing into the player orb. Do NOT do Phase B on Opus. Then Phase C (round-1 guided: breath-first + gentle relax sweep) and Phase D (round-2: meditation baby-stepped via `blueprint().practiceNovice` + mantra).

### 2026-07-09 later — onboarding cleanup shipped (v982, Opus) after David's device review
His feedback: messy, unnecessary pages/questions, a menu repeating what was established, an asymmetrical top bar that filled before the flow ended, and an idea to slide screens like an IG story. Done:
- **Progress bar = one continuous whole-flow track** (equal ticks, one per non-intro beat, fills exactly at the final beat). Replaced the uneven 3-section battery. `paintBar` now just lights `tickEls[i]` for `i < bi2`.
- **Trimmed:** the 3 per-section echoes (constel is the single recap), `plantom`, `mint` (seed is the sole closer), and the `bed` + `ingredients` survey questions. `voice` pulled up next to the questions. Flow now = intro -> pace -> gender/age/stage/experience/vibe/challenges/overwhelm -> voice -> constel -> plan -> seed (**12 ticks**).
- **Watch-out:** cutting `ingredients` removed the starter-plan's good-days personalization; `planItems` now leans on friction + vibe + fallback (still renders 3 rows). Restore that one Q if the plan feels thin.
- **OWED (David said "next pass"): the IG-story slide** — advancing should physically slide the current screen left and bring the next in from the right. Touches the beat-render loop (`drawObV2` currently `body.innerHTML=""` per beat); do it as its own careful pass, verify feel on device.

### 2026-07-09 — NEXT BUILD LOCKED: **first stone = "Intro" — Phase B rebuild (FABLE, do NOT build on Opus)**
David chose the full first-stone rebuild from `_specs/SPEC-FIRST-STONE-BUILD-2026-07-08.md` (Phase B). Two decisions locked:
1. **Rename the first stone `fd0`** title "Lesson 1 · The Switch" -> **"Intro"** (in `firstDayNodes`, ~L882, + the RU dict entries). Open question for David: renumber `fd1`-`fd4` to "Lesson 1-4" (so it reads Intro, Lesson 1..4) or leave them "Lesson 2-5"? Currently a gap.
2. **Kill the orb+spinning-rays ending.** The `fd0b` lesson ends on a `seal` beat = `orbEl(46)` (the ball) sitting on the lesson overlay's `gspin` ray field (added in `runLesson` ~L913, behind every beat). Replace fd0b's final beat with a **CLEAN FORWARD LINE close**: no orb, no spinning rays (hide `.gspin` on that beat), one warm forward line per the spec ("close on a real forward line, no meta"). Copy through BOTH gates.
- **Phase B scope (the carousel opening, Fable regression zone):** Tab-0 typewriter text hook (not an orb) -> "Do you have thirty seconds to try this?" -> 30s/60s/120s + skip chooser -> press-hold-to-commit morphing into the player orb -> the guided stack (breath-first, then gentle relax sweep). Reuse `firstDayStack`/`timelinePlayer`. REGRESSION CONTRACT: this is the 3x-rebuilt carousel; spec-first, boot-verify only, gesture feel DEVICE-UNTESTED. One focused Fable session; do not also cram Phase C/D.

### 2026-07-09 — **Phase B SHIPPED on Opus (v983)** — David had no Fable access, said "do it anyway" (his override, I'd flagged Fable)
Built with regression-zone care by reusing the carousel UNTOUCHED and only rewrapping `firstDayStack`'s opening + ending:
- New flow: typed HOOK (3 EPIC-anchored tension lines, word-by-word via `.obi-w`, no orb) -> OFFER "Do you have thirty seconds to try this?" -> `30s / 1 minute / 2 minutes` + `not now` -> tap a chip reveals the press-hold COMMIT ring (`.ob-pwrap`, reused seal gesture, 1.5s) -> `launch()` runs `runFirstStack([breath, relax], ...)` (breath-first, secs split from the choice) -> the proof `stackRecap` = the clean forward close. Node renamed to **Intro**; `runLesson(fd0b)` (the orb+spinning seal) removed from the node act.
- **VERIFIED (preview/boot only):** syntax ok, zero console errors, hook/offer/commit all render, node reads "Intro". **DEVICE-UNTESTED (tell David):** the press-hold gesture FEEL, the breath+relax carousel, and the audio — synthetic pointer events do not fill the ring in preview (expected). Confirm on the phone.
- **DEBT / OWED:** (1) `firstDayStack` still contains the OLD `showIntro`/`showPlan` opening as **retained-but-dead code** (kept to avoid transcribing the fragile plan-UI) — delete in a clean pass. (2) `tr("Intro")` has **no RU entry** yet (shows "Intro" in RU mode). (3) Renumber `fd1`-`fd4` "Lesson 2-5" -> "Lesson 1-4"? still open. (4) The IG-story slide still owed. (5) Per-tool why+results deepening (Phase C polish) not done.

### 2026-07-09 — **Phase D (round-2 escalation) SHIPPED on Opus (v984)** — David asked where meditation/mantra/gratitude went; chose "let it adapt"
After the breath+relax recap, `stackRecap`'s button now -> `showEscalation` (not straight to onDone):
- **Adaptive** via `blueprint()`: `lowDoor = lowStart || vibe stuck|overwhelmed` -> **gratitude + a baby-stepped sit** (`med.med=[{k:'settle'}]` for `practiceNovice`); else -> **stillness + mantra** (mantra last). Offer -> `runRound2` -> `timelinePlayer(composeStackSegs(list2))` (no second gauge; the carousel already supports `gratitude`/`mantra`/`medit` via STACK_CONTENT) -> logs to day -> `showPower` names the mechanism + `earn(6)` -> onDone. `I'm good for now` skips.
- Copy both gates (judge killed the gratitude power line twice; final: "You found a few good things and gave them a quiet minute. Naming them is what makes them stick.").
- **DEVICE-UNTESTED:** the whole round-2 chain (offer -> carousel -> power) sits behind the device-only carousel + hold; preview can't reach it. Syntax ok, boots clean, opening flow intact after the recap rewire.

### 2026-07-09 — Intro POLISH pass (v985-v989), all Opus, all from David's device notes
The "Intro" stone (`firstDayStack`) flow is now: **HOOK -> OFFER(pick time)+clarity -> HOLD(commit) -> breath+relax carousel -> proof RECAP -> ROUND-2 offer(context+pick time)+HOLD -> round-2 carousel -> POWER -> onDone.**
- **v985:** killed the "30 seconds?" vs 30s-chip redundancy; gauge010 rebuilt slider -> tappable graded bars.
- **v986:** hook reveal is now opening-style — lines one-at-a-time, FAST word-cascade within each (`.obi-w` at 0.045s/word), key words colored (HUES: tension/edge pink, tuned/personality/wound yellow) + sized up (BIG map 1.16-1.28em), tap-to-fast-forward. Knobs: the 0.045 cadence, the HUES/BIG maps in `showHook`.
- **v987:** BACK nav — `addBack(fn)` puts an `.ob-back` chevron (fixed top-left, tracked by `backEl`, cleared in `clearBoth`) on offer+commit; hook (first) has none. Offer reframed to "How much will you commit?"; hold simplified to just "Press and hold to commit."
- **v988:** offer sub clarifies the target ("to tell your body the emergency is over."). Round 2 now MIRRORS round 1: context sub on the two tools -> its own time picker -> the SAME press-hold. `showCommit(secs, onCommit, backFn)` is generic; `runRound2(totalSecs, lowDoor, bp)` splits the time across its two tools.
- **v989:** gauge010 supports press-and-DRAG (pointerdown/move + setPointerCapture, finger x -> level 0-10, `touch-action:none`); tap still works.
- **STILL OWED:** (1) the full IG-story tap-navigation + slide (David keeps circling it; the `addBack` groundwork is in — next natural step). (2) dead `showIntro`/`showPlan`/`showRecap` in `firstDayStack` (delete). (3) `tr("Intro")` RU entry. (4) renumber `fd1`-`fd4`? (5) all the device-untested gesture/carousel/audio bits confirmed on the phone.

### 2026-07-09 — Intro second-half DEEPENED + breath circle (v990-v993, Opus)
The **Intro** (`firstDayStack`) is now, end to end: HOOK (animated) -> offer "How much will you commit?" + "to tell your body the emergency is over." -> hold ("commit to a breath, then your body") -> pre-rating -> breath+relax carousel -> `showEscalation` "Keep the momentum. Complete the set." + "a short meditation, then a mantra to carry." -> time -> hold ("commit to a meditation and a mantra") -> **PRIME meditation -> meditate -> PRIME mantra -> mantra** -> post-rating -> `showClose` (proof + strong animated ending). All guardian prose animates via `animLines`/`narrate` (word-cascade, `HUES_ALL`/`BIG_ALL` color+size).
- **v990:** rating moved to bookend both halves; round 2 was pick-two (later replaced).
- **v991:** deep per-tool copy + strong close + commit-names-what + `animLines`/`narrate` extracted from the hook.
- **v992:** `@keyframes breatheReal` (16s = 4-4-6-2) on the timelinePlayer orb; breath segs tagged `breath:"in/hold/out/rest"`; `paintNow` restarts the orb on each "in" cue so it expands with the inhale + keeps breathing through the other tools. Standalone `breathwork` (own overlay) untouched. Meditation ordered before mantra.
- **v993 (David's latest):** round 2 is now a GUIDED meditation->mantra, each PRIMED before it (gratitude cancelled, pick-two dropped). `MED_PRIME` = Sam Harris "don't author your next thought / narrating and calling it thinking" + two-gears science; `MANTRA_PRIME` = Withers inherited-voice + Dispenza rewire. `runGuidedPair` -> `narrate(prime)` -> `runOneTool` per practice.
- **ALL device-untested** behind the carousel+hold. `showExplain`/pick-two/`stackRecap` now dead/removed. Every line passed both copy gates; the Gate-2 judge (Haiku, reads COPY-ANCHORS) called the priming DEEP/EPIC.
