# ALTER decisions
Append-only log of settled decisions, each with its evidence. `/standup` never re-litigates anything here; if a decision needs reversing, add a NEW dated line that supersedes the old (never delete). Format: `date · decision · EVIDENCE: source`.

- 2026-07-11 · **Positioning line LOCKED:** "Planners organize your day. ALTER trains the person living it." · EVIDENCE: David's pick, FOUNDATION-PITCH §7.
- 2026-07-11 · **Category term:** "daily planner, habit tracker, and mental fitness trainer in one." · EVIDENCE: live App Store copy of 11 category apps; "self-help/self-care" used by zero winners, FOUNDATION-PITCH §5.
- 2026-07-11 · **Streaks kept, punishment removed:** two streaks only (Planned Ahead = hero, Followed Through = quiet), repairable, never zeroed. · EVIDENCE: streak-backfire is the shatter-and-shame, not the streak; Gollwitzer implementation intentions d=0.65, FOUNDATION-PITCH §3.
- 2026-07-11 · **Launch on PWA + Stripe, NOT the App Store wrap, late August.** · EVIDENCE: 0% Apple web-checkout is legally unstable; RevenueCat/Dipsea shows IAP converts fine anyway; wrap = 1-3wk + 4.2 rejection risk, BUSINESS-MASTERPLAN §2.
- 2026-07-11 · **Founding lifetime ladder in tranches:** ~30 at $49 → ~40 at $69 → ~30 at $99, published upfront, never reissued. · EVIDENCE: undercharging = the most-repeated pricing regret; business canon PART 1.
- 2026-07-18 · **AI goal-architect = the primary PAID magnet; guardian voice = FREE.** (PROPOSAL — confirm before fully load-bearing.) · EVIDENCE: free core wins the category (Finch); paid buys depth, MASTER-GAMEPLAN §2.
- 2026-07-19 · **Mom invested $1,000 = FLAME tier, build-funder role, return-first family terms.** Terms option (A/B) still PENDING David. · EVIDENCE: her transfer; deck she approved, [[alter-mom-investor-pitch]].
- 2026-07-19 · **Start Apple Individual enrollment now ($99), but the wrap build stays gated** on tripwire #5. · EVIDENCE: account has lead time (optionality); wrap has no user demand yet, BUSINESS-MASTERPLAN §2.
- 2026-07-19 · **Next build = B1, the PM close, to book-grade spec.** · EVIDENCE: thinnest leg of the core loop + ~10-book convergence, MASTER-GAMEPLAN §3.
- 2026-07-21 · **NAV LOCKED = the compass rose.** Kill the bottom tab bar; home is the center of a world, journey up / tools down / planner left / garden right, visible doors + edge peeks. SUPERSEDES the 07-20 five-slot bottom-nav ruling. · EVIDENCE: David's verdict on the interactive mockup; his own concept, DESIGN-STUDIO-2026-07-21 §2.
- 2026-07-21 · **Guardian puck = the shape-shifter (fusion).** One bottom-left object: home button idle, mini-player during a session, live dial while tracking. · EVIDENCE: David's verdict on the mockup toggle; one-live-time-object GUARD LAW, HOME-PLAYER-GRAMMAR §4b.
- 2026-07-21 · **Settings door = guardian-mark avatar on home's status row, by the gems.** Garden stays a pure place. · EVIDENCE: David's verdict; "where the account lives," DESIGN-STUDIO §2.
- 2026-07-21 · **Planner plan-vs-reality = ONE block, battery fill (not two lanes) for future + now.** Retires the doubled-up "Focus block / Focus block". · EVIDENCE: David's verdict; matches the shipped one-bubble partial rule + [[timeline-battery-principle]]. **OPEN NUANCE (David):** the PAST may still encode planned-vs-actual for real habit tracking — direction = the battery IS the record (undershoot leaves the frame unfilled, overshoot spills past the planned end, late start = dim head), so it stays ONE object, not a second lane. Verdict at the planner-feel (D3) build with a mockup.
- 2026-07-21 · **PLAN↔REALITY LAW LOCKED: "reality fills, intention flows."** Tracking is one tap, never a replan ceremony. The past keeps only lived reality (one lane, battery grammar: late head / unfilled top / overshoot spill / empty frame for untouched plans). A displaced or unlived planned block auto-FLOWS forward (later today if it fits, else the PM-close carry basin: "carry to tomorrow or release"), whisper toast + undo, future-side only so regression contract #2 holds. Every divergence is logged as a planned/lived pair and surfaces in the PM close, per-activity stats, and (B2-era) guardian pattern talk; the timeline never wears the delta. · EVIDENCE: David "locked" 2026-07-21; MI readiness-gated planning + never-punish canon; the positioning line itself ("the planner built for the day you don't follow the plan").
- 2026-07-21 · **BEAUTY IS A LAUNCH GATE (David reverses the slip rule).** The app must be beautiful AND functional before launch; polish does NOT slide to post-launch. Reformed rule: if a week slips, FEATURE BREADTH slides to founder-updates (the AI architect etc. were already Sept), never the beauty or the core; the new nav never ships half; the date flexes 1-2 weeks before the beauty bar does. · EVIDENCE: David's verdict, "no major hiccups," DESIGN-STUDIO §4 + MASTER-GAMEPLAN §3B.

- 2026-07-21 · **Mom terms: NONE.** No A/B formal terms; family money, return-first as spirit only. Supersedes the 07-19 PENDING terms line; the deck's terms slide is moot. Standup stops asking. · EVIDENCE: David 2026-07-21 ("mom doesn't need any terms").
- 2026-07-21 · **REDESIGN FIRST, B1 AFTER.** The R-track (R1 physics → R2 broom → R3 compass rose → R4 toolbox → R5 planner feel) runs BEFORE B1/B4/B3. The free/paid + founding-page-promise call is DEFERRED to the B4 session, decided on the real page. Calendar slides right; the date flexes before the beauty (per the beauty-gate rule above). · EVIDENCE: David 2026-07-21 ("redesign first... tired of the app looking like it's designed by twenty people"); spec `_specs/_newera-build/REDESIGN-BUILD-2026-07-21.md`.

- 2026-07-21 · **THE LIVING PUCK grammar (refines the shape-shifter pick):** the bottom-left puck is a COMPACT pill (52px tall, extends sideways only, never a bottom bar; the old wide dock retires). The disc always wears the color of what matters now: current activity while tracking, the NEXT planned block when idle (with its icon), gold on pause, plain pink home only when nothing. **Disc tap = act on the thing** (start next / pause current / resume); **the tail = always home.** · EVIDENCE: David 2026-07-21 verbatim ("the circle becomes the color of that thing... if you click circles, you start tracking"); one-live-time-object GUARD LAW.
- 2026-07-21 · **THE ONE-HOME LAW:** #trackerFull full-screen ALWAYS wears the home frame (status row · story strip+chevron · journey/garden doors · avatar · circle stage; grid on calm states). State changes ONLY the circle contents + the line + one control row. There are no "other faces" for the user to land on. · EVIDENCE: David 3x "home still looks like the old cockpit"; root = six faces, only idle carried the frame.
- 2026-07-21 · **BUILD IN WAVES + BATCH FEEDBACK (supersedes per-slice device gating).** Parallel worktree agents per disjoint region, one integrator, one ship per wave, David tests against a 10-min checklist and returns ONE feedback batch. Safety: one commit per parcel (bisectable), kill-switch flags on structural slices, DEVICE-UNTESTED honesty batched not skipped, ≤1 SCHEMA-touching parcel per wave. Seats: Fable plans waves, Opus orchestrates + codes risky parcels, sonnet/haiku sweep + copy + checklists. · EVIDENCE: David 2026-07-21 ("agent swarms... batch feedback"); the proven v572 worktree-batch pattern; protocol `_specs/_newera-build/SWARM-PROTOCOL-2026-07-21.md`.

## PENDING (owed by David — pruned 2026-07-21)
- Free/paid split + founding-page promise: decided AT the B4 founding-page session, on the real page (not before).
- WTP round greenlight (family + warm circle, before the ladder locks) — parked until the B-track resumes.
- Investor deck: real terms numbers + tranche amounts — only if/when the deck is sent to a real investor.
- Dev-feel: sound pass lands inside D4; menu unification lands inside R4. No separate decision needed anymore.

- 2026-07-22 · **RU voice = ONE male izo voice, gender-aware TEXT (not two voices).** User-gender is handled by 70 gendered lines (male + ru_f female variant, picked by onboarding `P.gender`); 431 lines neutral; the same male voice reads both = 571 clips, not a second bank. A female VOICE deferred (addable later like millie). · EVIDENCE: David's pick 2026-07-22 ("izo only for now"); morphology classifier (only 70/501 lines gender-dependent); mom = RU persona, day-one contract.
- 2026-07-22 · **PENDING — voice DELIVERY/emotion strategy.** Confirmed live that izo supports v2 per-profile settings AND eleven_v3 emotion tags. Options in RU-VOICE-IZO spec (A flat / B v2-profiles / C v3-tags / recommended hybrid). David hearing the A/B/C demo before the call; nothing generates full-bank until he picks. · EVIDENCE: David 2026-07-22 (the Robbins/emotion question); live izo probe.

- 2026-07-28 · **PUCK REVERSAL: off home with something coming up, the corner button is the plain PINK HOME and navigate-only.** It no longer wears the next block's hue, no longer carries the play badge, and its disc tap only goes home (no start-the-block). The hue + play duty moved to the HOME DISC (the FP3 idle-with-a-plan face, which wears the next block's hue + stripes and starts it). Reverses the 2026-07-21 LIVING PUCK grammar for the idle-with-upcoming case ONLY; the tracking face (activity color + elapsed) and the gold paused face are UNCHANGED. · EVIDENCE: David 2026-07-28 direct report, verbatim: "If you have something coming up, the home button should still look like the pink home when you're not in home... clicking the home button only takes [you home]".

- 2026-08-15 · **THE DESIGN-PORT SYSTEM IS VERDICTED — the 2c saga is closed.** The months-long "app doesn't match my design" class ended at v1286 with five stacked causes fixed and locked: (1) design prototypes are RUN, never interpreted (LAW 8 + _dev/DESIGN-PORT-CHECKLIST.md); (2) artboards port as absolute px in centered columns, never vw/vh; (3) fidelity is verified by machine position-diff at the artboard's own geometry (_dev/design-diff-2c.js) plus 75 designAudit gates; (4) renderer razor edges (text wrap, zoom-vs-dvh) get pinned, and scaling is transform-only; (5) a wider phone FILLS via --tfscale uniform scale (his 440x956 Max = the 402x874 artboard × 1.0945 exactly) instead of letterboxing. The on-device audit overlay (📐 dev menu, COPY REPORT) is the standing instrument: the phone reports its own numbers before any guess ships. · EVIDENCE: David on device 2026-08-15, verbatim: "yay it works now only took a couple months please save this" — the first-ever on-device match confirmation; his phone's own 0-FAIL audit report (440x956) was the finding that located the final cause.

- 2026-08-16 · **BACKFILL: TAP = IT HAPPENED (variant 2A), instant fill + quiet undo.** Tapping a past planned block on the timeline fills and stripes it immediately; a quiet undo hangs for a moment. Rejects the 2B two-chip flyout ("Did it / Skip"). Rationale, David and the design agreeing independently: the tap IS the answer, a flyout asks twice, and undo covers the slip. Scope stays ONE moment — backfilling an untracked gap — after Round 20 was rejected for over-building ("everything is messy ugly and confusing"). The timeline's existing look is untouched; the new moment renders on top of it. · EVIDENCE: David's pick 2026-08-16 on the Round 21 frames, choosing the design's own MY PICK tag.
- 2026-08-16 · **THE HOUR GUTTER IS 12-HOUR BY DEFAULT, USER-SWITCHABLE TO 24, with a minimal am/pm signal only where the meridiem flips.** Partially reverses v1308, which made the gutter 24h to fix a real ambiguity (a coarse zoom read "4 9 2 7 12" with no way to tell morning from night). David's reference screenshot shows 12-hour numerals and his frame + words outrank the fix per the FRAME-WINS ORDER, so 12h returns as the DEFAULT and the ambiguity is solved his way instead: the am/pm tag rides only the switch points (12:00 and 00:00) rather than every label. Open edge case for the build: when the label interval skips the switch hour (5-hour spacing can omit 12 entirely), the marker must ride the first label AFTER the switch, or the ambiguity returns exactly where v1308 found it. Setting lives in settingsSheet. · EVIDENCE: David 2026-08-16, verbatim: "i want the user to choose between 12 hour and 24 hour but default is 12 hour with a minimal am pm signal when it switches"; his own timeline screenshot.
- 2026-08-16 · **THE GAP CARRIES ONE WHISPER (variant 3A).** An untracked stretch on the timeline shows a single faint inline line; tapping it opens the existing activity picker. Rejects 3B (pure silence) and the three treatments the design added beyond the brief (pill, slot frame, nudge) — all cut. This is the ONLY new chrome the backfill moment introduces: a line inside the empty stretch, never a card over the day, never a popup. Backfill still sizes itself to the whole empty stretch by default. · EVIDENCE: David's pick 2026-08-16 on the Round 21 frames, verbatim: "the empty stretch carries one faint line; tap it and the picker opens. The other treatments (silence, pill, slot frame, nudge) are cut."
- 2026-08-16 · **THE R21 CATEGORY PICKER *IS* THE ACTIVITY PICKER — no new design round.** David's "the activity picker is too overwhelming" referred to the OLD full-screen picker (8 families, 100+ activities, tabbed sections, sideways-scrolling chip rows that clip mid-word). The replacement already exists: the Round 21 category-first sheet he designed (eight domain tiles, ignite selection law, multi-select, naming deferred via "tap to name it"). This is a BUILD task, not a design task. Claude's round-22 brief asking to redesign the picker was a MISREAD and is deleted. Also killed by David in the same breath: **no "recents" anywhere** (Claude introduced it from a `frequent()`-counts-by-title finding; David never asked for it and does not want it) and **no accumulating "ON YOUR PLATE" tray with a button that morphs into "Arrange"** — a picked tile is already ignited, so the tray shows the same thing twice, and a primary action must not change identity by count. Open, and the one real gap: the R21 sheet's copy is PAST-tense because it was designed for backfill; used for planning ahead it needs a forward framing. Copy gates decide the wording. · EVIDENCE: David 2026-08-16, verbatim: "I don't like how it's taking up too much space... you simply choose which category and that's it... I did not mean in reference to the way we designed it recently in round twenty one. I like the way I designed it. It looks great... I was talking about the old prompt activity picker, which took up the full screen and was a bit overwhelming."
- 2026-08-16 · **THE DAY DOES NOT END AT MIDNIGHT: blocks flow past it, inside the same logical day. No clamp, no refusal.** David, verbatim: *"Yes. I was testing late at night, but the app shouldn't restrict you from adding activities past your own bedtime. It should treat you like an adult and let you plan whatever you want."* This CLOSES the three-way fork left open by the 2026-08-16 video audit (a: stay in tonight's logical day · b: move to tomorrow's date key · c: refuse with "no room left today"). **Verdict = (a).** It also confirms the root cause of the "two activities stack and dragging cannot unstack them" bug he reported the same day: `reflow`'s closing `s = Math.min(1410, s)` caps every start at 23:30, so late-evening additions pile onto one minute AND are re-piled on the next save, which is why dragging them apart does not hold. The same clamp is what inflates the live tracking bubble to fill the screen (N blocks sharing a minute emit N knots at that minute, each +MINFLOOR 100px, and the live bubble's `flowSpan` reads across all of them) — one bug, three symptoms. THE FIX: run the timeline in WINDOW minutes (the existing 4am→4am `dayWindow`/`toWin` space) instead of raw 0-1439 clock minutes, so 00:30 sorts and draws after 23:30 within the same logical day. Touches `computeFlow` (its `items[].s`), `reflow` (sort key, `_bs`, the pins list, the closing clamp, and the write-back as `s % 1440`), `pkOpen`/`pkArrTimes`' 1410/1439 caps, and the ~4 draw sites that pass raw `hm(b.time)` into `flowY`/`topFor`. This is the timeline REGRESSION ZONE (rebuilt 3x) — all four regression-contract items must be re-verified, and gesture feel stays DEVICE-UNTESTED. Note the existing latent bug this also fixes: `reflow` already compares raw `_bs` against window-space `_now`, so a 00:30 block is currently mis-classified as deep past. · EVIDENCE: David 2026-08-16 device report + verdict.
- 2026-08-20 · **THE VITAL SURVIVES: unhook it from the settings gear, delete nothing.** David: *"Conceptually, I like the Vital. I don't like its name, but I'm planning to add that to one of the designs. But first I would need to design it in Claude Design."* So the You-menu port (Settings Menus.dc.html) takes over the gear's door, and The Vital's surface, state and virtue ladder stay exactly as they are, simply unreferenced from that entry. It re-enters through whichever row David designs for it, under a new name he has not chosen yet. **Any agent touching the settings door must not remove The Vital's code as "dead" — it is parked, not retired.** · EVIDENCE: David 2026-08-20.
